import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
import fs from 'node:fs';

const BASE='http://127.0.0.1:3000';
const source=fs.readFileSync(new URL('./chatbox-100-live.mjs',import.meta.url),'utf8');
const declaration=source.indexOf('const questions');
const arrayStart=source.indexOf('[',declaration);
function matchingArrayEnd(text,start){let d=0,q=null,e=false;for(let i=start;i<text.length;i+=1){const c=text[i];if(q){if(e)e=false;else if(c==='\\')e=true;else if(c===q)q=null;continue;}if(c==='"'||c==="'"||c==='`'){q=c;continue;}if(c==='[')d++;else if(c===']'&&--d===0)return i;}return-1;}
const end=matchingArrayEnd(source,arrayStart);const parsed=Function(`return (${source.slice(arrayStart,end+1)})`)();if(!Array.isArray(parsed)||parsed.length<100)throw new Error('Canonical suite has fewer than 100 questions');const questions=parsed.slice(0,100);
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function ready(){for(let i=0;i<150;i++){try{if((await fetch(`${BASE}/health`,{signal:AbortSignal.timeout(1000)})).ok)return;}catch{}await sleep(200);}throw new Error('TonyAI server did not become ready');}
const ignored=new Set(['what','does','this','that','with','from','into','when','where','which','about','explain','define','difference','between','should','would','could','there','their','than','then','have','your','will','most','some','used','using','make','more','less']);
const words=s=>String(s).toLowerCase().match(/[a-z0-9]+/g)||[];
const validate=(q,a)=>{a=String(a).trim().toLowerCase();if(a.length<12)return'answer too short';if(/live search unavailable|search unavailable|unable to search|no results available/.test(a))return'search failure';const candidates=[...new Set(words(q).filter(w=>w.length>=4&&!ignored.has(w)))];return candidates.length&&!candidates.some(w=>a.includes(w))?'answer has no query-term overlap':null;};
const server=spawn(process.execPath,['server.js'],{cwd:process.cwd(),stdio:'inherit',env:{...process.env,PORT:'3000'}});let browser;
try{await ready();browser=await chromium.launch({headless:true});const context=await browser.newContext({locale:'en-US'});context.setDefaultTimeout(12000);context.setDefaultNavigationTimeout(15000);await context.setExtraHTTPHeaders({'Accept-Encoding':'identity'});await context.route('https://duck.ai/**',r=>r.fulfill({status:200,contentType:'text/html',body:'<!doctype html><title>Duck.ai CI</title>'}));const page=await context.newPage();await page.goto(`${BASE}/index.html`,{waitUntil:'domcontentloaded'});const input=page.locator('#messageInput');if(await input.count()!==1)throw new Error('Chatbox input missing');await page.addScriptTag({url:`${BASE}/engine/chatresponse-client.js`});await page.waitForFunction(()=>Boolean(window.TonyAIChatResponse?.search&&window.TonyAIChatResponse?.duckAiUrl));
let passed=0;for(let i=0;i<100;i++){const q=String(questions[i]);await input.fill(q);const before=await page.locator('[data-chatresponse]').count();const duck=await page.evaluate(text=>window.TonyAIChatResponse.openDuckAi(text),q);const expected=new URL('https://duck.ai/chat');expected.searchParams.set('prompt','1');expected.searchParams.set('q',q);if(duck!==expected.toString())throw new Error(`Question ${i+1}/100 Duck.ai URL mismatch`);await page.evaluate(text=>window.TonyAIChatResponse.search(text),q);await page.waitForFunction(({count})=>document.querySelectorAll('[data-chatresponse]').length>count,{count:before});const row=page.locator('[data-chatresponse]').last();await row.locator('[data-chatresponse-status]').waitFor({state:'detached',timeout:8000}).catch(()=>{});const answer=await row.innerText();const error=validate(q,answer);if(error)throw new Error(`Question ${i+1}/100 failed: ${error}\nQ: ${q}\nA: ${answer}`);passed++;console.log(`DUCKAI_TEST ${passed}/100 PASS`);}
console.log('DUCKAI_TEST_RESULT 100/100 PASS');await context.close();}finally{await browser?.close().catch(()=>{});server.kill('SIGTERM');await sleep(250);if(!server.killed)server.kill('SIGKILL');}
