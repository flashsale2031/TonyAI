# TONY

Server-side API integration for the TONY chat UI. Do not put the OpenAI API key in browser code.

## Run

```bash
npm install
OPENAI_API_KEY=your_key npm start
```

Then open `http://localhost:3000`.
