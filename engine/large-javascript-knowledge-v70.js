// TONY Large JavaScript Knowledge v70.
// Authored deterministic JavaScript/web engineering knowledge; not pretrained neural data.
const packs={
 language:[['ToPrimitive','JavaScript converts objects to primitive values through well-known conversion hooks before many operators perform their work.'],['NumericSeparators','Numeric separators improve readability without changing numeric value.'],['SafeInteger','Number.isSafeInteger identifies integers exactly representable within JavaScript’s safe integer range.'],['BigIntMix','BigInt arithmetic is separate from Number arithmetic and should not be mixed implicitly.'],['UnicodeNormalization','Unicode normalization can matter when comparing human text that has multiple equivalent representations.'],['IntlSegmenter','Intl.Segmenter can perform locale-aware segmentation for supported scripts.'],['IntlCollator','Intl.Collator provides locale-sensitive comparison rather than assuming code-unit ordering matches human sorting.'],['StaticBlocks','Class static initialization blocks run during class definition and can initialize related static state.'],['Accessor','Getters and setters expose property access through functions while preserving property syntax.'],['PrototypePollution','Dictionary-like objects and untrusted keys should be handled carefully to avoid modifying inherited application behavior.']],
 modules:[['ModuleRecord','A module is parsed and linked before evaluation, so syntax and dependency errors can appear before application code runs.'],['ExportStar','export * forwards selected exports but does not forward a default export automatically.'],['NamespaceReexport','Named re-exports can create a stable public surface while internal modules remain private.'],['ImportAttributes','Where supported, import attributes communicate additional information about imported resources.'],['ModulePreload','modulepreload can prepare module dependencies before evaluation when used for genuinely critical paths.'],['CycleBarrier','A module cycle is safest when initialization does not depend on a still-uninitialized imported binding.'],['PackageSelfReference','Package self-reference can provide stable internal imports through the package export map.'],['ConditionOrder','Conditional export resolution depends on the declared condition ordering and environment support.'],['BundlerBoundary','Bundlers may transform module graphs, so source-module assumptions should be validated against the production build.'],['DynamicBoundary','Dynamic import creates an explicit asynchronous boundary that can be used for feature loading and code splitting.']],
 async:[['AbortControllerReuse','An AbortController should not be reused for unrelated operations after it has been aborted.'],['AbortComposition','Composed cancellation lets parent operations terminate child work when ownership ends.'],['PromiseResolution','Promise resolution can assimilate thenables, so completion may involve additional asynchronous steps.'],['AsyncFinally','Async cleanup in finally should itself be bounded when resources have strict deadlines.'],['IteratorClose','Early termination of iteration can invoke iterator cleanup and should be considered when iterators own resources.'],['QueueMicrotask','queueMicrotask schedules work after the current synchronous turn but before later task opportunities.'],['TaskYield','setTimeout or scheduler-style yielding can provide task boundaries when microtasks are insufficient for responsiveness.'],['RetryAfter','Retry policies should honor server-provided retry timing when it is trustworthy and compatible with the client deadline.'],['Fanout','Fan-out concurrency should be bounded so one request cannot create unbounded downstream work.'],['SingleFlight','Single-flight coordination can collapse concurrent identical reads and prevent duplicate expensive work.']],
 browser:[['Focus','Keyboard and assistive-technology flows depend on deliberate focus ownership and restoration.'],['FocusVisible','The focus-visible state can distinguish keyboard-oriented focus indication from pointer-oriented interaction.'],['Inert','The inert attribute can remove a subtree from interaction and accessibility navigation while an overlay owns the user interaction.'],['ResizeLoop','ResizeObserver callbacks should avoid changing observed size indefinitely without a stable convergence condition.'],['IntersectionRoot','IntersectionObserver can use an explicit root to measure visibility relative to a scroll container.'],['MutationBatch','MutationObserver delivers batches of records, so consumers should process all relevant records before scheduling follow-up work.'],['CustomEventDetail','CustomEvent detail can carry application data but should be treated as an explicit event contract.'],['FormSubmit','The submit event represents form submission and validation should be coordinated with native constraints when possible.'],['BeforeUnload','beforeunload is intentionally constrained and should not be treated as a general-purpose lifecycle persistence mechanism.'],['PageTransition','Page lifecycle transitions can race with asynchronous work, so cancellation and state persistence should be coordinated.']],
 web:[['FetchRedirect','Fetch redirect behavior should match the intended trust boundary and credential policy.'],['CredentialsMode','Fetch credentials mode controls whether ambient credentials participate in cross-origin requests.'],['ReferrerPolicy','Referrer Policy controls how much referrer information is sent with requests.'],['OriginHeader','Origin is useful in selected security checks but must be interpreted according to the protocol and request type.'],['PreflightCache','CORS preflight results can be cached, so server policy changes may not appear immediately to every browser client.'],['ReadableBody','Streaming a response body can reduce latency to the first useful output while preserving backpressure.'],['AbortFetch','Passing an AbortSignal to fetch connects request cancellation to application ownership.'],['CacheAPI','CacheStorage provides script-controlled request/response caches distinct from ordinary HTTP cache behavior.'],['ClipboardWrite','Clipboard writes may require user activation and should expose a useful fallback when unavailable.'],['FileAccess','File System Access APIs are permission-sensitive and should treat selected handles as capabilities rather than trusted arbitrary paths.']],
 streams:[['PipeTo','ReadableStream.pipeTo can connect producer and consumer lifecycles while propagating cancellation and errors according to its options.'],['PipeThrough','pipeThrough composes a readable stream through a TransformStream without manually wiring every chunk.'],['WritableClose','WritableStream close signals that no more chunks will be written and should release downstream resources.'],['WriterLock','A writable stream writer owns a lock and should release it when ownership ends.'],['DesiredSize','Writable stream desiredSize provides a signal for whether producers should slow down.'],['ByteLength','Binary stream framing should account for byte length rather than assuming character count equals encoded byte count.'],['DecoderState','Incremental text decoding must preserve incomplete multibyte sequences between chunks.'],['JSONLines','Line-delimited JSON can be parsed incrementally but requires framing and per-record error policy.'],['ChunkRetry','Streaming retries require careful framing because replaying a partial stream may duplicate already delivered records.'],['TransformErrors','Transform streams should surface parsing or transformation failures instead of silently dropping malformed chunks.']],
 node:[['HeaderLimit','Header size limits reduce exposure to oversized metadata before request routing or parsing.'],['BodyStreaming','Streaming request bodies avoids buffering entire uploads when downstream processing can also be incremental.'],['BackpressureNode','Node streams use backpressure signals to prevent fast producers from overwhelming consumers.'],['Pipeline','stream.pipeline coordinates completion and error propagation across connected streams.'],['DNSCaching','DNS caching behavior can affect connection reuse and should be understood when diagnosing endpoint latency.'],['SocketTimeout','Socket inactivity and request deadlines are different controls and may both be required.'],['SignalHandling','Signal handlers should perform bounded coordination and avoid starting long unbounded asynchronous shutdown work.'],['EnvParsing','Environment variables arrive as strings and should be parsed and validated at configuration boundaries.'],['WorkerTransfer','Worker threads can transfer selected ArrayBuffer-backed resources to avoid copying large binary payloads.'],['Diagnostics','Node diagnostic channels can provide low-level observability hooks without requiring every call site to add instrumentation.']],
 security:[['Capability','Treat access to privileged handles, tokens, and resources as capabilities that should cross explicit boundaries.'],['InputSchema','External JSON should be schema-validated before business logic relies on its structure.'],['PrototypeKeys','Keys such as __proto__ and constructor require care when external objects are merged into dictionaries.'],['RedirectTrust','Following redirects across origins can alter credential and trust assumptions and should be policy-controlled.'],['CookieScope','Cookie Domain and Path attributes determine where ambient cookie state is sent and should be minimized.'],['SessionRotation','Sensitive session identifiers should support controlled rotation after meaningful security events.'],['CspNonce','CSP nonces must be unpredictable per response and should never be reused as application data.'],['SRIHash','Integrity hashes verify bytes, not the semantic safety of the downloaded program.'],['LogRedaction','Redaction should happen before sensitive data enters durable logs or telemetry buffers.'],['RateLimitCost','Rate limits should consider expensive operations separately from cheap requests so attackers cannot shift load into costly endpoints.']],
 performance:[['EventDelegationCost','Delegation reduces listener count but can increase path traversal work, so measure on realistic event rates.'],['DOMBatch','Batching DOM mutations into a single commit reduces repeated style and layout invalidation.'],['TextMeasure','Repeated text measurement can become expensive and should be cached when font and content inputs are stable.'],['ImageDecode','Image decoding can compete with responsiveness and should be scheduled according to visible priority.'],['VirtualWindow','A virtualized list should keep its live node count bounded even as the logical dataset grows.'],['Prefetch','Prefetch is speculative work and should yield to critical requests when network or CPU capacity is constrained.'],['MemoKey','Memoization keys must include every input that affects the computed result.'],['CachePressure','Caches should react to memory pressure or bounded limits rather than assuming unlimited retention.'],['LongTaskAttribution','Performance diagnostics are more useful when long work can be attributed to a feature or phase.'],['BudgetRegression','Performance budgets should fail CI or monitoring only when measurement variance and environment differences are accounted for.']],
 testing:[['MutationTesting','Mutation testing evaluates whether tests actually detect small behavioral changes.'],['DifferentialCorpus','Differential tests are strongest when their corpus spans edge cases rather than only common examples.'],['FuzzSeed','Persisting a failing fuzz seed turns a stochastic discovery into a deterministic regression test.'],['RaceSchedule','A concurrency harness can deliberately permute completion schedules to explore timing-sensitive behavior.'],['FakeTimers','Fake timers require explicit control of microtasks and promise completion when testing mixed scheduling.'],['SnapshotNormalization','Stable normalization reduces irrelevant snapshot churn while preserving meaningful output changes.'],['ContractVersion','Versioned contracts make compatibility expectations explicit when independent components evolve.'],['GoldenBinary','Binary golden fixtures should include format/version metadata so incompatible changes are obvious.'],['ResourceLeak','Tests for workers, sockets, file handles, streams, and listeners should verify disposal after both success and failure.'],['BoundaryPair','Boundary cases should be paired with just-inside and just-outside values to catch off-by-one errors.']],
 architecture:[['Ownership','Every resource should have a clear owner responsible for creation, cancellation, and disposal.'],['StructuredConcurrency','Structured concurrency keeps child asynchronous work within the lifetime of its parent operation.'],['Outbox','An outbox records intended external effects so durable state and delivery can be coordinated without assuming a distributed transaction.'],['InboxDedup','An inbox or deduplication record can make repeated message delivery converge to one logical operation.'],['BackpressureBoundary','Backpressure should be enforced at boundaries where work enters a constrained subsystem.'],['TimeoutBudget','A timeout budget should be divided across retries and downstream dependencies rather than independently restarted.'],['BulkheadPool','Separate pools can isolate expensive workloads from latency-sensitive traffic.'],['CircuitHalfOpen','A half-open circuit state allows controlled probes before restoring normal traffic.'],['DeadLetterReplay','Dead-letter replay should be explicit and observable so permanently bad messages are not accidentally reintroduced forever.'],['FeatureFlagSafety','Feature flags need default-safe behavior and should not become an unbounded collection of hidden branches.']],
 data:[['ArrayFrom','Array.from can materialize iterables and apply a mapping function during materialization.'],['Entries','Object.entries converts enumerable own string-keyed properties into ordered key-value pairs.'],['FromEntries','Object.fromEntries constructs an object from an iterable of key-value pairs.'],['HasOwn','Object.hasOwn checks own-property membership without depending on an inherited hasOwnProperty method.'],['CopyWithin','copyWithin mutates an array by copying a region within the same array and should be avoided when immutable state is required.'],['Fill','fill mutates an array and repeats the same value reference when that value is an object.'],['StableSort','Modern JavaScript sorting is stable, which preserves relative order for equal comparison results.'],['TypedArrayBounds','Typed-array indexing is bounded by the view length and out-of-range access does not behave like ordinary object properties.'],['BufferView','Multiple typed views can share storage, so mutation through one view can affect another view.'],['SchemaEvolution','Data schemas should define how old records are read when fields are added, renamed, or removed.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V70_TEXT=Object.entries(packs).flatMap(([domain,items])=>items.map(([topic,text])=>`${domain}: ${topic}. ${text}`)).join('\n');
export const javascriptKnowledgeV70Stats=()=>({version:'70',packs:Object.keys(packs).length,entries:Object.values(packs).reduce((n,x)=>n+x.length,0),domains:Object.keys(packs),authored:true,pretrained:false});
export default LARGE_JAVASCRIPT_KNOWLEDGE_V70_TEXT;
