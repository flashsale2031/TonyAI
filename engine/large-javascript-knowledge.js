// TONY Large JavaScript Knowledge Expansion v1.
// Curated executable local corpus for the pure-JavaScript LargeLM.
// This is knowledge text for retrieval/fitting, not a pretrained neural checkpoint.

const packs={
 language:[
  'JavaScript primitive values include string, number, bigint, boolean, undefined, symbol, and null; objects are reference-like values.',
  'Number uses IEEE 754 double precision; Number.isNaN distinguishes NaN without coercion.',
  'BigInt represents integers of arbitrary magnitude but cannot be freely mixed with Number in arithmetic.',
  'Object property keys are strings or symbols; numeric-looking keys are converted to strings for ordinary objects.',
  'Strict equality with === avoids most implicit type coercion and is generally preferred for predictable comparisons.',
  'Object.is differs from === for NaN and signed zero and is useful when those distinctions matter.',
  'Nullish coalescing ?? chooses a fallback only for null or undefined, unlike || which treats many falsy values as absent.',
  'Optional chaining ?. short-circuits property, element, or call access when the receiver is nullish.',
  'Destructuring binds values from arrays or object properties and can provide defaults for missing values.',
  'Rest syntax collects remaining arguments or elements, while spread syntax expands iterable or enumerable values.',
  'Template literals support interpolation and tagged templates can customize string construction.',
  'Symbols provide unique property keys and can also participate in language protocols such as iteration.',
  'Iterables expose Symbol.iterator and can be consumed by for...of, spread, and many collection APIs.',
  'Async iterables expose Symbol.asyncIterator and can be consumed with for await...of.',
  'Generators pause at yield and return iterator objects, making them useful for lazy sequences and stateful iteration.',
  'Generator delegation with yield* forwards iteration to another iterable or generator.',
  'Closures retain access to lexical variables after the surrounding function has returned.',
  'A closure captures bindings rather than making an automatic immutable snapshot of each value.',
  'The temporal dead zone applies to let, const, and class bindings between scope entry and initialization.',
  'const prevents reassignment of a binding but does not make an object or array immutable.',
  'Block scope is created by let, const, class, and ordinary statement blocks.',
  'Function declarations have hoisting behavior, while function expressions are evaluated when their expression runs.',
  'Arrow functions do not have their own this, arguments, super, or new.target bindings.',
  'Regular function this is determined by the call form in non-arrow functions and can be controlled with call, apply, or bind.',
  'new creates an object, establishes a prototype relationship, invokes the constructor, and returns an object result subject to constructor rules.',
  'Private class fields using # are enforced by the language and are not ordinary enumerable object properties.',
  'Static class fields and methods belong to the constructor rather than each instance.',
  'Getters and setters provide property syntax around functions and should avoid surprising side effects.',
  'Object.freeze prevents extensions and changes to existing own properties at the top level but is shallow.',
  'Object.seal prevents adding or removing own properties but does not recursively freeze nested objects.',
  'Object.create(proto) creates an object with an explicit prototype and can create a null-prototype dictionary.',
  'Object.hasOwn is a direct way to test whether a property is an own property without prototype ambiguity.',
  'Property descriptors control writability, enumerability, configurability, and getter/setter behavior.',
  'Reflect APIs provide predictable object operations and often return success values instead of throwing for ordinary failures.',
  'Proxy can intercept operations such as get, set, has, ownKeys, and function calls but must obey language invariants.',
  'WeakMap keys must be objects or non-registered symbols and do not prevent those keys from being garbage collected.',
  'WeakSet stores weakly held objects or non-registered symbols and is useful for object membership metadata.',
  'Map preserves insertion order and accepts arbitrary values as keys, while plain objects have prototype and key-coercion concerns.',
  'Set stores unique values according to SameValueZero semantics and preserves insertion order.',
  'Array methods such as map, filter, reduce, some, every, find, and flatMap express common collection transformations.',
  'Array.prototype.sort mutates the array and uses string comparison by default; supply a comparator for numeric or domain-specific order.',
  'Array-like values have indexed properties and length but are not necessarily iterable.',
  'Typed arrays provide fixed-type numeric views over ArrayBuffer-backed memory.',
  'DataView allows explicit reading and writing of multiple numeric formats and endianness over an ArrayBuffer.',
  'SharedArrayBuffer permits shared memory between agents when the execution environment enables the required isolation.',
  'Atomics supplies synchronization and atomic operations for shared typed-array memory.',
  'RegExp supports pattern matching and flags such as g, i, m, s, u, y, and d with semantics that should be chosen deliberately.'
 ],
 async:[
  'Promises represent eventual completion or failure and have pending, fulfilled, and rejected states.',
  'Promise.resolve assimilates thenables and returns an already settled promise when given an ordinary value.',
  'Promise.all rejects when any input rejects and otherwise preserves the input ordering in its fulfillment array.',
  'Promise.allSettled waits for every input and reports fulfillment or rejection for each item.',
  'Promise.race settles when the first input promise settles, whether fulfilled or rejected.',
  'Promise.any fulfills when the first input fulfills and rejects with AggregateError when every input rejects.',
  'An async function wraps returned values in promises and converts thrown exceptions into rejected promises.',
  'await unwraps a promise-like value and resumes the async function in a later turn of the execution model.',
  'Promise callbacks are scheduled as microtasks and generally run before the next task such as a timer callback.',
  'queueMicrotask schedules a microtask without creating a promise solely for scheduling.',
  'setTimeout schedules a timer task and does not guarantee exact execution time.',
  'setInterval repeats timer scheduling but long-running callbacks can delay later executions.',
  'AbortController communicates cancellation by dispatching an abort signal to APIs that support AbortSignal.',
  'Cancellation is cooperative: code must observe the signal and stop or reject appropriately.',
  'Async generators combine generator syntax with promises and are useful for streaming asynchronous data.',
  'for await...of consumes both async iterables and ordinary iterables whose values may be promises.',
  'Unhandled promise rejection behavior depends on the host, so applications should attach deliberate error handling.',
  'Promise chains should return nested promises so errors and results propagate through the chain.',
  'finally executes after fulfillment or rejection and normally preserves the original settlement unless finally throws or returns a rejected promise.',
  'Concurrency means multiple operations can make progress without necessarily executing simultaneously on separate CPU cores.',
  'A bounded concurrency pool prevents a large batch of asynchronous work from overwhelming a dependency.',
  'Retries should distinguish transient errors from permanent validation or authorization errors.',
  'Exponential backoff with jitter reduces synchronized retry storms against a failing service.',
  'Timeout wrappers should cancel underlying work when the underlying API supports cancellation rather than merely abandoning the promise.',
  'Async resource cleanup can use try/finally and, where supported, explicit resource-management protocols.'
 ],
 modules:[
  'ES modules are statically analyzable and use explicit import and export declarations.',
  'A module is evaluated once per module graph and subsequent imports normally reuse its module record.',
  'Named exports allow consumers to import selected bindings while default exports provide one conventional primary export.',
  'Re-export syntax can expose bindings from another module without duplicating implementation.',
  'Dynamic import returns a promise for a module namespace object and can enable lazy loading.',
  'Circular module dependencies are permitted but can expose partially initialized bindings during evaluation.',
  'Top-level await can pause module evaluation and therefore affects dependent modules.',
  'Package resolution in Node.js considers package.json fields, exports maps, conditions, file extensions, and package scope.',
  'The exports field can restrict which package subpaths are publicly importable.',
  'The imports field can define private package-local aliases beginning with #.',
  'CommonJS uses require and module.exports while ES modules use import and export; interoperation has host-specific rules.',
  'A good module keeps side effects explicit and avoids doing expensive work merely because it was imported.',
  'Tree shaking works best when modules expose static, side-effect-aware exports that bundlers can analyze.',
  'Dependency injection can make modules easier to test by passing collaborators rather than importing hidden global state.',
  'Pure functions are easier to cache, test, parallelize, and reason about because output depends on explicit inputs.',
  'A barrel module that re-exports many modules can simplify imports but may increase coupling or initialization work.',
  'Module-level singleton state is shared by all consumers of that module instance and should have clear lifecycle semantics.'
 ],
 node:[
  'Node.js runs JavaScript on a V8 engine and exposes host APIs for files, networking, processes, streams, workers, and more.',
  'The Node event loop coordinates asynchronous callbacks while JavaScript execution itself runs on a main thread per isolate.',
  'fs/promises provides promise-based filesystem operations and should be preferred over manual promise wrapping where suitable.',
  'Path utilities should be used instead of hand-building platform-dependent filesystem paths.',
  'URL provides standards-based URL parsing and is preferred over ad hoc string splitting for structured URLs.',
  'Buffer represents raw bytes and is integrated with Node streams and filesystem/network APIs.',
  'Readable streams model data flowing from a source and can be consumed with async iteration.',
  'Writable streams accept data and can apply backpressure when consumers cannot keep up.',
  'Transform streams convert chunks while preserving stream semantics and are useful for pipelines.',
  'stream.pipeline connects streams and handles teardown and errors more safely than manual piping chains.',
  'Child processes execute external programs and should validate arguments rather than constructing unsafe shell command strings.',
  'worker_threads provide separate JavaScript execution contexts for CPU-heavy work with message passing or shared memory.',
  'cluster creates multiple Node processes around a server workload and differs from worker_threads in isolation and memory model.',
  'The process object exposes arguments, environment variables, signals, standard streams, and lifecycle information.',
  'Environment variables are strings and should be parsed and validated before use as numbers, booleans, or structured configuration.',
  'Node HTTP servers expose request and response streams and require explicit handling of methods, paths, headers, and body limits.',
  'Keep-alive connections can reuse TCP connections and reduce connection setup overhead.',
  'DNS resolution may use operating-system or resolver facilities and can have caching and ordering effects.',
  'Crypto APIs provide primitives such as hashing, HMAC, random bytes, signatures, and authenticated encryption; algorithms should be selected for the threat model.',
  'crypto.randomUUID generates UUIDs suitable for identifiers but does not itself provide authentication or secrecy of associated data.',
  'Timers in Node are asynchronous handles and can keep a process alive unless explicitly unrefed where appropriate.',
  'AbortSignal is supported by many modern Node APIs and provides a consistent cancellation mechanism.',
  'A long-lived Node service should handle uncaught failures deliberately and expose health and readiness signals.',
  'Graceful shutdown should stop accepting new work, finish or cancel in-flight work, close resources, and then exit.',
  'JSON.parse converts valid JSON text into JavaScript values and throws on malformed input; JSON.stringify omits undefined object properties.'
 ],
 browser:[
  'The DOM represents a document as a tree of nodes and elements that scripts can query and mutate.',
  'querySelector returns the first element matching a CSS selector while querySelectorAll returns a static NodeList.',
  'Event listeners should usually be removed when their lifecycle ends, especially for long-lived application surfaces.',
  'Event delegation attaches a listener to a stable ancestor and inspects event.target to handle many dynamic children.',
  'CustomEvent can carry application-specific detail data through the browser event system.',
  'MutationObserver watches selected DOM changes asynchronously and can replace polling for many UI synchronization tasks.',
  'ResizeObserver reports changes to element dimensions and is useful for component-level responsive behavior.',
  'IntersectionObserver reports visibility intersection changes and can support lazy loading and viewport-aware work.',
  'requestAnimationFrame schedules visual updates near the browser rendering cycle and is preferable to timers for animation.',
  'requestIdleCallback can schedule lower-priority work when supported, but applications need a fallback for broader compatibility.',
  'The History API changes URL state without a full navigation and supports back/forward integration.',
  'URLSearchParams parses and constructs query strings without manual percent-encoding logic.',
  'FormData represents form fields and file uploads and can be sent with fetch without manually constructing multipart boundaries.',
  'File and Blob represent immutable binary data; object URLs can expose blobs to browser APIs until revoked.',
  'localStorage and sessionStorage store strings synchronously and should not be used for large or latency-sensitive data.',
  'IndexedDB provides asynchronous structured storage suitable for larger client-side datasets.',
  'Cache Storage stores Request/Response pairs and is commonly used with service workers for offline resources.',
  'Service workers run separately from pages and can intercept network requests, cache resources, and receive lifecycle events.',
  'Web Workers communicate through postMessage and structured cloning and cannot directly manipulate the page DOM.',
  'BroadcastChannel lets same-origin contexts communicate through named message channels.',
  'WebSocket provides bidirectional persistent communication but applications still need reconnect, heartbeat, and backpressure strategies.',
  'Server-Sent Events provide one-way server-to-client event streams over HTTP and automatically support browser reconnection semantics.',
  'Web Streams provide backpressure-aware streaming primitives for readable, writable, and transform data flows.',
  'ReadableStream can consume streamed fetch responses without waiting for the entire body to download.',
  'TextEncoder and TextDecoder convert between strings and UTF-8 byte sequences.',
  'Canvas 2D provides immediate-mode drawing while SVG provides a retained vector document model.',
  'Web Audio represents audio as a graph of processing nodes and can schedule precise audio operations.',
  'WebGPU exposes modern GPU compute and graphics primitives but requires explicit resource and shader management.',
  'Permissions and browser security policies can restrict powerful APIs even when JavaScript code is otherwise correct.',
  'Same-origin policy separates origins by scheme, host, and port; CORS is a server-controlled mechanism for selected cross-origin requests.'
 ],
 fetch:[
  'fetch returns a promise that fulfills for HTTP error statuses such as 404; callers must inspect response.ok or response.status.',
  'Response.json consumes the response body and parses JSON; the body can generally be consumed only once unless cloned first.',
  'Response.text reads the body as text and also consumes it.',
  'Request objects encapsulate method, URL, headers, body, credentials, cache, mode, redirect, signal, and other fetch options.',
  'POST requests commonly carry JSON with Content-Type application/json and a JSON.stringify encoded body.',
  'GET request bodies are not a portable way to send parameters; use query parameters or another method defined by the API.',
  'HTTP status 2xx represents successful responses, 3xx redirects, 4xx client-side problems, and 5xx server-side problems by conventional semantics.',
  'A fetch wrapper should apply explicit timeouts, retries where appropriate, structured error reporting, and response validation.',
  'Content-Type describes representation media type while Accept communicates preferred response representations.',
  'ETag and If-None-Match support conditional requests and can reduce transfer when a representation has not changed.',
  'Cache-Control controls HTTP caching behavior and should reflect whether content is public, private, immutable, or revalidated.',
  'Credentials behavior determines whether browser-managed credentials such as cookies participate in a request.',
  'CORS preflight requests use OPTIONS to ask a server whether a cross-origin request is permitted under the requested method and headers.',
  'A server should never treat an Origin header as trusted authentication merely because a browser sent it.',
  'Streaming fetch responses are useful for large downloads, generated text, logs, and event-like protocols.',
  'Upload progress is not uniformly exposed by fetch across browsers; XMLHttpRequest remains relevant for some progress-reporting needs.'
 ],
 typescript:[
  'TypeScript adds static type syntax to JavaScript and erases types when compiling to JavaScript.',
  'Interfaces describe structural object shapes and are useful for documenting contracts without creating runtime values.',
  'Type aliases can describe unions, intersections, conditional types, mapped types, tuples, and other compositions.',
  'A union type means a value may conform to one of several alternatives and often requires narrowing before use.',
  'A discriminated union uses a shared literal field to make control-flow narrowing predictable.',
  'Generics parameterize functions, classes, and types so relationships between inputs and outputs can be preserved.',
  'unknown is safer than any for values of uncertain type because it requires narrowing before most operations.',
  'never represents impossible values or functions that do not successfully return.',
  'strict mode enables stronger checks and catches many classes of JavaScript mistakes earlier.',
  'Type assertions change the compiler view of a value but do not validate or transform the runtime value.',
  'Runtime validation is still required when data crosses a trust boundary because TypeScript types are erased.',
  'tsconfig controls compiler behavior such as module resolution, target syntax, strictness, source maps, and path settings.',
  'Declaration files describe the types of JavaScript modules that do not provide native TypeScript source types.',
  'The satisfies operator checks that an expression conforms to a type while preserving more specific inferred information.',
  'Enums generate runtime constructs unlike interfaces and most type aliases, so their use should be deliberate.',
  'Template literal types can compute string unions from other literal types.',
  'Utility types such as Partial, Required, Pick, Omit, Record, ReturnType, and Awaited compose common type transformations.'
 ],
 performance:[
  'Algorithmic complexity often matters more than micro-optimizing individual JavaScript expressions.',
  'Avoid repeated DOM layout reads and writes in alternating order because they can cause forced synchronous layout.',
  'Batch DOM mutations so the browser can process fewer intermediate layouts and paints.',
  'Event delegation can reduce listener count for large dynamic lists, but direct listeners can be clearer for small stable trees.',
  'Memoization trades memory for repeated computation savings and requires a correct cache invalidation or key strategy.',
  'A hot loop should minimize unnecessary allocation when profiling shows allocation pressure is significant.',
  'Object shape consistency can help JavaScript engines optimize property access, but engine internals should not replace measurement.',
  'Typed arrays are useful for dense numeric data and can reduce representation overhead compared with arrays of boxed values.',
  'Streaming avoids buffering an entire large payload and can reduce peak memory usage.',
  'Lazy iteration defers work until values are requested and can avoid generating unused data.',
  'Worker threads and Web Workers can improve responsiveness for CPU-heavy independent work by moving computation off the UI or server main thread.',
  'Caching should be measured against memory cost, invalidation complexity, staleness, and correctness requirements.',
  'Compression reduces transfer size but consumes CPU and may increase latency for already small payloads.',
  'Profilers and benchmark harnesses should use representative inputs and distinguish cold startup from steady-state performance.',
  'Premature optimization can make code harder to maintain without improving the actual bottleneck.',
  'Backpressure is a performance correctness feature because unbounded producers can create memory growth and latency collapse.'
 ],
 testing:[
  'A JavaScript unit test should isolate the behavior under test and assert observable outcomes rather than implementation trivia.',
  'Integration tests verify boundaries such as HTTP handlers, database adapters, filesystem operations, and message queues.',
  'End-to-end tests validate complete user workflows but are usually slower and more environment-sensitive.',
  'Fake timers can make time-dependent tests deterministic but should be reset after each test to prevent cross-test contamination.',
  'Randomized tests should record a seed or failing input so failures can be reproduced.',
  'Snapshot tests are useful when large structured output is intentionally stable, but excessive snapshots can hide meaningful changes.',
  'Property tests are strongest when the invariant is simpler and more general than the implementation.',
  'Tests for parsers should include malformed syntax, empty input, boundary lengths, Unicode, and unexpected tokens.',
  'Tests for HTTP clients should cover non-2xx statuses, malformed payloads, timeouts, aborts, retries, and partial failures.',
  'Tests for async code should await the actual promise rather than relying on arbitrary delays.',
  'A test double should model only the contract required by the caller and should not accidentally reproduce the implementation under test.',
  'Coverage percentages are diagnostic signals rather than proof of correctness.',
  'Mutation testing can measure whether tests detect deliberate small changes to implementation behavior.',
  'Regression tests should preserve the smallest input that reproduces a previously observed defect.'
 ],
 security:[
  'Never evaluate untrusted text with eval or Function when ordinary parsing or explicit dispatch can express the required behavior.',
  'innerHTML interprets markup and can create injection risks when untrusted content reaches it; textContent is safer for plain text.',
  'Avoid constructing shell commands from untrusted strings; pass arguments through structured APIs when available.',
  'Prototype pollution can occur when attacker-controlled property names mutate shared prototypes through unsafe object merging.',
  'Use null-prototype objects or own-property checks when processing untrusted key-value maps with special property names.',
  'Regular expressions supplied by users can create denial-of-service risks when patterns exhibit catastrophic backtracking.',
  'Server-side validation remains authoritative even if a browser validates the same fields for user experience.',
  'CSRF defenses are required for state-changing browser requests when ambient credentials such as cookies are used.',
  'SameSite cookies can reduce some cross-site request risks but should be configured with the application authentication model in mind.',
  'HttpOnly prevents JavaScript from reading a cookie but does not stop the browser from sending that cookie where policy permits.',
  'Secure cookies are restricted to secure transport and should be used for sensitive session material.',
  'Content Security Policy can restrict script sources and reduce exploitability of some injection flaws.',
  'Subresource Integrity lets browsers verify expected hashes for selected externally loaded resources.',
  'Passwords should be handled by dedicated password hashing algorithms such as Argon2id, scrypt, or bcrypt rather than general-purpose hashes.',
  'Cryptographic random values should come from a cryptographically secure random source, not Math.random.',
  'Authorization checks belong at the server boundary and should not depend on hidden UI controls.',
  'Error responses should avoid exposing stack traces, secrets, filesystem paths, or internal topology to untrusted clients.',
  'Dependency updates should be reviewed for breaking changes and security advisories rather than blindly auto-updating production code.'
 ],
 patterns:[
  'A finite-state machine makes legal application states and transitions explicit and is useful for complex workflows.',
  'A reducer models state changes as pure transitions from current state and action to next state.',
  'The adapter pattern isolates a domain interface from a concrete external API or storage representation.',
  'The strategy pattern selects interchangeable algorithms through a common interface.',
  'The observer pattern broadcasts changes to multiple listeners but requires lifecycle management to avoid leaks.',
  'The command pattern represents an operation as data and can support queues, retries, logging, or undo where semantics allow.',
  'A repository abstraction can isolate persistence details but should not become a generic wrapper that hides useful database capabilities.',
  'Dependency inversion is stronger when high-level policies depend on stable abstractions rather than concrete infrastructure classes.',
  'Functional composition combines small transformations into pipelines with explicit data flow.',
  'A validation pipeline should return structured errors so callers can distinguish malformed input from unavailable dependencies.',
  'A circuit breaker tracks dependency failures and temporarily prevents calls while allowing controlled recovery attempts.',
  'A bulkhead isolates resource pools so failure in one workload does not consume every available resource.',
  'A queue worker should be idempotent or use a deduplication strategy because retries can produce duplicate delivery.',
  'Outbox patterns store an intended event alongside the transaction that changes business state, then publish it asynchronously.',
  'Event sourcing stores state-changing events as a history and derives current state by replaying or projecting those events.',
  'CQRS separates command-side mutation models from query-side read models when the complexity is justified by workload or domain needs.'
 ],
 tooling:[
  'npm scripts provide repeatable project commands such as test, lint, build, start, and development tasks.',
  'package-lock files pin dependency resolution and improve reproducibility across installations.',
  'Semantic versioning communicates intended compatibility through major, minor, and patch version components.',
  'ESLint detects configurable JavaScript and TypeScript patterns and can enforce project conventions.',
  'Prettier formats code according to deterministic style rules and is most useful when formatting is automated.',
  'Source maps connect generated JavaScript locations back to original source locations for debugging.',
  'Environment-specific configuration should be explicit and validated at startup rather than failing later at request time.',
  'A build pipeline should fail on deterministic type, lint, or test errors before packaging artifacts.',
  'Continuous integration should run from a clean environment to expose undeclared local dependencies.',
  'Lockfiles reduce dependency drift but do not replace vulnerability review or update strategy.',
  'Semantic release automation should derive versions from documented commit or change conventions only when the team actually follows them.',
  'Monorepos benefit from explicit package boundaries, dependency graphs, and reproducible workspace tooling.',
  'Generated code should be marked as generated and should not be hand-edited when a source generator is authoritative.',
  'A CLI should expose stable exit codes and useful stderr diagnostics for automation.',
  'Configuration parsing should distinguish missing, invalid, and intentionally empty values.'
 ]
};

export const LARGE_JAVASCRIPT_KNOWLEDGE=Object.values(packs).flat();
export const LARGE_JAVASCRIPT_KNOWLEDGE_TEXT=LARGE_JAVASCRIPT_KNOWLEDGE.join('\n');
export const javascriptKnowledgeStats=()=>({packs:Object.keys(packs).length,entries:LARGE_JAVASCRIPT_KNOWLEDGE.length,characters:LARGE_JAVASCRIPT_KNOWLEDGE_TEXT.length,focus:'JavaScript language, browser, Node.js, async, modules, TypeScript, security, testing, performance, and architecture'});
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledge={LARGE_JAVASCRIPT_KNOWLEDGE,LARGE_JAVASCRIPT_KNOWLEDGE_TEXT,javascriptKnowledgeStats};
