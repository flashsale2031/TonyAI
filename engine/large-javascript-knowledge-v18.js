// TONY Large JavaScript Knowledge Expansion v18.
// Authored pure-JavaScript engineering knowledge; not a pretrained neural checkpoint.
const packs={
language:[['destructuring','Destructuring binds values from arrays or objects and can provide defaults for missing properties.'],['rest','Rest syntax collects remaining iterable or object properties into a new value.'],['spread','Spread syntax expands iterable elements or enumerable object properties and performs shallow copying.'],['symbol','Symbols provide unique property keys and can customize language protocols such as iteration.'],['iterator','An iterator exposes next() and can represent lazy traversal without materializing a complete collection.'],['generator','Generators suspend and resume execution and provide a natural implementation for custom iterators.'],['realm','A JavaScript realm has its own intrinsic objects; values from another realm can fail instanceof checks against local constructors.'],['eval','Dynamic evaluation changes compilation and security assumptions and should be avoided when ordinary functions or parsers suffice.'],['strict','Strict mode changes selected semantics and turns several silent failures into errors.'],['classfields','Class fields initialize per instance while static fields belong to the constructor, and initialization order is part of class semantics.']],
async:[['allsettled','Promise.allSettled waits for every input and reports fulfillment or rejection records without short-circuiting on failure.'],['withresolvers','Promise.withResolvers separates promise creation from its resolve and reject functions where supported.'],['asyncdispose','Async disposal lets asynchronous resources define cleanup that can be coordinated at scope boundaries where supported.'],['disposal','Explicit resource management patterns make ownership and cleanup visible instead of relying on eventual garbage collection.'],['backpressure','Backpressure prevents producers from overwhelming consumers and should propagate through every stage of an async pipeline.'],['batching','Batching async operations reduces overhead but should respect latency and payload-size limits.'],['fanout','Concurrent fan-out should have explicit limits so one request cannot create unbounded downstream work.'],['fan-in','Fan-in combines multiple asynchronous producers and must define ordering, failure, and cancellation behavior.'],['deadlines','A deadline is stronger than a timeout alone because downstream operations should inherit the remaining time budget.'],['microtask','Microtasks run after the current job before the browser proceeds to another task, so recursive microtask scheduling can starve rendering.']],
node:[['workerthreads','worker_threads move CPU-heavy JavaScript work to separate threads and communicate through messages or transferable memory.'],['cluster','Node cluster can run multiple processes sharing a server port, but state coordination and deployment topology remain application concerns.'],['childprocess','child_process APIs execute external programs and require deliberate argument, environment, timeout, and output handling.'],['execfile','execFile avoids an intermediate shell for normal executable invocation and can reduce shell-injection risk compared with shell-based execution.'],['spawn','spawn streams process input and output incrementally and is preferable when large output should not be buffered.'],['env','process.env exposes environment configuration as strings and should be validated at startup rather than trusted implicitly.'],['cwd','process.cwd reports the current working directory, which can differ from the directory containing the executing module.'],['urlfile','file URLs provide an explicit module-relative path representation and avoid several platform-specific path ambiguities.'],['watch','File watching is platform-dependent and may coalesce or duplicate events, so consumers should tolerate imperfect notification behavior.'],['testrunner','Node’s built-in test runner supports structured tests, assertions, mocking, and asynchronous cases without requiring a third-party framework.']],
browser:[['mutationobserver','MutationObserver batches DOM mutation notifications and is preferable to manually polling for structural changes.'],['intersectionobserver','IntersectionObserver reports visibility intersections asynchronously and can support lazy loading and viewport-aware work.'],['mediadevices','MediaDevices exposes permission-mediated camera and microphone access and should handle denial and device changes.'],['webaudio','Web Audio models audio processing as connected nodes and supports scheduling work against an audio clock.'],['websocket','WebSocket provides bidirectional communication after an HTTP upgrade, with application-level heartbeat and reconnection policies often required.'],['eventsource','EventSource consumes server-sent events over a long-lived HTTP connection and automatically reconnects under defined conditions.'],['webrtc','WebRTC supports peer media and data channels but requires explicit signaling and careful network and permission handling.'],['offscreen','OffscreenCanvas can move selected rendering work away from the main thread where supported.'],['pointer','Pointer Events unify mouse, pen, and touch input under a common event model with pointer identity.'],['keyboard','Keyboard events expose physical and logical key information; accessibility behavior should not depend solely on key codes.']],
security:[['cspnonce','CSP nonces authorize specific inline resources using an unpredictable per-response value and must not be reused across responses.'],['cspreport','CSP reporting provides telemetry about policy violations and can help discover unexpected resource paths.'],['xsleak','Cross-site leak defenses reduce inference of sensitive state through timing, resource loading, or observable browser behavior.'],['sameorigin','Same-origin policy restricts cross-origin reads while allowing selected explicitly mediated communication mechanisms.'],['csrf','CSRF defenses should combine appropriate request validation, cookie settings, and origin-aware controls for state-changing actions.'],['xcontenttype','X-Content-Type-Options can prevent MIME sniffing in supporting browsers when set appropriately.'],['coop','Cross-Origin-Opener-Policy isolates browsing contexts from cross-origin opener relationships.'],['coep','Cross-Origin-Embedder-Policy restricts embedding of cross-origin resources unless they satisfy required conditions.'],['corscredentials','Credentialed CORS requests require compatible server headers and do not permit wildcard origins for credentials.'],['secretlogs','Secrets should be excluded from logs, traces, URLs, crash reports, and diagnostic payloads wherever practical.']],
performance:[['memoryleak','A JavaScript memory leak occurs when reachable references keep objects alive after their logical lifetime has ended.'],['listenerleak','Repeatedly registering event listeners without removal can retain objects and duplicate work.'],['weakrefs','WeakRef and FinalizationRegistry expose non-deterministic lifetime observations and should not replace explicit resource management.'],['memoization','Memoization trades memory for repeated computation savings and requires a cache invalidation or bounded-size strategy.'],['cachekey','Cache keys must include every input that affects the result or stale cross-context results can be returned.'],['serialization','Serialization cost can dominate computation for large objects, so transferables or shared memory may be preferable in specialized paths.'],['transferable','Transferable objects move ownership between agents without copying their backing storage where supported.'],['compressionratio','Compression saves transfer bandwidth at the cost of CPU and latency; the optimal setting depends on payload and workload characteristics.'],['profilermarks','Performance marks and measures provide application-defined timing points for diagnosing real critical paths.'],['gc','Garbage collection is automatic but not free; allocation rate, object lifetime, and retained references influence pause and throughput behavior.']],
architecture:[['eventdriven','Event-driven architecture communicates through events and requires explicit delivery, ordering, duplication, and retention semantics.'],['pubsub','Publish-subscribe decouples producers and consumers but can make tracing and failure ownership harder without strong observability.'],['queueworker','Queue workers should be idempotent or use deduplication because delivery can be repeated after crashes or retries.'],['idempotency','Idempotent operations produce the same externally meaningful result when the same request is safely repeated.'],['outbox','The transactional outbox pattern records an event with the transaction and publishes it later, reducing database-to-message consistency gaps.'],['invariant','An invariant states a property that must remain true across valid state transitions and is useful for design and testing.'],['state','Explicit state machines make lifecycle transitions and invalid operations easier to reason about than scattered boolean flags.'],['configuration','Configuration should be parsed once, validated, and exposed through a stable typed interface instead of repeatedly reading raw environment strings.'],['observability','Logs, metrics, and traces should share stable correlation identifiers so distributed failures can be reconstructed.'],['graceful','Graceful degradation returns a reduced but useful capability when optional dependencies or resources are unavailable.']],
testing:[['property','Property-based tests generate many inputs against general invariants rather than enumerating only hand-picked examples.'],['fuzzing','Fuzzing supplies unexpected or malformed inputs and is particularly useful for parsers, protocol boundaries, and state machines.'],['loadtest','Load tests measure behavior under representative concurrency and should track latency percentiles as well as throughput.'],['soaktest','Soak tests run workloads for extended periods to expose leaks, degradation, and resource exhaustion.'],['chaos','Chaos testing introduces controlled failures to validate resilience assumptions and recovery paths.'],['testdouble','Mocks, stubs, and fakes are test doubles with different tradeoffs; tests should avoid coupling to implementation details unnecessarily.'],['integration','Integration tests validate interactions between real components and are especially valuable at serialization and protocol boundaries.'],['e2e','End-to-end tests validate complete user-visible workflows but are typically slower and more environment-sensitive.'],['coverage','Coverage identifies exercised code but does not prove semantic correctness or meaningful assertions.'],['regression','A regression test should capture a previously observed failure so the defect does not silently return.']],
data:[['mapkey','Map keys use SameValueZero semantics, so NaN keys compare as equal and object keys use identity.'],['setkey','Set membership also uses SameValueZero semantics and stores unique values by that comparison.'],['flat','Array.flat removes nesting to a requested depth and creates a new array rather than mutating the source.'],['flatmap','Array.flatMap maps each element and flattens one level, making it useful for one-to-many transformations.'],['find','Array.find returns the first matching element while findIndex returns its position; both stop at the first match.'],['reduce','Array.reduce folds elements into one accumulated result and should define an initial value when empty input is possible.'],['localecompare','localeCompare provides locale-sensitive string comparison and should not be assumed to equal simple code-unit ordering.'],['json','JSON represents a constrained data format and does not preserve functions, undefined values in objects, or arbitrary class instances.'],['parse','JSON.parse constructs ordinary data from JSON text and should be wrapped with appropriate error handling for untrusted input.'],['reviver','JSON.parse revivers can transform parsed values during construction but add complexity and should be used deliberately.']],
web:[['etag','ETag identifies a representation version and enables conditional requests that avoid transferring unchanged content.'],['ifnone','If-None-Match lets a client validate an ETag and can produce a 304 response when the cached representation remains current.'],['ifmatch','If-Match can protect state-changing operations against overwriting a representation that changed since it was read.'],['cachecontrol','Cache-Control defines caching directives such as max-age, no-store, private, and stale behavior.'],['authorization','Authorization carries application credentials or bearer tokens and must be protected from logs and unintended disclosure.'],['problem','application/problem+json provides a structured format for communicating HTTP API errors.'],['webhook','Webhooks deliver events to an HTTP endpoint and should authenticate, validate, deduplicate, and tolerate retries.'],['retryafter','Retry-After communicates when a client should retry after selected responses and should be combined with a bounded retry policy.'],['idempotencykey','Idempotency-Key lets an API associate repeated requests with one logical operation when the server implements the corresponding contract.'],['contentencoding','Content-Encoding describes representation coding such as gzip or br and is distinct from the media type in Content-Type.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V18=Object.entries(packs).flatMap(([domain,items])=>items.map(([topic,text])=>({domain,topic,text})));
export const LARGE_JAVASCRIPT_KNOWLEDGE_V18_TEXT=LARGE_JAVASCRIPT_KNOWLEDGE_V18.map(x=>`[${x.domain}/${x.topic}] ${x.text}`).join('\n');
export const javascriptKnowledgeV18Stats={version:'18.0',domains:Object.keys(packs).length,entries:LARGE_JAVASCRIPT_KNOWLEDGE_V18.length,pretrained:false,externalNeuralModel:false,source:'authored-pure-javascript-knowledge'};
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledgeV18={LARGE_JAVASCRIPT_KNOWLEDGE_V18,LARGE_JAVASCRIPT_KNOWLEDGE_V18_TEXT,javascriptKnowledgeV18Stats};
