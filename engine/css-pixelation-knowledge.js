// TONY CSS pixelation knowledge: authored procedural/browser techniques only.
export const CSS_PIXELATION_KNOWLEDGE=[
 ['CanvasNearestNeighbor','Disable smoothing and draw source pixels at integer-aligned destination coordinates for crisp nearest-neighbor enlargement.'],
 ['CanvasImageSmoothing','CanvasRenderingContext2D.imageSmoothingEnabled=false selects pixel-preserving interpolation for supported image operations.'],
 ['PixelGrid','A CSS pixelation effect can be represented as a logical pixel grid whose cells sample a source image or generated color field.'],
 ['CSSImageRendering','image-rendering: pixelated communicates a preference for sharp pixel-style scaling when the browser supports the requested rendering mode.'],
 ['CSSVariables','CSS custom properties can expose pixel size, contrast, scale, and palette controls to JavaScript-driven visual effects.'],
 ['OffscreenCanvas','OffscreenCanvas can move compatible pixel processing away from the visible document context when the platform supports it.'],
 ['ImageData','ImageData exposes RGBA samples that JavaScript can transform before writing pixels back to a canvas.'],
 ['Quantization','Color quantization maps many source colors into a smaller palette to create a deliberate pixel-art appearance.'],
 ['Dithering','Ordered or error-diffusion dithering can approximate intermediate colors when a restricted palette is required.'],
 ['NearestSample','Nearest-cell sampling can compute one representative source coordinate per logical pixel cell.'],
 ['IntegerScale','Integer scale factors avoid fractional destination boundaries and preserve consistent pixel geometry.'],
 ['DevicePixelRatio','Canvas backing resolution should account for devicePixelRatio while keeping logical pixelation dimensions explicit.'],
 ['CSSBackgroundGrid','Repeating linear gradients can create a visual pixel grid overlay without modifying source image pixels.'],
 ['MaskGrid','CSS masks and gradients can reveal repeated blocks or geometric pixel patterns over an element.'],
 ['FilterChain','CSS filters can provide stylized contrast or saturation before or after a pixelation layer, but should be tested for browser consistency.'],
 ['ResizeObserver','ResizeObserver can recompute a pixel grid when a responsive pixelated element changes size.'],
 ['IntersectionObserver','IntersectionObserver can defer expensive pixel processing until a visual effect approaches the viewport.'],
 ['PointerInteraction','Pointer coordinates can be mapped from CSS pixels into logical pixel-grid coordinates for interactive pixel effects.'],
 ['AnimationLoop','requestAnimationFrame is appropriate for continuously animated pixel transformations tied to rendering.'],
 ['ReducedMotion','A pixelation animation should respect prefers-reduced-motion and provide a non-animated presentation.'],
 ['ColorSpace','Pixel effects should define expected color-space behavior when accurate color reproduction matters.'],
 ['AlphaHandling','RGBA transformations should preserve or intentionally modify alpha rather than accidentally premultiplying colors twice.'],
 ['MemoryReuse','Reuse ImageData buffers and canvas resources when repeatedly processing similarly sized frames.'],
 ['WorkerTransfer','Transferable ArrayBuffers can reduce copying when pixel buffers are exchanged with workers.'],
 ['PointerCapture','Pointer capture can keep an interactive pixel brush associated with the same element during pointer movement.'],
 ['GridSnap','Snap brush coordinates to logical cells before changing pixel data to maintain a stable block shape.'],
 ['PaletteLookup','A palette lookup table can replace repeated distance calculations when the palette is fixed.'],
 ['Luma','Relative luminance can drive threshold or palette selection for monochrome pixelation effects.'],
 ['EdgeAware','Edge-aware sampling can preserve high-contrast boundaries while still reducing image resolution.'],
 ['ProceduralNoise','Deterministic seeded noise can add controlled pixel texture without external image assets.'],
 ['CSSContainment','CSS containment can limit layout and paint interactions around a pixelated component.'],
 ['ContentVisibility','content-visibility can defer rendering work for pixelated content that is outside the immediate viewport.'],
 ['CanvasExport','Canvas toBlob can serialize generated pixel art for download or later processing.'],
 ['BlobURL','Object URLs can preview generated image blobs and should be revoked when no longer needed.'],
 ['ClipboardImage','Supported clipboard APIs can copy generated image blobs when permission and user-activation requirements are satisfied.'],
 ['SpriteSheet','A pixel-art sprite sheet can be represented as integer-aligned source rectangles and rendered without interpolation.'],
 ['TileMap','A tile map uses a fixed logical grid and palette-aware tile selection to construct larger scenes.'],
 ['PixelBrush','A pixel brush modifies a bounded set of logical cells rather than scanning the entire canvas for every pointer event.'],
 ['DirtyRect','Dirty-region tracking limits redraws to changed pixel cells when full-frame rendering is unnecessary.'],
 ['FrameBudget','Animated pixel processing should track frame time and degrade effect complexity when the main thread is overloaded.'],
 ['CSSFallback','When image-rendering behavior differs between browsers, a JavaScript canvas fallback can provide deterministic pixel sampling.'],
 ['NoExternalModel','All described pixelation operations are deterministic JavaScript/CSS/canvas algorithms and do not require a pretrained neural model.']
];
export const CSS_PIXELATION_KNOWLEDGE_TEXT=CSS_PIXELATION_KNOWLEDGE.map(([topic,text])=>`css-pixelation: ${topic}. ${text}`).join('\n');
export const cssPixelationKnowledgeStats=()=>({version:'1',entries:CSS_PIXELATION_KNOWLEDGE.length,domains:['css','canvas','pixelation','browser'],authored:true,pretrained:false});
export default CSS_PIXELATION_KNOWLEDGE_TEXT;
