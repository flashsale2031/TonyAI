// TONY Large JavaScript Knowledge Expansion v4.
// Additional pure-JavaScript engineering knowledge for retrieval and local fitting.
// Not a pretrained neural checkpoint.
const packs={
 runtime:[
  'ECMAScript execution contexts track lexical environments, variable environments, and execution state for running code.',
  'The global object and global environment are related but are not interchangeable concepts in ECMAScript semantics.',
  'Strict mode changes selected language behaviors and turns several silent failures into errors.',
  'The this binding is determined differently for scripts, modules, ordinary calls, methods, constructors, and arrow functions.',
  'Private class elements use brand checks so unrelated objects cannot access another class private field by spelling the same name.',
  'Static initialization blocks run during class definition and can initialize private or static state.',
  'Computed property names evaluate expressions while constructing object or class definitions.',
  'Destructuring assignment can invoke iterators and property getters, so it can have observable side effects.',
  'Default values in destructuring apply when a selected value is undefined, not merely when a property is absent.',
  'Logical operators return operand values rather than forcing boolean results in ordinary JavaScript expressions.',
  'Nullish coalescing differs from logical OR because zero, false, and empty strings are valid non-nullish values.',
  'Optional chaining short-circuits a contiguous chain when its receiver is nullish but does not make arbitrary surrounding expressions safe.',
  'The comma operator evaluates expressions left to right and returns the value of the final expression.',
  'Automatic semicolon insertion has defined grammar rules and should not be treated as a general whitespace-based parser.',
  'Unicode escapes can represent identifiers and strings, so source spelling and runtime text are not always identical.',
  'Regular expressions with the sticky flag match from the current lastIndex while global matching advances through input.',
  'The dotAll flag allows a regular-expression dot to match line terminators that it normally excludes.',
  'Named capture groups make complex regular-expression results easier to consume than relying only on numeric positions.',
  'String normalization can matter when visually equivalent Unicode sequences have different underlying code-point sequences.',
  'Intl.Collator provides locale-aware comparison and should be preferred over naive lexical ordering for user-facing sorted text.'
 ],
 collections:[
  'Map iteration follows insertion order and deleting then re-adding a key moves that key to the end of iteration.',
  'Set membership uses SameValueZero and therefore treats NaN as equal to itself.',
  'WeakMap and WeakSet are appropriate for metadata associated with object lifetime when enumeration is unnecessary.',
  'Array.from can consume iterables and array-like objects and can apply a mapping function during construction.',
  'Array.fromAsync can consume asynchronous iterables and await produced values in supported runtimes.',
  'TypedArray methods generally mirror array transformations while preserving typed numeric storage semantics.',
  'ArrayBuffer resizing features are environment-dependent and require deliberate capacity and memory management.',
  'DataView is useful when binary protocols specify explicit byte offsets, widths, and endianness.',
  'Atomics.wait and related primitives coordinate agents using shared memory and have environment-specific availability.',
  'Structured clone supports many built-in values but intentionally does not preserve functions or arbitrary prototype behavior.',
  'MessagePort transfers communication endpoints between execution contexts and can be closed when no longer needed.',
  'Transferable objects move ownership rather than cloning bytes, which can reduce copies for suitable binary workloads.',
  'Sorting millions of objects can dominate runtime, so selecting a key once can avoid repeatedly computing expensive comparison values.',
  'Stable sorting preserves relative order for equal comparison results in modern JavaScript implementations.',
  'Sparse arrays contain absent elements and several methods skip those holes rather than treating them as explicit undefined values.'
 ],
 modules:[
  'ES module bindings are live bindings, so an imported binding reflects updates to the exported binding rather than copying a value once.',
  'Default exports and named exports create different import syntax but both participate in the module dependency graph.',
  'Dynamic import is useful for optional features and code splitting because module loading is asynchronous.',
  'Top-level await can delay evaluation of dependent modules and should be used with awareness of startup dependencies.',
  'Circular imports can work when bindings are accessed after initialization, but early reads can encounter temporal dead zones.',
  'Import maps can control browser module specifier resolution when supported by the deployment environment.',
  'Package exports can expose conditional entry points and prevent consumers from depending on private internal paths.',
  'Package self-references can allow a package to import itself through its declared package name under supported resolution rules.',
  'Dual CommonJS and ES module packages need explicit interoperation strategy to avoid divergent entry points and behavior.',
  'Module side effects should be documented because importing a module can execute its top-level code exactly once per module record.',
  'A module graph can contain asynchronous evaluation dependencies when top-level await is used.',
  'Bundlers can statically eliminate unused exports more reliably when modules avoid opaque dynamic mutation of their public surface.'
 ],
 node:[
  'Node.js streams use backpressure to coordinate producers and consumers; ignoring write return values can cause memory growth.',
  'stream.pipeline centralizes completion and error propagation across connected streams and is safer than manually coordinating teardown.',
  'Readable.from can adapt iterables and async iterables into Node readable streams.',
  'AsyncLocalStorage can associate contextual state with asynchronous execution chains such as request identifiers.',
  'The node: prefix explicitly identifies built-in Node modules and avoids ambiguity with package names.',
  'Node ESM resolution treats URL-like module identifiers and file extensions differently from CommonJS resolution.',
  'fs.open returns a file handle whose lifecycle should be closed even when processing fails.',
  'Recursive directory operations should consider symlinks, permissions, cycles, and resource limits.',
  'Node buffers share memory in some construction patterns, so ownership and mutation assumptions must be explicit.',
  'Buffer.subarray creates a view over existing memory rather than copying bytes.',
  'HTTP request bodies should be bounded before parsing JSON or other potentially large representations.',
  'HTTP response headers should be written before the response body and should match the actual representation being returned.',
  'Node timers return handles that can be cleared or unrefed to control process lifetime.',
  'Worker threads are separate isolates and require explicit communication for application state.',
  'The worker_threads MessageChannel API provides two linked MessagePort endpoints for direct worker communication.',
  'Child process stdio modes determine whether streams are inherited, piped, ignored, or detached.',
  'DNS results and connection attempts can change over time, so long-lived applications should not assume permanent address mappings.',
  'TLS certificate validation should remain enabled unless a narrowly controlled development environment requires otherwise.',
  'Node test runners can execute subtests and support assertions without requiring a third-party framework for basic cases.',
  'Graceful shutdown should stop accepting work before closing resources so new requests do not race with teardown.'
 ],
 browser:[
  'The browser rendering pipeline includes style calculation, layout, paint, and compositing; unnecessary synchronous DOM work can trigger costly updates.',
  'Reading layout-dependent properties after DOM writes can create forced synchronous layout when the browser must calculate geometry immediately.',
  'Batching DOM reads separately from DOM writes can reduce layout thrashing in interactive interfaces.',
  'Document fragments are useful for building detached subtrees before inserting them into a live document.',
  'AbortSignal can cancel fetches, event listeners, and other APIs that explicitly accept a signal.',
  'addEventListener with an AbortSignal can automatically remove the listener when the signal aborts.',
  'Pointer capture lets an element continue receiving pointer events after the pointer moves outside its hit-test region.',
  'Keyboard focus should be returned to a sensible origin after closing a modal or transient interaction surface.',
  'ARIA attributes supplement native semantics and should not replace semantic HTML when native elements provide the required behavior.',
  'The dialog element provides browser semantics for modal and non-modal dialog patterns in supported environments.',
  'Popover APIs can manage transient overlays with browser-integrated light-dismiss behavior when supported.',
  'CSS containment can limit layout, style, paint, or size effects and can improve performance when applied correctly.',
  'ResizeObserver callbacks should avoid creating feedback loops by changing the observed size without a stable stopping condition.',
  'IntersectionObserver thresholds define visibility ratios at which callbacks should be delivered.',
  'Web Workers cannot directly access DOM nodes because DOM ownership remains in the page execution context.',
  'SharedArrayBuffer availability depends on cross-origin isolation requirements in browsers.',
  'Service worker activation and controller changes are asynchronous lifecycle events and should be handled explicitly.',
  'Cache Storage keys are request/response pairs and cache matching semantics should be chosen deliberately.',
  'IndexedDB transactions have defined lifetimes and applications should avoid assuming a transaction remains active across arbitrary asynchronous gaps.',
  'BroadcastChannel messages are same-origin communication and should still validate message structure before acting on them.'
 ],
 web:[
  'HTTP semantics distinguish safe, idempotent, and cacheable methods; applications should select methods according to operation meaning.',
  'PUT commonly represents replacement semantics while PATCH represents partial modification, but actual API contracts define precise behavior.',
  'HTTP conditional requests can prevent lost updates when ETags and If-Match are used as optimistic concurrency controls.',
  'A 304 response indicates that a cached representation remains valid and normally has no response body.',
  'A 429 response can communicate rate limiting and may include Retry-After guidance for clients.',
  'A 503 response can indicate temporary service unavailability and may also communicate retry timing.',
  'HTTP redirects can change method behavior depending on status code, client rules, and the original request.',
  'Content negotiation can use Accept, Accept-Language, and related headers to select representations.',
  'Compression reduces transfer size but consumes CPU and should be evaluated against latency and resource constraints.',
  'Streaming responses let consumers process partial data before the entire representation is available.',
  'AbortController is preferable to merely ignoring a late response because cancellation can release underlying resources.',
  'A robust fetch wrapper validates status, content type, body schema, size limits, timeout, cancellation, and retry policy.',
  'WebSocket application protocols should define heartbeat, reconnect, authentication refresh, message schema, and backpressure behavior.',
  'Server-Sent Events use text/event-stream framing and are suitable for one-way incremental updates.',
  'Web Streams can transform network data incrementally without materializing the complete payload in memory.',
  'CORS is enforced by browsers and does not itself protect a server from non-browser clients.',
  'Origin and Referer headers are contextual request metadata and should not be treated as a complete authorization mechanism.',
  'HTTP cookies can be scoped by domain, path, expiration, and security attributes and should be minimized for sensitive state.',
  'Content Security Policy can restrict script sources and reduce the impact of some injection vulnerabilities.',
  'Subresource Integrity verifies selected fetched resources against expected cryptographic digests.'
 ],
 testing:[
  'A deterministic unit test should isolate external time, randomness, network, filesystem, and process state when those are not under test.',
  'Test doubles include stubs, spies, mocks, and fakes, with different tradeoffs in coupling and behavioral realism.',
  'Snapshot tests are useful for stable structured output but can become noisy when large snapshots change frequently.',
  'Golden-file tests compare generated output with an approved fixture and work well for serializers and code generators.',
  'Boundary-value tests target values immediately inside and outside accepted ranges.',
  'Fuzz testing explores broad malformed or unusual input spaces and is especially valuable for parsers.',
  'Property-based tests express invariants that should hold across many generated inputs.',
  'Concurrency tests should control scheduling where possible so a failure is reproducible rather than timing-dependent.',
  'A retry test should verify both retryable and non-retryable failures so error policy does not accidentally become infinite retry.',
  'Integration tests should verify contracts at real boundaries while keeping external dependencies controlled.',
  'End-to-end tests are most valuable for critical user journeys rather than exhaustive coverage of every internal branch.',
  'Code coverage measures exercised structure and does not prove that assertions meaningfully validate behavior.',
  'Mutation testing can expose tests that execute code without detecting important semantic changes.',
  'Regression tests should encode the smallest reliable example of a previously broken behavior.',
  'Performance tests need stable workloads, warm-up policy, repeated measurements, and environmental controls.'
 ],
 performance:[
  'Algorithmic complexity often dominates micro-optimizations; reducing an O(n^2) operation can matter more than tuning individual allocations.',
  'Memoization can trade memory for speed and should include invalidation or bounded size when inputs are unbounded.',
  'Object allocation creates garbage-collection work, so high-frequency paths can benefit from carefully designed data reuse.',
  'Typed arrays can provide compact numeric storage and predictable binary layouts for large numeric datasets.',
  'Avoiding unnecessary string concatenation in hot loops can reduce temporary allocations, although modern engines optimize many common cases.',
  'Regular expressions can become expensive with ambiguous nested quantifiers and should be tested against adversarial input.',
  'Parsing large JSON values materializes the complete object graph and can cause memory pressure.',
  'Streaming parsers and incremental processing can reduce peak memory for very large inputs.',
  'Web Workers can move CPU-heavy JavaScript away from the main UI thread but add serialization and scheduling overhead.',
  'WebGPU is useful for highly parallel numeric workloads but requires data transfer and pipeline setup that can outweigh benefits for small jobs.',
  'Performance measurements should use representative workloads rather than synthetic microbenchmarks alone.',
  'Profilers distinguish CPU hotspots from I/O waits, garbage collection, layout work, and network latency.',
  'Caching improves latency only when cache lookup, memory cost, invalidation, and stale-data behavior are acceptable.',
  'Batching reduces per-operation overhead but can increase latency and memory usage if batches become too large.',
  'Backpressure is a performance and reliability mechanism because it prevents fast producers from overwhelming slower consumers.'
 ],
 architecture:[
  'A layered architecture separates policy, orchestration, deterministic tools, retrieval, generation, persistence, and transport concerns.',
  'Pure functions make deterministic components easier to test and reuse because hidden state is minimized.',
  'An adapter should translate interfaces rather than duplicate the business rules of the adapted component.',
  'A facade can provide a small stable API over several internal services without exposing their coordination details.',
  'A command bus can centralize dispatch and instrumentation for application actions when command semantics are explicit.',
  'Event-driven systems should define delivery guarantees, ordering assumptions, duplicate handling, and consumer recovery.',
  'Idempotent handlers make retries safer in queues and distributed workflows.',
  'Circuit breakers prevent repeated calls to a failing dependency and should have explicit half-open recovery behavior.',
  'Bulkheads isolate resource pools so failure in one workload does not exhaust capacity needed by another.',
  'Timeouts should reflect downstream service objectives rather than arbitrary globally shared constants.',
  'A bounded queue needs an explicit policy for overload such as rejection, shedding, prioritization, or backpressure.',
  'Observability should expose latency, throughput, errors, saturation, queue depth, and dependency health.',
  'Configuration should be parsed once into validated typed state instead of repeatedly interpreting raw environment strings.',
  'Feature flags should have ownership, default behavior, rollout strategy, and removal criteria.',
  'Schema evolution should preserve compatibility or include explicit migrations when persisted data outlives a software release.'
 ],
 data:[
  'A stable sort key enables cursor pagination because records can be resumed without relying on mutable page numbers.',
  'Offset pagination can skip or repeat records when data changes between requests; cursor pagination can reduce this problem.',
  'Aggregation should define treatment of missing, null, NaN, infinite, and invalid values before computing statistics.',
  'A histogram depends on bin boundaries and should document whether intervals are open, closed, or half-open.',
  'Median and percentile calculations require a defined interpolation method for even or fractional ranks.',
  'Correlation measures association and does not establish causation without additional assumptions and experimental design.',
  'Joining datasets requires explicit key equality, missing-key behavior, duplicate-key semantics, and output cardinality.',
  'A left join preserves all records from the left relation while adding matching data from the right where available.',
  'Pivot operations transform distinct values into columns and can create very wide outputs when cardinality is high.',
  'CSV output should quote fields containing delimiters, quotes, or line breaks according to the chosen CSV dialect.',
  'UTF-8 byte length differs from JavaScript UTF-16 code-unit length and matters for network and storage limits.',
  'Checksums can detect accidental corruption while cryptographic hashes provide stronger integrity properties against deliberate modification.',
  'Canonical serialization can make hashes and signatures stable by defining ordering and representation rules.',
  'Schema validation should happen at trust boundaries before downstream business logic assumes field types.',
  'Data normalization should preserve semantics and avoid silently converting values whose distinctions matter to the domain.'
 ],
 security:[
  'Input validation should constrain type, length, encoding, structure, and allowed values rather than merely checking for a few bad substrings.',
  'Output encoding should match the target context such as HTML, URL, JavaScript string, CSS, or SQL syntax.',
  'Parameterized database queries separate data from SQL syntax and reduce injection risk.',
  'Command execution should pass arguments structurally rather than constructing shell command strings from untrusted input.',
  'Path traversal defenses should canonicalize paths and verify that the resolved target remains within the intended directory.',
  'Prototype pollution defenses should avoid unsafe recursive merges of attacker-controlled keys.',
  'Object.hasOwn and null-prototype dictionaries can reduce ambiguity when processing untrusted object keys.',
  'Secrets should not be logged, embedded in browser bundles, committed to repositories, or exposed in error messages.',
  'Cryptographic random values should come from a cryptographically secure source rather than Math.random.',
  'Password storage should use a dedicated password hashing scheme with appropriate cost parameters rather than general-purpose hashing.',
  'Authentication and authorization are distinct: proving identity does not automatically grant permission to perform an operation.',
  'Least privilege limits the impact of compromised credentials or components by granting only necessary permissions.',
  'Rate limiting can reduce abuse but should be designed around identities, resources, costs, and legitimate burst behavior.',
  'Audit logs should capture security-relevant events without recording sensitive secrets or unnecessary personal data.',
  'Dependency security includes version review, lockfile integrity, provenance, and minimizing unnecessary packages.'
 ]
};
const entries=Object.entries(packs).flatMap(([domain,items])=>items.map((text,index)=>({id:`js-v4-${domain}-${index+1}`,domain,text})));
export const LARGE_JAVASCRIPT_KNOWLEDGE_V4=entries;
export const LARGE_JAVASCRIPT_KNOWLEDGE_V4_TEXT=entries.map(x=>x.text).join('\n');
export const javascriptKnowledgeV4Stats=()=>({version:4,entries:entries.length,domains:Object.keys(packs),characters:LARGE_JAVASCRIPT_KNOWLEDGE_V4_TEXT.length});
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledgeV4={LARGE_JAVASCRIPT_KNOWLEDGE_V4,LARGE_JAVASCRIPT_KNOWLEDGE_V4_TEXT,javascriptKnowledgeV4Stats};
