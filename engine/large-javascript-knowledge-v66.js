// TONY Large JavaScript Knowledge v66.
// Authored deterministic JavaScript/web engineering knowledge; not pretrained neural data.
const packs={
  semantics:[['Equality','Use strict equality for predictable primitive comparisons; Object.is is appropriate when NaN and signed zero distinctions matter.'],['Descriptors','Property descriptors define writable, enumerable, and configurable behavior and can be inspected with Object.getOwnPropertyDescriptor.'],['Proxy','Proxy traps can customize object operations but should preserve expected invariants of the target object.'],['Reflect','Reflect methods provide predictable primitive operations useful when implementing Proxy traps.'],['WeakMap','WeakMap keys are objects or non-registered symbols and do not expose enumerable key iteration.'],['WeakRef','WeakRef and FinalizationRegistry are nondeterministic lifecycle tools and should not be used for correctness-critical cleanup.'],['BigInt','BigInt represents arbitrary-precision integers but cannot be mixed directly with Number arithmetic.'],['Symbol','Symbols provide unique property keys and protocol hooks such as Symbol.iterator and Symbol.toPrimitive.'],['Iterators','Iterator protocols separate value production from iterable discovery and support explicit return cleanup.'],['Regex','Regular expression flags change matching semantics and stateful global or sticky expressions require careful lastIndex handling.']],
  modules:[['Instantiation','ES modules are instantiated before evaluation, establishing bindings across the dependency graph.'],['LiveBindings','Imported bindings remain connected to exports, so changes to exported variables can be observed by importers.'],['NamespaceObjects','A module namespace exposes exported names through a stable read-oriented namespace object.'],['DynamicImport','Dynamic import creates an asynchronous module boundary and returns a promise for the namespace.'],['TopLevelAwait','Top-level await can delay dependent module evaluation, so startup dependency chains should remain intentional.'],['ExportsMap','Package exports maps should expose only supported entry points and prevent accidental reliance on internal paths.'],['ImportConditions','Conditional exports select files for different environments and must maintain compatible public behavior.'],['ImportMaps','Browser import maps provide explicit specifier-to-URL mappings and should be deployed consistently.'],['Circularity','Circular dependencies can observe bindings before their initialization completes and should be tested explicitly.'],['SideEffectMetadata','Bundler side-effect declarations are correctness metadata and must match actual module initialization behavior.']],
  async:[['AbortController','AbortController provides cooperative cancellation and should be propagated through nested operations.'],['AbortTimeout','Timeout signals turn deadlines into composable cancellation inputs.'],['PromiseAll','Promise.all rejects when any input rejects and otherwise preserves fulfillment order.'],['AllSettled','Promise.allSettled reports every outcome and is useful when independent failures should not short-circuit aggregation.'],['PromiseAny','Promise.any resolves after the first fulfillment and aggregates failures only when every input rejects.'],['Microtasks','Promise callbacks run as microtasks and can starve later tasks if code continually schedules more microtasks.'],['AsyncGenerators','Async generators combine incremental production with asynchronous waiting and should support early cleanup.'],['Concurrency','Bounded concurrency prevents an asynchronous workload from creating unbounded resource pressure.'],['Retry','Retries need transient-failure classification, a maximum attempt budget, and an overall deadline.'],['Finally','finally is appropriate for cleanup that must happen regardless of fulfillment or rejection.']],
  dom:[['CurrentTarget','event.currentTarget identifies the listener target while event.target identifies the event target, which can differ under delegation.'],['Capture','Capture listeners run while an event travels from the root toward the target and can enforce early policy.'],['Passive','Passive listeners promise not to cancel the event and can improve handling of scroll-related input.'],['Once','The once listener option automatically removes a listener after its first invocation.'],['SignalListeners','Passing an AbortSignal when registering listeners provides lifecycle-driven listener cleanup.'],['Template','Template elements provide inert reusable markup that can be cloned into live DOM.'],['ShadowDOM','Shadow DOM encapsulates structure and style while allowing explicit slot-based composition.'],['Mutation','MutationObserver batches mutation records and should be disconnected when its observation scope ends.'],['Resize','ResizeObserver reacts to element geometry changes without continuous polling.'],['Intersection','IntersectionObserver enables viewport-aware loading and work scheduling.']],
  networking:[['RequestBody','Fetch bodies are streams and can only be consumed once unless a request or response is cloned before consumption.'],['Headers','Headers normalizes HTTP header access and works across Fetch request and response objects.'],['URL','The URL API provides structured parsing and serialization instead of manual string concatenation.'],['QueryParameters','URLSearchParams handles encoding and repeated query keys consistently.'],['WebSocket','WebSocket applications need explicit authentication, reconnect, heartbeat, and close policies.'],['EventSource','EventSource provides one-way server-sent events and needs lifecycle-aware close behavior.'],['BroadcastChannel','BroadcastChannel enables same-origin tab communication but incoming messages still require validation.'],['MessageChannel','MessageChannel creates paired MessagePorts for explicit bidirectional asynchronous messaging.'],['WebRTC','WebRTC media and data sessions require signaling and explicit connection lifecycle management.'],['ConditionalRequests','ETag and conditional request headers can avoid transferring representations that have not changed.']],
  streams:[['ReadableLock','A readable stream is locked while an active reader owns it, so consumer ownership must be explicit.'],['Backpressure','Writable and transform streams communicate capacity so producers can avoid unbounded buffering.'],['PipeTo','pipeTo composes readable and writable streams and can propagate errors and cancellation.'],['PipeThrough','pipeThrough inserts a transform stage while keeping the pipeline streaming.'],['ByteStreams','Byte streams can use byte-oriented readers for efficient binary processing.'],['DecoderStream','TextDecoderStream safely decodes UTF-8 across arbitrary chunk boundaries.'],['Tee','Teeing duplicates a stream but can increase buffering when branches consume at different rates.'],['Framing','Application protocols need explicit framing because transport chunks do not necessarily align with messages.'],['IncrementalJSON','Incremental parsers must retain incomplete lexical state across chunks rather than assuming chunk completeness.'],['UploadCancellation','Streaming request producers should stop generating data when their cancellation signal fires.']],
  node:[['HTTPTimeouts','Node HTTP servers should bound header, body, request-processing, and idle durations.'],['KeepAlive','Connection reuse improves latency but requires bounded pools and sensible idle expiration.'],['SocketErrors','Socket failures should clear timers and request bookkeeping before resources are released.'],['Filesystem','Filesystem resources such as descriptors and streams need deterministic closure on both success and failure.'],['RootedPaths','Resolve user-controlled paths against an approved root and reject traversal outside that root.'],['Environment','Environment variables arrive as strings and should be validated against the intended configuration schema.'],['Signals','Signal handlers should initiate controlled shutdown rather than abruptly abandoning active work.'],['Workers','Worker threads are useful for CPU-bound tasks that would otherwise block the event loop.'],['DNS','DNS resolution failures should be distinguished from application protocol failures.'],['TLS','TLS certificate verification is an authenticity boundary and should remain enabled for normal secure connections.']],
  security:[['AuthenticationAuthorization','Authentication identifies an actor; authorization decides whether that actor may perform a specific action.'],['CORS','CORS controls browser cross-origin reads and cannot replace server-side authorization.'],['CSRF','State-changing browser requests need CSRF protection appropriate to the authentication design.'],['CSP','Content Security Policy limits resource sources and can reduce the impact of injected scripts.'],['TrustedTypes','Trusted Types can enforce controlled construction of strings reaching dangerous DOM sinks.'],['SRI','Subresource Integrity allows browsers to verify expected hashes for fetched resources.'],['HSTS','HTTP Strict Transport Security instructs compatible browsers to use HTTPS for a protected origin.'],['ReferrerPolicy','Referrer Policy controls how much referrer information is transmitted across requests.'],['PermissionsPolicy','Permissions Policy constrains selected browser capabilities by origin and embedding context.'],['RateLimit','Rate limits should be applied to expensive or abuse-prone operations with predictable rejection behavior.']],
  performance:[['LayoutReadsWrites','Interleaving layout reads with writes can force synchronous recalculation; batching phases reduces thrashing.'],['Animation','requestAnimationFrame aligns visual updates with browser rendering opportunities.'],['Idle','Idle callbacks are appropriate for noncritical work that can tolerate delay and should still be bounded.'],['Virtualization','Virtualization limits rendered nodes to a moving viewport window for large collections.'],['ContentVisibility','content-visibility can allow the browser to skip rendering work for offscreen content.'],['Memoization','Memoization requires stable keys and bounded cache growth when inputs are unbounded.'],['PerformanceObserver','PerformanceObserver provides structured access to performance entries without polling.'],['LongTasks','Long-task observations identify main-thread work that can harm responsiveness.'],['ResourceTiming','Resource Timing exposes network resource timing data useful for identifying slow dependencies.'],['Budgets','Performance budgets turn latency, bundle size, and long-task goals into measurable regression checks.']],
  testing:[['Properties','Property-based tests validate invariants across many generated inputs rather than a few examples.'],['Metamorphic','Metamorphic testing checks relationships that should remain true after controlled input transformations.'],['Differential','Differential tests compare outputs from independent implementations or versions.'],['RaceTesting','Race tests deliberately vary asynchronous completion order to reveal hidden scheduling assumptions.'],['Fuzzing','Fuzzing should use bounded CPU, memory, input-size, and execution budgets.'],['Golden','Golden outputs require intentional review when expected behavior changes.'],['Contracts','Contract tests validate integration request, response, and error semantics at system boundaries.'],['Cleanup','Tests should assert that listeners, timers, streams, workers, and temporary resources are released.'],['Regression','Every repaired defect should have focused coverage that would fail against the defective behavior.'],['Reproducibility','Seeded generation and deterministic clocks make intermittent failures easier to reproduce.']],
  architecture:[['DependencyInversion','Dependency inversion keeps core policy independent of concrete infrastructure.'],['CommandQuery','Command-query separation makes mutation and observation responsibilities easier to reason about.'],['Idempotency','Idempotent operations converge safely when clients retry after ambiguous failures.'],['Deadlines','End-to-end deadlines prevent downstream work from continuing after the caller can no longer benefit.'],['Bulkheads','Bulkheads isolate resource pools so one overloaded workload cannot consume every available resource.'],['CircuitBreaker','Circuit breakers reduce repeated calls to dependencies that are already failing.'],['BoundedQueue','Bounded queues make overload explicit and prevent memory growth without limit.'],['DeadLetter','Dead-letter storage preserves messages that cannot be processed automatically for controlled investigation.'],['Readiness','Readiness withdrawal can stop new traffic before shutdown while active work drains.'],['Observability','Structured logs, metrics, traces, and correlation IDs make distributed behavior diagnosable.']],
  data:[['Map','Map models explicit key-value relationships and preserves insertion order for iteration.'],['Set','Set provides direct membership and deduplication semantics.'],['TypedArrays','Typed arrays provide fixed-width views over binary memory and are useful for compact numerical data.'],['DataView','DataView supports explicit byte offsets and endianness for binary protocols.'],['StructuredClone','structuredClone can copy supported object graphs and transfer selected transferable resources.'],['ImmutableSorting','toSorted creates a sorted copy and avoids mutating the original array.'],['ImmutableWith','with creates an array copy with one indexed value replaced.'],['SchemaVersion','Persisted schemas need version identifiers and migration behavior when representations evolve.'],['CanonicalSerialization','Canonical serialization produces stable representations for hashing, caching, comparison, or signatures.'],['BoundsValidation','Binary decoders must validate offsets, lengths, and numeric ranges before creating views or reading fields.']],
  tooling:[['SourceMaps','Source maps connect generated code locations to authored sources and are valuable for debugging optimized builds.'],['Lockfiles','Lockfiles make dependency resolution reproducible by recording selected package versions and integrity data.'],['Semver','Semantic version ranges communicate compatibility expectations but should not replace testing against actual dependency behavior.'],['CodeSplitting','Code splitting defers optional modules so initial downloads contain only startup-critical functionality.'],['Minification','Minification reduces transport size but should preserve semantics and remain covered by build validation.'],['TypeDeclarations','TypeScript declaration files describe external type contracts but do not validate runtime values.'],['Linting','Lint rules catch recurring classes of mistakes before execution and should be integrated into automated checks.'],['BuildCache','Build caches improve iteration speed when cache keys include every meaningful build input.'],['DependencyAudit','Dependency auditing identifies known package vulnerabilities but does not prove application security.'],['CIValidation','Continuous integration should combine static checks, focused tests, integration tests, and reproducible build checks.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V66_TEXT=Object.entries(packs).flatMap(([domain,items])=>items.map(([topic,text])=>`${domain}: ${topic}. ${text}`)).join('\n');
export const javascriptKnowledgeV66Stats=()=>({version:'66',packs:Object.keys(packs).length,entries:Object.values(packs).reduce((n,x)=>n+x.length,0),domains:Object.keys(packs),authored:true,pretrained:false});
export default LARGE_JAVASCRIPT_KNOWLEDGE_V66_TEXT;
