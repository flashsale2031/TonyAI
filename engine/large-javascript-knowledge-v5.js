// TONY Large JavaScript Knowledge Expansion v5.
// Additional pure-JavaScript engineering knowledge for local retrieval and fitting.
// These are authored knowledge statements, not pretrained neural weights.
const packs={
 language:[
  'Primitive values include undefined, null, boolean, number, bigint, string, and symbol; objects form the other broad value category.',
  'Object property keys are strings or symbols; numeric-looking keys on ordinary objects are represented as strings.',
  'Property descriptors distinguish value, writable, enumerable, configurable, get, and set attributes.',
  'Object.defineProperty can create non-enumerable or non-writable properties that ordinary assignment would not express.',
  'Object.freeze prevents extensions and makes existing data properties non-writable and non-configurable at the object level.',
  'Object.seal prevents extensions and makes existing properties non-configurable while preserving writability where applicable.',
  'Object.preventExtensions blocks new own properties but does not by itself make existing properties immutable.',
  'Reflect methods provide functional counterparts to several internal object operations and return explicit success information.',
  'Proxy traps can intercept property access and mutation, but invariants imposed by the language still constrain legal trap results.',
  'A proxy can change observable behavior of property operations and therefore should be used carefully around identity-sensitive code.',
  'Symbols provide non-string property keys and can participate in language protocols such as iteration and primitive conversion.',
  'Well-known symbols such as Symbol.iterator and Symbol.asyncIterator let objects integrate with language iteration protocols.',
  'The iterable protocol exposes a method returning an iterator whose next method returns iterator-result objects.',
  'Iterator results use a value field and a done field, with done indicating completion of iteration.',
  'Generators combine iterator production with resumable execution and can yield multiple values over time.',
  'Async generators combine asynchronous control flow with the async iteration protocol.',
  'Promise callbacks run through the microtask queue after the current synchronous execution completes.',
  'Promise resolution can assimilate thenables, so resolving with an object may adopt another asynchronous computation.',
  'Promise.all rejects when any input rejects and preserves the input ordering of fulfilled results.',
  'Promise.allSettled reports fulfillment or rejection for every input rather than failing fast on the first rejection.',
  'Promise.race settles when the first supplied promise-like input settles.',
  'Promise.any fulfills on the first fulfillment and rejects only when all supplied inputs reject.',
  'Finally handlers execute after settlement and normally preserve the prior outcome unless the handler throws or returns a rejected promise.',
  'Async functions always return promises even when their body returns an ordinary value.',
  'Await pauses the async function and resumes it through promise continuation machinery rather than blocking the JavaScript thread.'
 ],
 types:[
  'Number uses IEEE 754 binary64 semantics, so some decimal fractions cannot be represented exactly.',
  'NaN is not equal to itself under ordinary equality, while Number.isNaN provides a direct NaN check.',
  'Object.is distinguishes positive and negative zero and treats NaN as equal to itself.',
  'BigInt represents integers of arbitrary magnitude but cannot be freely mixed with Number in arithmetic expressions.',
  'Typed numeric conversion can wrap, clamp, truncate, or reinterpret values depending on the typed-array and DataView operation.',
  'String length counts UTF-16 code units rather than Unicode scalar values or grapheme clusters.',
  'Code points above the BMP may occupy two UTF-16 code units in JavaScript strings.',
  'for...of iterates values while for...in enumerates property keys and has different semantics.',
  'typeof null returns object for historical compatibility and should not be used as a complete null check.',
  'Array.isArray distinguishes arrays across realms more reliably than instanceof Array.',
  'Dates represent an instant as milliseconds from the ECMAScript epoch but have legacy parsing and timezone behavior.',
  'Temporal, where available, separates concepts such as instants, plain dates, and zoned date-time values more explicitly than Date.',
  'Intl.DateTimeFormat formats dates for a locale and timezone without requiring manual month and weekday tables.',
  'Intl.NumberFormat can format currency, units, percentages, compact numbers, and locale-sensitive decimal representations.',
  'Intl.RelativeTimeFormat formats phrases such as relative days or durations according to locale rules.',
  'Intl.Segmenter can segment text into graphemes, words, or sentences when supported by the runtime.',
  'RegExp Unicode property escapes can match character categories using the u or v regular-expression modes.',
  'The v regular-expression mode extends Unicode-aware character-set operations in runtimes that support it.',
  'Locale-aware string comparison should not assume that Unicode code-point order is the desired human sorting order.',
  'Numeric coercion rules differ among +, arithmetic operators, comparisons, and bitwise operators.',
  'Unary plus performs numeric coercion while unary minus performs numeric coercion followed by negation.',
  'Bitwise operators convert operands to signed 32-bit integer representations before operating.',
  'The unsigned right-shift operator produces a non-negative 32-bit result while signed right shift preserves sign bits.',
  'The exponentiation operator has precedence and associativity rules that differ from several familiar arithmetic operators.',
  'Relational comparison can compare strings lexically or convert values numerically depending on operand types.'
 ],
 functions:[
  'Functions are objects and can have properties, prototypes, and callable behavior.',
  'Arrow functions do not create their own this, arguments, super, or new.target bindings.',
  'Ordinary functions can be used as constructors when they have the required construct behavior.',
  'Class constructors cannot be invoked as ordinary functions without new.',
  'Bound functions retain a bound this value and can also carry pre-applied arguments.',
  'Function.prototype.call invokes a function with an explicit this value and individual arguments.',
  'Function.prototype.apply invokes a function with an explicit this value and an array-like argument collection.',
  'Function.prototype.bind creates a new function with selected this and argument bindings.',
  'Closures retain access to lexical bindings after the outer function has returned.',
  'Closures can intentionally encapsulate mutable state without exposing the state as a public object property.',
  'Recursion needs a terminating condition and can exceed the call stack for very deep synchronous recursion.',
  'Tail-call syntax exists in ECMAScript semantics but broad proper-tail-call optimization is not a dependable assumption across JavaScript engines.',
  'Higher-order functions accept functions as values or return functions as results.',
  'Currying transforms a multi-argument conceptual operation into a sequence of unary function applications.',
  'Partial application binds some arguments while leaving others to be supplied later.',
  'Debouncing delays execution until calls stop arriving for a configured interval.',
  'Throttling limits execution frequency while calls continue to arrive.',
  'A cancellation-aware function should propagate AbortSignal or another explicit cancellation mechanism rather than relying on shared mutable flags.',
  'A callback that captures a large object can keep that object reachable longer than intended.',
  'Memoized functions should define equality of arguments and invalidation or capacity policy.',
  'Recursive tree algorithms can be rewritten with explicit stacks when call-stack depth is a concern.',
  'Iterative state machines can make asynchronous workflows easier to pause, resume, inspect, and recover.',
  'Function composition should document evaluation order because left-to-right and right-to-left composition conventions both exist.',
  'Pure functions do not mutate observable external state and therefore are easier to cache and test.',
  'A function contract should specify accepted inputs, returned values, side effects, errors, and cancellation behavior.'
 ],
 errors:[
  'Error objects can carry name, message, stack, cause, and custom domain properties.',
  'Error causes can preserve the lower-level failure while presenting a higher-level error context.',
  'try...catch catches exceptions thrown during the protected synchronous evaluation and awaited promise rejection inside an async function.',
  'finally executes during control-flow completion and should avoid accidentally masking the original error with a new throw or return.',
  'AggregateError represents multiple underlying errors and is useful for operations that evaluate several alternatives.',
  'RangeError indicates a value outside an allowed numeric or structural range when the API chooses that error type.',
  'TypeError commonly indicates an operation applied to an incompatible value or type.',
  'SyntaxError indicates invalid source or syntax when parsing or evaluating JavaScript text.',
  'ReferenceError commonly occurs when an identifier cannot be resolved in the current lexical environment.',
  'URIError represents malformed URI handling in APIs that use that error category.',
  'Errors crossing worker or process boundaries may lose custom prototypes and should be serialized into explicit data.',
  'Network failures are not all represented as HTTP errors; a fetch promise can reject before an HTTP response exists.',
  'HTTP status codes such as 404 and 500 do not automatically reject fetch promises and should be checked explicitly.',
  'A timeout should produce a distinguishable error so callers can decide whether retrying is appropriate.',
  'Retry policies should distinguish transient failures from deterministic validation or authorization failures.',
  'Exponential backoff reduces synchronized retry pressure and is commonly combined with jitter.',
  'Circuit breakers should stop attempting a dependency after repeated failures and later probe recovery.',
  'Error messages should avoid exposing secrets, credentials, authorization tokens, or private request content.',
  'Logging an entire request object can accidentally capture sensitive headers or payloads and should be avoided.',
  'An error boundary in an application architecture should prevent one failed task from corrupting unrelated state.',
  'Promise rejection handlers should be attached deliberately so expected failures do not become unhandled rejections.',
  'An error should be classified before recovery so retry, fallback, user correction, or terminal failure can be selected.',
  'Idempotency keys can make retried mutation requests safer when a service supports them.',
  'Compensation actions can restore application invariants when a multi-step workflow fails after partial completion.',
  'A recovery record should preserve enough state to diagnose the failure without storing unnecessary sensitive input.'
 ],
 async:[
  'The event loop processes queued work and allows asynchronous I/O to progress without blocking the JavaScript execution thread.',
  'Microtasks generally run before the event loop proceeds to later task phases after the current job completes.',
  'setTimeout provides a minimum delay before a callback becomes eligible rather than a guaranteed exact execution time.',
  'setInterval can drift and can overlap logical work if callbacks take longer than the intended interval.',
  'A recursive timeout can schedule the next operation after the current asynchronous work finishes and can avoid interval overlap.',
  'queueMicrotask schedules a microtask without creating a promise solely for scheduling.',
  'MessageChannel can provide a task-style scheduling mechanism in environments that expose it.',
  'requestAnimationFrame schedules work near a browser rendering opportunity and is suited to visual updates.',
  'requestIdleCallback, where available, schedules lower-priority work during browser idle periods.',
  'Async iteration can consume streams and other asynchronous producers with for await...of.',
  'Promise concurrency should be bounded when the producer can create many expensive operations.',
  'A semaphore can limit the number of active asynchronous tasks.',
  'A queue worker should distinguish queued, active, succeeded, failed, cancelled, and retrying states.',
  'AbortController can propagate cancellation from a caller into nested asynchronous operations.',
  'Cancellation is cooperative; an API must observe the signal for aborting to have an effect.',
  'Async resource cleanup belongs in finally blocks or explicit disposal protocols so failure does not skip cleanup.',
  'The Explicit Resource Management proposal uses using and await using in runtimes that implement it.',
  'Top-level await can make module initialization asynchronous and can delay dependents.',
  'Promise chains should return nested promises so errors and completion propagate to the outer chain.',
  'Launching promises without awaiting or collecting them can create background work whose errors are easy to lose.',
  'A concurrency pool can preserve input ordering while completing tasks in parallel.',
  'Backpressure can be expressed by awaiting queue capacity before producing additional work.',
  'Async generators are useful for paginated APIs because they expose one logical stream over repeated requests.',
  'A robust async operation should define timeout, cancellation, retry, cleanup, and result semantics.',
  'Race conditions can occur even in single-threaded JavaScript when asynchronous callbacks observe state at different times.'
 ],
 storage:[
  'localStorage and sessionStorage store strings and are synchronous APIs, so large writes can block the main thread.',
  'Storage quota and persistence behavior vary by browser and should not be treated as an unlimited database.',
  'IndexedDB stores structured cloneable values asynchronously and supports indexed queries and transactions.',
  'IndexedDB transactions are scoped to an object store set and have lifecycle rules that affect when requests may be issued.',
  'An IndexedDB schema upgrade is an explicit version transition and can require careful migration of existing records.',
  'Cache Storage is optimized for request and response pairs and is commonly used by service workers.',
  'A cache entry can become stale even though its lookup succeeds, so freshness policy belongs in application logic.',
  'File System Access APIs, where available, require explicit user permission for many file operations.',
  'Origin Private File System provides origin-scoped file storage in supported browsers.',
  'Web Locks can coordinate work between same-origin contexts when supported.',
  'BroadcastChannel can notify multiple same-origin contexts but does not provide durable message storage.',
  'Shared workers can coordinate multiple browser contexts but have lifecycle and support considerations.',
  'Node file descriptors consume operating-system resources and should be closed deterministically.',
  'Atomic file replacement can use a temporary file followed by a rename when filesystem semantics support the required guarantees.',
  'Directory traversal should not follow untrusted symlinks without an explicit security policy.',
  'A persistent cache should include a schema or version so stale serialized objects can be migrated or invalidated.',
  'Serialization formats should distinguish missing fields from explicit null values when that distinction matters.',
  'JSON cannot directly encode BigInt, undefined properties, Map, Set, functions, or cyclic object graphs.',
  'Structured clone supports more built-in data types than JSON but still does not clone functions.',
  'Binary persistence should define endianness, versioning, record boundaries, and corruption detection.',
  'Checksums can detect accidental storage corruption and should be combined with length or framing information.',
  'A storage layer should bound record sizes to prevent one malformed item from consuming excessive memory.',
  'A cache eviction strategy can use LRU, LFU, TTL, size, or domain-specific priority.',
  'Durable state should be separated from ephemeral runtime state so recovery can reconstruct only what is necessary.',
  'Persistent application data should not contain raw credentials, authentication secrets, or unnecessary private information.'
 ],
 modules2:[
  'CommonJS exports are mutable object properties while ES module exports are statically declared bindings with live-binding semantics.',
  'require caches loaded CommonJS modules by resolved module identity in a process.',
  'Dynamic import returns a promise for a module namespace object.',
  'Import assertions and module attributes can communicate how certain module resources should be interpreted where supported.',
  'JSON modules are environment-dependent and should follow the host runtime rules rather than assumptions from another bundler.',
  'Package.json fields such as type, exports, imports, and bin influence Node package behavior.',
  'The exports field can prevent deep imports into package internals by defining the public module surface.',
  'Conditional exports can choose different entry points for environments such as import and require.',
  'The imports field can define private package-internal specifiers beginning with #.',
  'Workspace and monorepo tooling can change dependency resolution through package-manager-specific linking behavior.',
  'Lockfiles record dependency resolution and integrity metadata and improve repeatability of installations.',
  'Semantic version ranges express compatibility intent but do not guarantee behavioral compatibility.',
  'A package should declare runtime and development dependencies separately so production installation remains predictable.',
  'Tree shaking works best when module exports and side effects are statically analyzable.',
  'Code splitting can defer rarely used modules and reduce initial transfer or parsing work.',
  'Circular dependency graphs should be tested because initialization order can expose partially initialized bindings.',
  'Module top-level code is executed as part of module evaluation and should avoid unnecessary expensive work.',
  'Side-effect-only imports are appropriate when importing a module intentionally initializes a global registration.',
  'A package API should avoid exposing internal object references that consumers can mutate unexpectedly.',
  'Compatibility shims should document which host versions require them and when removal is safe.',
  'Module resolution failures should report the attempted specifier and relevant environment without leaking private paths.',
  'Bundler aliases can diverge from Node runtime resolution and should be tested in both environments when both are supported.',
  'ESM URL resolution treats percent encoding and URL semantics differently from filesystem path string manipulation.',
  'Importing user-controlled module specifiers can become code execution and should never be treated as ordinary data lookup.',
  'Generated module code should escape identifiers and strings according to the target syntax rather than concatenating raw input.'
 ],
 web2:[
  'Fetch Request and Response objects represent HTTP messages and can carry headers, bodies, methods, URLs, and signal state.',
  'A Response body is a stream and generally can be consumed once unless it is cloned before consumption.',
  'ReadableStream teeing can split a stream into two branches but can increase buffering requirements.',
  'TransformStream connects a writable side to a readable side and is useful for incremental processing.',
  'TextDecoderStream can decode streaming UTF-8 data without requiring complete message materialization.',
  'URLSearchParams encodes query parameters using URL form semantics and avoids manual delimiter handling.',
  'URL objects normalize and expose URL components without requiring custom string parsing.',
  'FormData represents multipart form fields and file-like values and can be submitted through fetch.',
  'Headers normalize HTTP header names but do not make arbitrary values safe to trust.',
  'CORS preflight requests can be triggered by methods, headers, or content types outside the browser simple-request rules.',
  'Credentials mode controls whether browser-managed credentials participate in cross-origin fetch behavior.',
  'Referrer policy controls what referrer information the browser sends on outgoing requests.',
  'Cache-Control directives define client and intermediary caching behavior and should align with data sensitivity.',
  'ETag validators support conditional requests and can reduce transfer when representations have not changed.',
  'Last-Modified and If-Modified-Since provide date-based conditional caching semantics.',
  'Range requests can retrieve portions of a representation when the server supports byte ranges.',
  'HTTP trailers can carry metadata after a streamed body where the protocol and runtime support them.',
  'WebSocket close codes communicate why a connection ended and should be interpreted alongside application state.',
  'SSE reconnection can use event identifiers so a server can resume from a previously observed event where supported.',
  'Long polling keeps an HTTP request open until an update or timeout and can be simpler than a persistent socket in some systems.',
  'DNS over HTTPS and other encrypted DNS mechanisms change the network path but do not replace application authorization.',
  'TLS protects transport confidentiality and integrity but applications still need authentication and authorization.',
  'Certificate pinning strategies have operational tradeoffs and should not be improvised in browser applications.',
  'HTTP request size limits should be enforced before expensive parsing or decompression.',
  'A server should treat request bodies as untrusted bytes until content type, encoding, size, and schema have been validated.'
 ],
 algorithms:[
  'Binary search requires a monotonic ordering assumption and runs in logarithmic search time over an indexed sequence.',
  'Two-pointer techniques can reduce nested scans when the input has suitable ordering or window properties.',
  'Sliding-window algorithms maintain aggregate state while a moving interval changes its endpoints.',
  'Prefix sums allow repeated range-sum queries after linear preprocessing.',
  'Difference arrays can represent repeated interval updates efficiently before a final reconstruction pass.',
  'A hash table typically offers expected constant-time lookup but depends on hash distribution and implementation details.',
  'A trie stores prefixes explicitly and is useful for autocomplete, dictionary lookup, and prefix queries.',
  'A heap supports efficient access to an extreme element and is useful for priority queues and top-k selection.',
  'A monotonic stack can solve next-greater or next-smaller element problems in linear time.',
  'Depth-first search naturally supports recursive or explicit-stack traversal of graphs and trees.',
  'Breadth-first search finds shortest paths in unweighted graphs when all edges have equal cost.',
  'Dijkstra requires non-negative edge weights and commonly uses a priority queue for efficient implementations.',
  'Topological sorting applies to directed acyclic graphs and can be used for dependency ordering.',
  'Union-find maintains connected components with near-constant amortized operations using path compression and union by rank or size.',
  'Dynamic programming stores overlapping subproblem results and requires a correct state definition and transition.',
  'Greedy algorithms require a proof or domain property showing that local choices lead to a globally acceptable result.',
  'Divide-and-conquer splits a problem into subproblems, solves them, and combines their results.',
  'Stable sorting preserves the order of equal-key records and is useful when earlier ordering carries meaning.',
  'Quickselect can find a kth-order statistic without fully sorting all elements.',
  'Reservoir sampling selects a bounded random sample from a stream whose length is not known in advance.',
  'Bloom filters trade false positives for compact probabilistic membership tests and do not support exact negative-free deletion without variants.',
  'Counting distinct values exactly can require memory proportional to the number of distinct values, while sketches trade accuracy for bounded memory.',
  'Amortized analysis explains why sequences of operations can be efficient even when individual operations occasionally cost more.',
  'Algorithm choice should account for data distribution, memory limits, mutation costs, and constant factors rather than Big-O notation alone.',
  'A benchmark should measure end-to-end behavior when integration overhead is material.'
 ],
 data2:[
  'CSV parsing must account for quoted delimiters, escaped quotes, embedded newlines, and dialect differences.',
  'JSON Lines represents one JSON value per line and supports incremental processing of large datasets.',
  'NDJSON consumers should tolerate final newline conventions without treating an empty trailing line as a record.',
  'Sorting tabular data requires explicit null ordering and type comparison rules.',
  'Joining data with duplicate keys can multiply rows, so cardinality should be measured before and after the join.',
  'Group-by aggregation should define whether missing keys form a group and how null-like values are handled.',
  'A median requires an ordering definition and a policy for even-sized populations.',
  'Percentile implementations differ in interpolation method, so reported results should document the chosen method.',
  'A weighted average requires non-negative or otherwise explicitly interpreted weights and a policy for zero total weight.',
  'Variance calculations can suffer floating-point cancellation and may benefit from numerically stable online algorithms.',
  'Kahan-style compensated summation can reduce accumulated floating-point error in long numeric sums.',
  'Correlation is undefined or unstable when one variable has effectively zero variance.',
  'Normalization can mean min-max scaling, z-score standardization, vector normalization, or domain-specific transformation.',
  'Missing-data handling can use omission, imputation, sentinel values, or explicit unknown states depending on semantics.',
  'A data pipeline should preserve provenance so transformed values can be traced to their source and transformation step.',
  'Schema drift occurs when incoming records change shape or types over time and should be detected explicitly.',
  'Versioned schemas allow readers to interpret older records while producers evolve.',
  'Canonical field ordering can make deterministic serialization easier to compare and hash.',
  'UTF-8 validation matters when converting arbitrary byte streams into JavaScript strings.',
  'Text normalization should be applied only when semantic equivalence is actually intended.',
  'A numeric parser should reject unexpected trailing characters rather than silently accepting a partial number when strict input is required.',
  'A CSV writer should escape embedded quotes according to the target dialect rather than relying on JSON string escaping.',
  'Streaming aggregation can bound memory when the operation can be expressed as incremental state.',
  'Materializing every intermediate array can multiply memory usage in a long data-processing pipeline.',
  'Data validation belongs at trust boundaries and should produce actionable diagnostics without exposing sensitive record contents.'
 ],
 tooling:[
  'A linter detects structural or stylistic patterns and is not a substitute for semantic tests.',
  'A formatter should produce deterministic output so formatting does not create unrelated diff churn.',
  'Source maps associate generated code positions with original source positions and aid debugging of transformed code.',
  'AST-based transforms are safer for syntax-aware rewriting than regular expressions applied to arbitrary source code.',
  'A parser should distinguish lexical errors, syntax errors, and semantic validation errors.',
  'Tokenization converts source text into lexical units while parsing establishes grammatical structure.',
  'A compiler pipeline can include parsing, transformation, optimization, code generation, and source-map production.',
  'Minification should preserve program semantics and account for strings, comments, regular expressions, and syntax contexts.',
  'Dead-code elimination depends on proving that removed code has no required observable side effects.',
  'Tree shaking can be defeated by dynamic module patterns or unknown side effects.',
  'Dependency graphs should detect cycles when the build system has assumptions about acyclic packages.',
  'A CLI should separate argument parsing, validation, execution, output formatting, and exit-code selection.',
  'Exit code zero conventionally indicates success while non-zero values communicate failure categories to automation.',
  'Environment variables are strings and should be parsed and validated before being used as booleans, numbers, URLs, or enumerations.',
  'Configuration precedence should be documented when defaults, files, environment variables, and command-line arguments can overlap.',
  'Feature flags should be evaluated consistently across a request or workflow so behavior does not change midway through an operation.',
  'Build caches need stable keys based on relevant source, configuration, and toolchain inputs.',
  'Reproducible builds depend on deterministic inputs, pinned dependencies, and controlled timestamps or generated metadata where necessary.',
  'A test runner should report failures with enough context to reproduce the failing case without dumping secrets.',
  'Property-based testing can explore many cases from a compact invariant definition.',
  'Fuzz harnesses should bound input size and runtime so malformed cases cannot exhaust the test process.',
  'Golden fixtures should be reviewed when outputs change to distinguish intended evolution from regressions.',
  'A migration tool should be idempotent or track applied versions so reruns are safe.',
  'Code generation should validate its inputs before emitting syntax and should test representative generated programs.',
  'Developer tooling should fail clearly when a required capability is unavailable rather than silently producing partial output.'
 ],
 security2:[
  'Least privilege means granting only the permissions required for a task and limiting the scope and lifetime of those permissions.',
  'Defense in depth uses independent controls so failure of one protection does not automatically produce compromise.',
  'Authentication establishes identity while authorization determines what an identified principal may do.',
  'Session expiration and revocation policies should reflect the sensitivity and expected lifetime of the application action.',
  'CSRF protections are relevant to browser credentialed requests and should match the application authentication mechanism.',
  'SameSite cookie settings can reduce some cross-site request risks but do not replace authorization checks.',
  'XSS prevention requires context-appropriate output encoding and safe DOM APIs rather than blacklist-only filtering.',
  'textContent is generally safer than innerHTML for inserting untrusted plain text into the DOM.',
  'URL construction from untrusted input should validate allowed schemes and destinations before navigation or resource loading.',
  'Open redirects can arise when applications redirect to arbitrary user-controlled destinations.',
  'Prototype pollution can occur when untrusted keys such as constructor or prototype paths are merged into ordinary objects.',
  'Object.create(null) can be useful for dictionaries where inherited properties are undesirable.',
  'Path normalization should happen before containment checks so encoded or redundant path components cannot bypass a directory boundary.',
  'Symlink handling can defeat simple string-prefix path checks and requires filesystem-aware policy.',
  'Regular expressions supplied by users can cause denial of service if patterns permit catastrophic backtracking.',
  'ReDoS defenses include pattern restrictions, timeouts where available, safe regex engines, or avoiding dynamic regex construction.',
  'Compression bombs can expand to huge data sizes and should be constrained before full decompression.',
  'Resource exhaustion can target memory, CPU, file descriptors, queue slots, or network connections rather than only request body size.',
  'Rate limits should consider identity, endpoint cost, concurrency, and burst behavior rather than using one global number blindly.',
  'Audit logs should record security-relevant actions with timestamps and actor context while minimizing sensitive payload storage.',
  'Secrets should be redacted from logs, errors, telemetry, crash reports, and generated artifacts.',
  'Cryptographic hashes are not encryption and should not be used as a way to recover protected plaintext.',
  'Random values used for security tokens should come from a cryptographically secure random source.',
  'Timing-sensitive comparisons should use appropriate constant-time primitives when comparing secret-derived values.',
  'Security controls should be tested with negative cases as well as successful cases so accidental bypasses are visible.'
 ],
 architecture2:[
  'A hexagonal architecture separates domain logic from adapters for transport, persistence, external services, and user interfaces.',
  'Dependency inversion keeps high-level policy independent from low-level implementation details.',
  'A repository interface can isolate persistence mechanics from domain services while preserving explicit query semantics.',
  'A service layer should contain meaningful domain operations rather than becoming a generic pass-through for every function.',
  'A state machine represents allowed transitions explicitly and can reject impossible states early.',
  'A workflow engine can persist checkpoints so long operations can resume after process failure.',
  'Saga-style workflows coordinate distributed steps with compensating actions rather than assuming one global transaction.',
  'Outbox patterns can couple database state changes with reliable publication of domain events.',
  'Inbox or deduplication records can prevent repeated processing of the same event where delivery is at least once.',
  'Idempotency keys let clients safely retry selected operations when the server stores the result for the key.',
  'A bounded worker pool protects a service from unbounded concurrency while preserving throughput under load.',
  'Priority queues should define starvation policy so low-priority work is not indefinitely blocked.',
  'Fair scheduling can rotate among tenants or task classes to avoid one producer monopolizing resources.',
  'Backpressure should propagate toward the producer when consumers cannot keep up.',
  'Circuit breakers and bulkheads address different failure modes and can be combined when dependencies are costly.',
  'Timeout budgets should be propagated through nested calls so inner operations do not consume more time than the caller can tolerate.',
  'Retries multiply load on dependencies, so retry count and backoff must be part of capacity planning.',
  'Graceful shutdown should stop intake, drain or cancel active work according to policy, then close resources.',
  'Health checks should distinguish process liveness from readiness to serve real traffic.',
  'Readiness should fail when required dependencies are unavailable if serving requests without them would be unsafe or useless.',
  'Metrics should avoid unbounded cardinality such as raw user IDs or arbitrary URLs as labels.',
  'Tracing should propagate correlation context across asynchronous boundaries without storing sensitive payloads by default.',
  'Structured logs should use stable event names and typed fields so operational queries remain reliable.',
  'A configuration object should be immutable after validation when runtime mutation would create inconsistent behavior.',
  'Architecture documentation should describe invariants and failure behavior, not only boxes and arrows.'
 ],
 testing2:[
  'A test fixture should be minimal so failures point clearly to the behavior under test.',
  'Arrange-act-assert separates test setup, invocation, and verification and can improve readability.',
  'Tests that depend on wall-clock time should inject a clock or use a controllable timer abstraction.',
  'Tests that depend on randomness should use a deterministic seed or injected random source when reproducibility matters.',
  'Network tests should separate protocol behavior from real external service availability.',
  'Contract tests verify that one component meets the interface assumptions of another component.',
  'Metamorphic tests verify relationships between outputs when a single expected output is difficult to enumerate.',
  'Differential tests compare two implementations on the same inputs and are useful for parsers and transformations.',
  'Property tests should include shrinking so a failing complex case can be reduced to a small counterexample.',
  'Concurrency tests should exercise cancellation, duplicate delivery, ordering, and resource exhaustion paths.',
  'Load tests measure behavior under sustained traffic while stress tests deliberately push beyond expected capacity.',
  'Soak tests run for extended periods to reveal leaks, queue buildup, timer drift, or gradual degradation.',
  'A performance regression test should compare distributions or robust summaries rather than one lucky timing sample.',
  'Flaky tests often depend on timing, shared state, nondeterministic ordering, or external services and should be isolated at the cause.',
  'Test retries can hide real flakiness and should not be used as the primary fix for nondeterministic failures.',
  'Mutation testing changes implementation logic and checks whether tests detect those changes.',
  'Coverage thresholds can enforce a floor but should not be treated as proof of test quality.',
  'Security tests should include malformed inputs, authorization boundary cases, resource exhaustion, and sensitive-data leakage checks.',
  'Snapshot tests should use normalized output when timestamps, identifiers, or ordering are intentionally nondeterministic.',
  'Regression tests should remain after a bug is fixed so the same failure mode is protected in future refactors.',
  'Integration tests should clean up created resources even when assertions fail.',
  'A failed setup step should be reported distinctly from an assertion failure so diagnosis is faster.',
  'Test data should avoid real secrets and should use synthetic identifiers whenever possible.',
  'A test runner should exit non-zero when required tests fail so CI can enforce correctness.',
  'Critical workflows benefit from a small number of high-value end-to-end tests combined with broad lower-level coverage.'
 ],
 performance2:[
  'Hot-path profiling should identify where time is actually spent before optimization changes are made.',
  'Premature optimization can increase complexity without measurable benefit, so benchmarks should accompany material performance work.',
  'Object shapes and property access patterns can influence JavaScript engine optimization, but code should prioritize clear semantics first.',
  'Creating many short-lived objects can increase allocation and garbage-collection pressure.',
  'Large arrays may consume substantial memory because each element can require more representation than a compact typed numeric buffer.',
  'Typed arrays are useful for dense numeric data and interoperability with binary APIs.',
  'Data locality can matter when processing large arrays because memory access patterns affect cache behavior.',
  'Batching work can improve throughput by reducing per-item overhead but can increase latency for the first item.',
  'Streaming avoids materializing all input and output simultaneously when the transformation supports incremental processing.',
  'Chunk sizes should balance syscall overhead, memory use, latency, and downstream processing cost.',
  'Worker threads can parallelize CPU-bound Node.js work when the overhead of message transfer is justified.',
  'Web Workers keep CPU-heavy browser tasks off the main thread and can preserve UI responsiveness.',
  'Structured cloning can be expensive for large objects transferred between workers.',
  'Transferable ArrayBuffers can move binary ownership without copying the underlying bytes.',
  'SharedArrayBuffer can enable shared-memory coordination but introduces synchronization complexity.',
  'WebGPU can accelerate parallel numeric kernels when data transfer and pipeline setup costs are amortized.',
  'Caching can improve latency while increasing memory usage and invalidation complexity.',
  'Cache stampedes occur when many callers regenerate the same missing entry simultaneously.',
  'Request coalescing can allow concurrent callers to share one in-flight computation.',
  'A performance budget defines acceptable latency, transfer size, memory, or CPU limits for a workflow.',
  'Browser layout thrashing occurs when repeated DOM writes and geometry reads force synchronous layout.',
  'Using CSS transforms for suitable visual movement can avoid some layout work compared with changing layout properties.',
  'Virtualized lists render only visible portions of very large collections and reduce DOM size.',
  'Debouncing search input can reduce unnecessary network or computation work while preserving responsive typing.',
  'Performance optimization should preserve cancellation, correctness, accessibility, and observability rather than optimizing one metric in isolation.'
 ]
};
const text=Object.entries(packs).flatMap(([domain,items])=>items.map((text,index)=>`[javascript-v5:${domain}:${index+1}] ${text}`)).join('\n');
export const LARGE_JAVASCRIPT_KNOWLEDGE_V5=Object.fromEntries(Object.entries(packs));
export const LARGE_JAVASCRIPT_KNOWLEDGE_V5_TEXT=text;
export const javascriptKnowledgeV5Stats={version:5,domains:Object.keys(packs),entries:Object.values(packs).reduce((n,a)=>n+a.length,0),characters:text.length,pretrained:false,source:'authored-pure-javascript-knowledge'};
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledgeV5={LARGE_JAVASCRIPT_KNOWLEDGE_V5,LARGE_JAVASCRIPT_KNOWLEDGE_V5_TEXT,javascriptKnowledgeV5Stats};
