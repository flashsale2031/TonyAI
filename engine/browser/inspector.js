export async function inspectPage(page){return await page.evaluate(()=>{const visible=el=>{const r=el.getBoundingClientRect(),s=getComputedStyle(el);return r.width>0&&r.height>0&&s.visibility!=='hidden'&&s.display!=='none';};const attrs=el=>Object.fromEntries([...el.attributes].map(a=>[a.name,a.value]).filter(([k])=>['id','name','type','role','aria-label','aria-labelledby','placeholder','value','href','for'].includes(k)));const controls=[...document.querySelectorAll('input,textarea,select,button,[contenteditable="true"],[role="button"],[role="checkbox"],[role="radio"],[role="combobox"]')].filter(visible).slice(0,500).map((el,i)=>({i,tag:el.tagName.toLowerCase(),text:(el.innerText||el.value||'').trim().slice(0,500),attrs:attrs(el),disabled:!!el.disabled}));const links=[...document.querySelectorAll('a[href]')].filter(visible).slice(0,300).map((a,i)=>({i,text:(a.innerText||a.textContent||'').trim().slice(0,300),href:a.href}));const text=(document.body?.innerText||'').replace(/\s+/g,' ').trim().slice(0,15000);return {url:location.href,title:document.title,text,controls,links,metrics:{controls:controls.length,links:links.length,textLength:text.length}};});}
