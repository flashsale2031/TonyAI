// TONY Large JavaScript Knowledge v37.
// Authored deterministic JavaScript/web engineering knowledge; no pretrained model.
const packs={
 language:['Lexical declarations use temporal dead zones until initialization; accessing a let, const, or class binding before initialization throws ReferenceError.','The comma operator evaluates operands left to right and yields the final operand value; parentheses make intentional use clearer.','Logical operators return operand values rather than always booleans, enabling compact conditional value selection.','Nullish coalescing treats only null and undefined as missing, unlike || which also treats 0, false, and empty strings as falsy.','Optional chaining short-circuits a contiguous property, call, or element access chain when the receiver is nullish.','Destructuring defaults apply when the matched value is undefined, not when it is null.','Object.is differs from strict equality for NaN and signed zero: Object.is(NaN,NaN) is true while Object.is(0,-0) is false.','A Symbol primitive is unique unless created through Symbol.for, which uses the global symbol registry.','BigInt arithmetic cannot be mixed directly with Number arithmetic; explicit conversion is required at numeric boundaries.','Numeric separators improve readability of large numeric literals without changing their numeric value.'],
 functions:['Function declarations are instantiated during declaration processing, while function expressions depend on evaluation of their binding.','Arrow functions capture this lexically and do not provide their own arguments, super, or new.target bindings.','Default parameters are evaluated at call time and can reference earlier parameters in the same parameter list.','Rest parameters collect remaining arguments into a real array rather than the legacy array-like arguments object.','Closures retain lexical bindings after the creating function returns, enabling private state and factory patterns.','new creates an object whose prototype is linked to the constructor prototype before the constructor body runs.','bind creates a new function with a fixed this value and optional pre-applied arguments; it does not mutate the original function.','Higher-order functions accept or return functions and support adapters, middleware, composition, and reusable policies.','Generators pause at yield and resume with next, throw, or return; next(value) supplies the result of the suspended yield expression.','Async functions always return promises, and returning a value fulfills that promise with the value.'],
 promises:['Promise resolution assimilates thenables, so resolving with an object containing a callable then property can adopt that thenable state.','Promise.all preserves input order in its fulfillment array even when operations complete in a different order.','Promise.allSettled reports every outcome and is useful when partial failure should not abort independent work.','Promise.any fulfills on the first fulfillment and rejects with AggregateError only when every input rejects.','Promise.race settles with the first settled input and does not automatically cancel slower operations.','finally normally preserves the prior outcome unless its callback throws or returns a rejected promise.','AbortController provides a shared cancellation signal; APIs must explicitly observe the signal for cancellation to take effect.','Awaiting a rejected promise throws at the await expression and should be handled with try/catch when recovery is intended.','Promise callbacks run as microtasks after the current job and before the event loop proceeds to another task.','Unbounded self-scheduled microtasks can starve timers and rendering; yielding through a task boundary restores responsiveness.'],
 collections:['Map keys use SameValueZero, so NaN can be a key and positive and negative zero are treated as the same key.','Set stores unique values according to SameValueZero and is useful for membership and deduplication.','WeakMap keys are objects or non-registered symbols and weak references avoid keeping otherwise unreachable keys alive.','WeakSet provides non-enumerable membership for objects and non-registered symbols without strong collection iteration.','Array.prototype.at supports negative indexes and returns undefined when the resulting index is outside the array.','findLast and findLastIndex search from the end and stop at the first matching element.','Array.prototype.with returns a copy with one index replaced while leaving the original array unchanged.','Array grouping methods create keyed collections from callback results and can replace repetitive reduce grouping.','Typed arrays provide fixed-width numeric views over ArrayBuffer storage for binary protocols and numeric workloads.','DataView provides explicit byte-level reads and writes with selectable endianness over buffer storage.'],
 dom:['Event delegation attaches a listener to a stable ancestor and uses target or closest to handle dynamic descendants.','CustomEvent carries application data through detail while preserving the DOM event dispatch model.','DocumentFragment provides a detached tree for batching DOM construction before insertion.','The template element stores inert markup whose content can be cloned before activation.','Shadow DOM encapsulates a component subtree and changes style, selector, and event boundaries.','The slot element projects light-DOM children into a shadow tree and slotchange reports assignment changes.','The dialog element supplies native modal and non-modal behavior and integrates with browser focus handling.','The details and summary elements provide disclosure behavior without custom open-state code.','The popover API provides browser-managed transient UI with explicit show, hide, and toggle behavior.','MutationObserver batches DOM mutation notifications asynchronously and avoids polling for structural changes.'],
 webapis:['Fetch response bodies are streams; consuming json, text, blob, or arrayBuffer normally consumes the body.','Request and Response can be cloned when independent consumers need the same body before consumption.','Headers normalizes names and enforces forbidden-header restrictions in browser contexts.','URLSearchParams uses URL form encoding and supports repeated keys through append.','WebSocket exposes open, message, error, and close lifecycle events; application protocols should define framing and reconnect policy.','EventSource maintains a server-sent event stream and reconnects according to the protocol.','BroadcastChannel communicates between same-origin browsing contexts without sharing a Window reference.','MessageChannel creates two entangled MessagePort endpoints for explicit bidirectional messaging.','Web Locks coordinates named asynchronous resource access across same-origin tabs and workers.','File System Access operations commonly require explicit user permission and should degrade gracefully when unavailable.'],
 workers:['Dedicated workers execute JavaScript off the main thread and communicate through structured cloning and transferable objects.','Transferring an ArrayBuffer moves ownership to another realm and makes the original buffer detached.','SharedArrayBuffer enables shared memory when required cross-origin isolation conditions are satisfied.','Atomics supplies synchronization operations for shared integer typed arrays.','OffscreenCanvas can move supported rendering work into a worker.','Service workers are event-driven and can intercept eligible requests through FetchEvent.','Service worker installation and activation are separate lifecycle phases, so an update may not become active immediately.','Shared workers can serve multiple browsing contexts through MessagePort communication.','Worker termination should be explicit when temporary compute work is complete.','Worker error handling should distinguish startup, runtime, protocol, and cancellation failures.'],
 node:['Node.js streams model readable, writable, duplex, and transform flows and use backpressure to regulate producers.','The node:http API exposes request and response streams; production handlers should handle malformed input and disconnects.','Keep-alive agents reuse TCP connections and reduce setup overhead for repeated HTTP requests.','DNS resolution is asynchronous and may produce different addresses over time; applications should not assume permanence.','TLS servers require certificates and private keys and should keep secret material outside source code and logs.','fs/promises provides promise-based filesystem operations and should be paired with explicit cleanup and error classification.','File handles should be closed in finally blocks or equivalent lifecycle management when operations can fail.','readline interfaces provide line-oriented processing and should be closed when input lifecycle ends.','Environment variables are strings at process boundaries; configuration should validate and convert them explicitly.','SIGTERM commonly initiates graceful shutdown by stopping new work and finishing bounded in-flight operations.'],
 security:['Same-origin policy separates origins by scheme, host, and port and restricts many cross-origin reads unless permitted.','CORS response headers grant selected cross-origin browser access; they do not disable same-origin policy globally.','CSRF defenses should combine appropriate cookie attributes with server-side request validation and suitable anti-CSRF tokens.','Content Security Policy can restrict executable script sources and reduce the impact of many injection vulnerabilities.','Trusted Types can require approved policies for dangerous DOM sinks and make DOM XSS prevention more enforceable.','Subresource Integrity lets browsers verify fetched subresources against expected cryptographic digests.','Permissions Policy limits which powerful browser features documents and embedded frames may use.','Referrer Policy controls how much referring URL information browsers send with requests.','HSTS instructs browsers to prefer HTTPS for an origin and helps prevent downgrade attacks.','Server-side authorization must be enforced independently of client UI visibility; hiding a button is not access control.'],
 performance:['Layout thrashing occurs when code alternates DOM writes with layout reads, forcing repeated synchronous style and layout work.','requestAnimationFrame aligns visual updates with browser rendering and is preferable to arbitrary timer loops for animation.','Debouncing waits for activity to pause, while throttling limits execution frequency during continuous activity.','Virtualization renders only the visible portion of large lists and reduces DOM, layout, and paint costs.','content-visibility can let browsers skip rendering work for content outside the relevant viewport.','PerformanceObserver exposes performance entries asynchronously for targeted instrumentation.','Long main-thread tasks reduce responsiveness; splitting computation or using workers can improve interaction latency.','Resource hints such as preconnect and preload should be applied selectively because unnecessary hints consume capacity.','Caching improves latency when freshness and invalidation rules are explicit; incorrect caching can serve stale data.','Core Web Vitals measure user-facing loading, responsiveness, and visual stability and should be measured in realistic deployments.'],
 testing:['Property-based tests generate many inputs from invariants rather than relying on a small example set.','Metamorphic tests validate relationships between transformed inputs and outputs when an exact oracle is difficult.','Contract tests verify that independently developed components agree on request, response, and error semantics.','Snapshot tests work best for stable structured output and become noisy for volatile nondeterministic data.','Fake timers make time-dependent tests deterministic when code uses an injectable or framework-controlled clock.','Boundary tests target empty, maximum, minimum, malformed, duplicate, and off-by-one inputs where defects cluster.','Mutation testing evaluates test strength by introducing controlled changes and checking whether the suite detects them.','Golden tests compare output against curated fixtures and require deliberate fixture updates for intentional behavior changes.','Deterministic randomness uses an injectable seed or generator so failures can be reproduced exactly.','Regression corpora preserve previously failing cases and should run after parser or ranking changes.']
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V37_TEXT=Object.entries(packs).flatMap(([domain,items])=>items.map((text,index)=>`[javascript-v37:${domain}:${index+1}] ${text}`)).join('\n');
export const javascriptKnowledgeV37Stats=()=>({version:37,packs:Object.keys(packs).length,entries:Object.values(packs).reduce((n,x)=>n+x.length,0),characters:LARGE_JAVASCRIPT_KNOWLEDGE_V37_TEXT.length,pretrained:false});
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledgeV37={LARGE_JAVASCRIPT_KNOWLEDGE_V37_TEXT,javascriptKnowledgeV37Stats};
