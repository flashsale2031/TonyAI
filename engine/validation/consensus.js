export function consensus(candidates=[]){const usable=candidates.filter(x=>x&&x.value!==undefined);const groups=new Map();for(const c of usable){const k=JSON.stringify(c.value);groups.set(k,[...(groups.get(k)||[]),c]);}const best=[...groups.values()].sort((a,b)=>b.length-a.length)[0]||[];return {value:best[0]?.value,agreement:usable.length?best.length/usable.length:0,support:best.map(x=>x.source)};}
