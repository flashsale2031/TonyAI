// Tiny dependency-free local language model, upgraded with adaptive context,
// phrase memory, repetition control, beam-like candidate scoring, and streaming.
const WORD=/[\p{L}\p{N}]+(?:['’-][\p{L}\p{N}]+)*|[^\p{L}\p{N}\s]/gu;
const PUNCT=new Set(['.',',','!','?',';',':',')',']','}']);
function hashSeed(s){let h=2166136261;for(const c of String(s))h=Math.imul(h^c.charCodeAt(0),16777619);return h>>>0;}
const norm=x=>String(x??'').toLowerCase().replace(/\s+/g,' ').trim();
export class TinyLanguageModel{
 constructor({order=5,maxVocabulary=16000,seed=0x9e3779b9,minCount=1,decay=.96}= {}){this.order=Math.max(2,Math.min(8,order));this.maxVocabulary=maxVocabulary;this.seed=typeof seed==='number'?seed>>>0:hashSeed(seed);this.minCount=minCount;this.decay=decay;this.counts=new Map();this.contexts=new Map();this.vocabulary=new Map();this.phrases=new Map();this.trainedTokens=0;this.epochs=0;}
 tokenize(text){return String(text??'').toLowerCase().match(WORD)||[];}
 train(text,{epochs=1}= {}){for(let e=0;e<Math.max(1,epochs);e++){const t=['<s>',...this.tokenize(text),'</s>'];this.trainedTokens+=t.length;this.epochs++;for(let i=0;i<t.length;i++){const w=t[i];this.vocabulary.set(w,(this.vocabulary.get(w)||0)+1);for(let n=1;n<=this.order;n++){if(i-n+1<0)continue;const gram=t.slice(i-n+1,i+1).join(' '),ctx=t.slice(Math.max(0,i-n+1),i).join(' ');this.counts.set(gram,(this.counts.get(gram)||0)+1);this.contexts.set(ctx,(this.contexts.get(ctx)||0)+1);}}for(let i=1;i<t.length-1;i++){const phrase=t.slice(i-1,i+2).join(' ');this.phrases.set(phrase,(this.phrases.get(phrase)||0)+1);}}if(this.vocabulary.size>this.maxVocabulary){this.vocabulary=new Map([...this.vocabulary.entries()].sort((a,b)=>b[1]-a[1]).slice(0,this.maxVocabulary));}return this;}
 candidates(context){const toks=this.tokenize(context),best=[];for(let n=Math.min(this.order-1,toks.length);n>=0;n--){const prefix=toks.slice(-n).join(' '),den=this.contexts.get(prefix)||0;if(!den)continue;for(const [g,c] of this.counts){const p=g.split(' '),pre=p.slice(0,-1).join(' '),w=p.at(-1);if(pre===prefix&&w!=='</s>')best.push({w,c,den,order:n});}if(best.length)break;}return best;}
 scoreCandidates(context,{temperature=.7,topK=10,repetitionPenalty=3}= {}){const recent=this.tokenize(context).slice(-12),cs=this.candidates(context),freq=new Map();for(const x of cs){const base=x.c/x.den;const repeat=recent.includes(x.w)?repetitionPenalty:1;const score=Math.log(Math.max(base,1e-9))/Math.max(.15,temperature)/repeat + Math.log(1+x.order*.35);freq.set(x.w,Math.max(freq.get(x.w)||-Infinity,score));}return [...freq].map(([w,score])=>({w,score})).sort((a,b)=>b.score-a.score).slice(0,topK);}
 sample(context,{temperature=.7,topK=10,repetitionPenalty=3}= {}){const arr=this.scoreCandidates(context,{temperature,topK,repetitionPenalty});if(!arr.length)return null;const weights=arr.map(x=>Math.exp(Math.max(-20,Math.min(8,x.score-arr[0].score))));const total=weights.reduce((a,b)=>a+b,0);let r=this.random()*total;for(let i=0;i<arr.length;i++){r-=weights[i];if(r<=0)return arr[i].w;}return arr.at(-1).w;}
 random(){this.seed=(Math.imul(1664525,this.seed)+1013904223)>>>0;return this.seed/4294967296;}
 generate(prompt,{maxTokens=128,temperature=.7,topK=10,seed=null,repetitionPenalty=3,stopOnSentence=true}={}){if(seed!==null)this.seed=hashSeed(seed);let out=String(prompt||'').trim();if(!out)return '';for(let i=0;i<maxTokens;i++){const w=this.sample(out,{temperature,topK,repetitionPenalty});if(!w)break;out+=(PUNCT.has(w)||out.endsWith('(')?'':' ')+w;if(stopOnSentence&&/[.!?]$/.test(w)&&i>=12)break;}return out;}
 *stream(prompt,options={}){const max=options.maxTokens??128;let out=String(prompt||'').trim();for(let i=0;i<max;i++){const w=this.sample(out,options);if(!w)return;out+=(PUNCT.has(w)?'':' ')+w;yield {token:w,text:out,index:i};if(options.stopOnSentence!==false&&/[.!?]$/.test(w)&&i>=12)return;}}
 perplexity(text){const t=this.tokenize(text);if(t.length<2)return 0;let loss=0,n=0;for(let i=1;i<t.length;i++){const cs=this.candidates(t.slice(0,i).join(' ')),hit=cs.find(x=>x.w===t[i]);loss+=Math.log(1/(hit?hit.c/hit.den:1e-6));n++;}return Math.exp(loss/n);}
 stats(){return {architecture:'adaptive-weighted-token-n-gram',order:this.order,grams:this.counts.size,vocabulary:this.vocabulary.size,phrases:this.phrases.size,trainedTokens:this.trainedTokens,epochs:this.epochs,perplexityAvailable:true,streaming:true};}
 toJSON(){return {order:this.order,maxVocabulary:this.maxVocabulary,seed:this.seed,minCount:this.minCount,decay:this.decay,counts:[...this.counts],contexts:[...this.contexts],vocabulary:[...this.vocabulary],phrases:[...this.phrases],trainedTokens:this.trainedTokens,epochs:this.epochs};}
 static fromJSON(x){const m=new TinyLanguageModel(x);m.counts=new Map(x.counts||[]);m.contexts=new Map(x.contexts||[]);m.vocabulary=new Map(x.vocabulary||[]);m.phrases=new Map(x.phrases||[]);m.trainedTokens=x.trainedTokens||0;m.epochs=x.epochs||0;return m;}
}
export const createTinyLanguageModel=(corpus='',options={})=>new TinyLanguageModel(options).train(corpus);
if(typeof window!=='undefined')window.TONYTinyLanguageModel={TinyLanguageModel,createTinyLanguageModel};
