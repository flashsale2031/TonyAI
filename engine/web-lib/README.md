# TONY Web Library

Browser-native modules with zero third-party runtime dependencies. Modules use Web Platform APIs and are loaded by the TONY runtime. Node.js remains optional for privileged server capabilities such as protected credentials, server-side browser automation, persistent queues and audit storage.

The library is intentionally modular so chat commands can compose capabilities without requiring a package manager.