// TONY Large JavaScript Knowledge Expansion v6.
// Additional authored JavaScript/web engineering knowledge for pure-JS retrieval and local fitting.
// Not a pretrained neural checkpoint.
const packs={
 language:[
  'Primitive values in JavaScript include undefined, null, boolean, number, bigint, string, symbol, and object values.',
  'NaN is a number value and comparisons with NaN are false except for the APIs designed to recognize it.',
  'Object.is differs from strict equality for NaN and signed zero, so equality semantics should match the application requirement.',
  'Property keys are strings or symbols; numeric-looking object keys are represented as strings during ordinary property access.',
  'Enumerability affects Object.keys, for...in, and several reflection operations, so property visibility should be chosen deliberately.',
  'Object.getOwnPropertyDescriptors can inspect writable, enumerable, configurable, getter, and setter characteristics together.',
  'Getters and setters are executable behavior attached to property access and assignment rather than stored data values.',
  'Prototype lookup continues through the prototype chain until a property is found or the chain reaches null.',
  'Object.create(null) creates a dictionary without Object.prototype inherited properties.',
  'Reflect APIs provide explicit forms of several meta-object operations and can return operation success as boolean values.',
  'Proxy traps can intercept fundamental operations but should preserve expected invariants or operations can throw.',
  'Symbols provide non-string property keys and well-known symbols customize language protocols such as iteration.',
  'The iterator protocol uses next methods returning objects with value and done fields.',
  'The iterable protocol is recognized through Symbol.iterator and enables for...of, spread, and related consumers.',
  'The asynchronous iterable protocol uses Symbol.asyncIterator and is consumed by for await...of and compatible APIs.',
  'Generator functions return iterator objects and can suspend execution at yield points while retaining local state.',
  'Async generator functions combine asynchronous waiting with the generator protocol for incremental asynchronous production.',
  'Promise callbacks are scheduled asynchronously and do not execute synchronously merely because a promise is already fulfilled.',
  'Promise.all fails when any input promise rejects, while Promise.allSettled reports every outcome.',
  'Promise.any fulfills when the first input fulfills and rejects with an aggregate error when all inputs reject.'
 ],
 async:[
  'An async function always returns a promise even when its body returns an ordinary value.',
  'Awaiting a promise suspends the async function continuation until the awaited result is settled.',
  'Await does not block the JavaScript thread in the sense of a synchronous sleep; other eligible work can proceed.',
  'Promise resolution recursively assimilates thenables, which is why arbitrary objects with then methods require care.',
  'Microtasks are processed at defined points around the event loop and can starve other work if recursively generated without bounds.',
  'A long chain of microtasks can delay timers, rendering, and I/O callbacks even though each individual callback is short.',
  'Cancellation in JavaScript is commonly cooperative and AbortSignal provides a shared cancellation notification mechanism.',
  'A cancelled operation should reject or otherwise report cancellation consistently so callers do not confuse it with successful completion.',
  'Race conditions can occur even in single-threaded JavaScript when asynchronous operations complete in an unexpected order.',
  'Sequence numbers or request tokens can prevent stale asynchronous results from overwriting newer state.',
  'A mutex abstraction can serialize asynchronous critical sections even though JavaScript execution itself is single-threaded per agent.',
  'Async iterators are useful when a producer emits values whose availability is naturally asynchronous.',
  'for await...of can consume both async iterables and suitable synchronous iterables while awaiting yielded values.',
  'Unhandled promise rejection behavior is runtime-dependent but should always be treated as an application error signal.',
  'Promise-based resource acquisition should pair acquisition with cleanup in finally blocks or structured resource-management APIs where available.'
 ],
 errors:[
  'Error objects can carry a cause property so higher-level failures can preserve the underlying failure context.',
  'Custom error classes should preserve useful names and messages while keeping machine-readable properties explicit.',
  'Throwing strings loses stack and error metadata and is generally less useful than throwing Error instances.',
  'finally blocks run during control-flow completion and can replace an earlier return or thrown completion if they themselves return or throw.',
  'A retry policy should distinguish transient failures from deterministic validation errors.',
  'Exponential backoff reduces synchronized retry storms when many clients encounter the same transient failure.',
  'Jitter added to retry delays prevents clients from repeatedly retrying at exactly the same time.',
  'Circuit breakers should fail fast while a dependency is unhealthy and probe recovery during a controlled half-open state.',
  'Fallback behavior should preserve correctness and should not silently convert security or integrity failures into successful results.',
  'Error messages intended for users should be separated from diagnostic details that may expose internal implementation state.',
  'Structured errors make logging, metrics, and programmatic recovery more reliable than parsing free-form error strings.',
  'An error boundary should record enough context to reproduce a failure without storing unnecessary sensitive data.',
  'Cleanup failures should be surfaced or recorded rather than silently discarded when resource integrity matters.',
  'Timeout errors should identify which operation exceeded its deadline when multiple downstream operations can be active.',
  'Defensive code should avoid catching every exception and continuing because programming errors can otherwise become corrupted state.'
 ],
 tooling:[
  'Node package lockfiles record resolved dependency versions and integrity metadata so installs can be reproduced more reliably.',
  'Semantic version ranges express compatibility intent but do not guarantee behavioral compatibility across every release.',
  'npm scripts run project commands from package configuration and can compose tooling without a custom task runner.',
  'ESLint-style static analysis can detect suspicious patterns before runtime tests execute them.',
  'Formatters reduce stylistic variation but should not be confused with semantic validation or type checking.',
  'Source maps associate generated code locations with original sources and improve debugging of transpiled or bundled applications.',
  'Tree shaking removes statically unreachable module exports in compatible build graphs and depends on accurate side-effect assumptions.',
  'Code splitting can reduce initial download cost by deferring rarely used features until they are needed.',
  'Lazy loading should balance startup performance against interaction latency when deferred code is first requested.',
  'A build should fail when required generated artifacts are stale rather than silently shipping mismatched source and output.',
  'Environment variables should be parsed and validated at process startup rather than repeatedly interpreted inside business logic.',
  'A configuration schema should distinguish required values, defaults, allowed enumerations, and secret-bearing fields.',
  'Dependency updates should be tested for runtime and behavioral changes rather than accepted solely because installation succeeds.',
  'Reproducible builds depend on stable inputs, pinned or constrained dependencies, deterministic generation, and controlled environment effects.',
  'Development-only diagnostics should not accidentally become production data-exfiltration or debug endpoints.'
 ],
 webapi:[
  'URL and URLSearchParams provide structured URL manipulation and avoid fragile manual concatenation of query strings.',
  'URL encoding must distinguish path components, query parameters, fragments, and form-style data because their escaping rules differ.',
  'FormData represents multipart form fields and file-like values and can be passed directly to fetch in supported environments.',
  'Blob represents immutable binary-like data and can be combined or sliced without requiring an application-specific byte container.',
  'File extends Blob with metadata such as a name and modification time for browser file workflows.',
  'TextEncoder converts JavaScript strings to UTF-8 bytes and TextDecoder converts encoded bytes back to strings.',
  'CompressionStream and DecompressionStream can expose streaming compression primitives where supported by the runtime.',
  'ReadableStream locking means a stream can be actively consumed by a reader or piping operation and cannot be arbitrarily read twice.',
  'ReadableStream teeing creates branches that receive the same data, which can increase buffering requirements if consumers progress at different rates.',
  'WritableStream backpressure signals when producers should slow down before writing additional data.',
  'TransformStream connects writable and readable sides and is useful for incremental parsing or encoding transformations.',
  'Clipboard access is permission-sensitive and should provide a user-visible fallback when unavailable.',
  'File System Access APIs are permission-controlled and should not be assumed to exist in every browser.',
  'Notification and other user-facing browser APIs can require explicit permission and should degrade gracefully.',
  'The Page Visibility API can help pause nonessential work when a document becomes hidden.'
 ],
 algorithms:[
  'Binary search requires a sorted search domain and should define whether the result is any match, first match, or insertion position.',
  'A stable partition preserves relative ordering within partitions and can matter when downstream logic depends on original sequence.',
  'Top-k selection can avoid fully sorting a large collection when only the best few records are required.',
  'A heap supports incremental priority selection with logarithmic insertion and removal behavior.',
  'Breadth-first search explores graph layers and naturally finds shortest unweighted paths when edges have equal cost.',
  'Depth-first search is useful for reachability, cycle detection, and graph decomposition but does not by itself guarantee shortest paths.',
  'Dijkstra algorithms require nonnegative edge weights; negative weights require different approaches.',
  'A union-find structure efficiently tracks connected components under repeated union and find operations.',
  'Dynamic programming stores overlapping subproblem results so repeated computation is avoided.',
  'Memoization is top-down dynamic programming that caches results keyed by function inputs.',
  'Greedy algorithms require a proof or domain property showing that local choices preserve global optimality.',
  'Divide-and-conquer algorithms split a problem, solve subproblems, and combine their results; recurrence analysis estimates their complexity.',
  'Hash tables depend on good key distribution and resizing strategy to maintain expected efficient lookup.',
  'Collision handling can use chaining or probing and affects memory layout, deletion semantics, and performance.',
  'A Bloom filter provides probabilistic membership checks with false positives possible but false negatives excluded under correct use.',
  'Prefix trees represent strings by shared prefixes and are useful for autocomplete and prefix queries.',
  'Interval trees or segment trees can accelerate repeated range queries compared with scanning every interval.',
  'Reservoir sampling can select a uniform sample from a stream when the total length is not known in advance.',
  'Approximate algorithms can trade exactness for memory or latency savings when application error bounds are explicit.',
  'Algorithm selection should consider input distribution, memory constraints, mutation cost, and worst-case behavior rather than asymptotic complexity alone.'
 ],
 architecture:[
  'Dependency inversion reduces direct coupling when high-level application policies depend on stable interfaces rather than concrete infrastructure classes.',
  'Ports and adapters architecture keeps domain logic independent from databases, HTTP servers, and vendor-specific clients.',
  'A repository abstraction should model application persistence needs instead of exposing every database-specific feature by default.',
  'A service layer is useful when an operation coordinates multiple domain actions and must preserve a clear transaction boundary.',
  'DTOs define transport shapes separately from internal domain objects so API evolution does not require leaking internal representation.',
  'Validation at ingress prevents invalid data from propagating through layers where failures become harder to diagnose.',
  'Normalization and canonicalization should occur at well-defined boundaries so the same value is not interpreted differently by different components.',
  'A state machine makes legal transitions explicit and is often safer than scattered boolean flags for complex workflow state.',
  'Finite-state modeling can expose impossible combinations that ad hoc state variables would accidentally permit.',
  'Queues should expose visibility, retry, dead-letter, and ordering semantics explicitly when processing can fail.',
  'Exactly-once business effects are usually achieved through idempotency and durable state rather than assuming a transport delivers exactly once.',
  'Correlation identifiers allow logs from multiple services or asynchronous steps to be associated with one logical operation.',
  'Metrics should use bounded-cardinality labels because unbounded identifiers can overwhelm monitoring systems.',
  'Graceful degradation should define which capabilities may be unavailable and what safe reduced behavior remains.',
  'A health endpoint should distinguish process liveness from readiness to serve traffic and dependency health.'
 ],
 security:[
  'Secrets should be handled as data with restricted lifetime and scope rather than copied into logs, URLs, or arbitrary error messages.',
  'Constant-time comparison can reduce timing leakage when comparing sensitive authentication tokens in appropriate server-side contexts.',
  'Cryptographic randomness should come from a cryptographically secure random source rather than Math.random.',
  'Session identifiers should be unpredictable, scoped, rotated when appropriate, and invalidated on logout or security events.',
  'CSRF defenses depend on the authentication mechanism and request context; same-origin checks and anti-CSRF tokens are common controls.',
  'Clickjacking protections can use frame-ancestors in Content Security Policy or compatible response headers.',
  'Open redirect defenses should validate destinations against an explicit allowlist rather than accepting arbitrary user-provided URLs.',
  'SSRF defenses should restrict outbound destinations and account for DNS rebinding, redirects, private address ranges, and alternate IP representations.',
  'Rate limits should be applied at appropriate trust boundaries and should avoid allowing attackers to bypass limits through trivial identifier changes.',
  'Audit logs should record security-relevant actions with timestamps and actor context while minimizing sensitive payload capture.',
  'Authorization should be checked on the server for every protected operation; client-side hiding is not an authorization control.',
  'Security headers should be selected for the actual deployment architecture and tested so they do not create false confidence.',
  'Dependency security review should consider transitive dependencies because vulnerabilities can enter through indirect packages.',
  'Deserialization of untrusted data should validate both structure and resource usage before constructing large or complex objects.',
  'Denial-of-service defenses include request size limits, timeouts, bounded concurrency, queue limits, and expensive-operation budgets.'
 ]
};
const entries=Object.entries(packs).flatMap(([domain,items])=>items.map((text,index)=>({domain,index,text}))); 
export const LARGE_JAVASCRIPT_KNOWLEDGE_V6=entries;
export const LARGE_JAVASCRIPT_KNOWLEDGE_V6_TEXT=entries.map(x=>`[javascript-v6:${x.domain}] ${x.text}`).join('\n');
export const javascriptKnowledgeV6Stats=()=>({version:6,domains:Object.keys(packs).length,entries:entries.length,characters:LARGE_JAVASCRIPT_KNOWLEDGE_V6_TEXT.length,pretrained:false,source:'authored-pure-javascript-knowledge'});
if(typeof window!=='undefined')window.TONYLARGEJAVASCRIPTKNOWLEDGEV6={LARGE_JAVASCRIPT_KNOWLEDGE_V6,LARGE_JAVASCRIPT_KNOWLEDGE_V6_TEXT,javascriptKnowledgeV6Stats};
