// TONY Large JavaScript Knowledge Expansion v3.
// Additional pure-JavaScript corpus for local retrieval and numerical fitting.
// These are curated knowledge statements, not pretrained neural weights.

const packs={
 runtime:[
  'JavaScript execution contexts contain lexical and variable environments that determine how identifiers are resolved.',
  'The global object and global lexical bindings are related but are not identical concepts.',
  'Strict mode changes several legacy semantics, including accidental global assignment and this behavior in ordinary functions.',
  'Completion values are part of language evaluation semantics even when ordinary application code does not expose them directly.',
  'Reference records describe where a value is obtained or assigned and help explain property access and assignment semantics.',
  'ToBoolean conversion determines truthiness for conditional and logical operations.',
  'ToNumber conversion can produce NaN, infinities, signed zero, or numeric values depending on the input.',
  'ToString conversion follows defined language rules and differs from arbitrary serialization.',
  'SameValueZero is used by Set, Map key equality, and several collection operations.',
  'SameValue treats NaN as equal to itself and distinguishes positive and negative zero.',
  'Prototype inheritance is dynamic for ordinary objects, so changing a prototype can affect later property lookup.',
  'Object property lookup walks the prototype chain when an own property is absent.',
  'Deleting a configurable property changes subsequent lookup behavior but does not recursively remove referenced objects.',
  'The in operator checks inherited properties as well as own properties.',
  'Object.getOwnPropertyNames includes non-enumerable string keys while Object.getOwnPropertySymbols returns symbol keys.',
  'Reflect.ownKeys returns both string and symbol own keys including non-enumerable properties.',
  'Class syntax is primarily syntax over prototype-based object behavior rather than a separate object model.',
  'Derived class constructors must initialize this through super before using the instance.',
  'super property access uses the prototype associated with the method home object and has language-defined receiver semantics.',
  'Private fields cannot be accessed through ordinary bracket notation or reflection APIs.',
  'Static initialization blocks run during class definition evaluation and can initialize static state.',
  'Computed property names are evaluated during object or class definition and can have side effects.',
  'Object spread copies selected own enumerable properties and does not clone nested object graphs.',
  'Destructuring assignment can invoke iterators for array patterns and property accessors for object patterns.',
  'Default destructuring values apply when a selected value is undefined rather than for every falsy value.'
 ],
 arrays:[
  'Array iteration methods generally skip holes in sparse arrays, while methods such as keys can expose their positions.',
  'Array.from can consume iterables or array-like objects and can apply a mapping function during construction.',
  'Array.of avoids the special single-number constructor behavior of new Array.',
  'find and findIndex inspect elements according to their specified traversal semantics and can return an explicit not-found result.',
  'findLast and findLastIndex traverse from the end in environments that implement them.',
  'includes uses SameValueZero and therefore can locate NaN values in arrays.',
  'indexOf uses strict equality and therefore does not locate NaN.',
  'copyWithin copies a range inside an array without changing its length.',
  'fill writes one value across a selected array range and mutates the receiver.',
  'splice can insert, remove, and replace elements and mutates the receiver.',
  'toSpliced returns a modified copy instead of mutating the original array in supporting runtimes.',
  'toSorted returns a sorted copy while sort mutates its receiver.',
  'toReversed returns a reversed copy while reverse mutates its receiver.',
  'Array.prototype.with returns a copy with one indexed value replaced.',
  'flat removes nesting levels and can flatten arrays containing sparse positions according to its defined semantics.',
  'reduce should have an explicit initial accumulator when empty input is a valid possibility.',
  'reduceRight processes elements from the end and can be useful for right-associated transformations.',
  'every on an empty array returns true because no element violates the predicate.',
  'some on an empty array returns false because no element satisfies the predicate.',
  'map preserves array length and produces holes corresponding to skipped holes in sparse input.',
  'filter creates a dense result containing values whose predicate returns truthy.',
  'sort comparators should return negative, zero, or positive ordering signals rather than boolean values.',
  'Stable sorting allows equal-key records to retain their relative order in modern JavaScript implementations.'
 ],
 strings:[
  'JavaScript strings are sequences of UTF-16 code units rather than guaranteed sequences of user-perceived characters.',
  'String.prototype.normalize can convert Unicode text to a selected normalization form.',
  'Locale-sensitive comparison can use Intl.Collator rather than simple code-unit ordering.',
  'String.prototype.localeCompare can produce locale-dependent ordering and should not be treated as a stable numeric ordering across locales.',
  'startsWith, endsWith, and includes operate on string sequences without interpreting them as regular expressions.',
  'replace replaces a selected match while replaceAll targets all matching occurrences under its specified rules.',
  'String replacement callbacks receive match text and capture information according to the regular expression or string form.',
  'RegExp named capture groups make complex patterns easier to consume than positional capture indexes.',
  'The d regular-expression flag can expose match indices in supporting runtimes.',
  'The u regular-expression flag enables Unicode-aware code point handling for many regular expression operations.',
  'The v regular-expression flag extends Unicode set and string-property capabilities in supporting modern runtimes.',
  'RegExp lastIndex is observable for global and sticky regular expressions and can create stateful matching behavior.',
  'String.raw creates a string from raw template literal segments without interpreting escape sequences in the usual way.',
  'padStart and padEnd are presentation helpers and do not change the underlying character encoding.',
  'trim removes defined whitespace from both ends while trimStart and trimEnd operate on one side.',
  'split with a regular expression can produce captures in the returned array when the pattern contains capturing groups.',
  'matchAll returns an iterator over all regular-expression matches and preserves capture information.',
  'Unicode escape sequences can represent code points while ordinary string indexing still exposes UTF-16 units.',
  'Grapheme segmentation can use Intl.Segmenter when available to approximate user-perceived character boundaries.',
  'TextEncoder encodes strings as UTF-8 bytes and TextDecoder decodes byte sequences into strings.'
 ],
 collections:[
  'Map is appropriate when keys are arbitrary values and insertion order matters.',
  'Set is appropriate when membership uniqueness is the primary collection requirement.',
  'Map and Set constructors accept iterables of entries or values respectively.',
  'WeakMap is useful for metadata associated with objects without extending their lifetime.',
  'WeakSet can mark object membership without creating a strong reference to those objects.',
  'WeakMap and WeakSet are intentionally not directly enumerable.',
  'Map.prototype.get returns undefined for a missing key, so applications may need has when undefined is a valid stored value.',
  'Set.prototype.has tests membership without exposing an index because Set is not an indexed sequence.',
  'Map iteration yields entries in insertion order and keys and values provide corresponding iterators.',
  'Collection conversion should consider whether duplicate values, object identity, or insertion order have semantic meaning.',
  'A null-prototype object is useful for dictionary-like storage when inherited properties should not participate.',
  'Using Object.create(null) removes ordinary Object prototype methods from the resulting dictionary.',
  'Records intended as maps should validate keys when untrusted data can influence property names.',
  'JSON objects cannot preserve Map, Set, functions, or cyclic references without an explicit serialization strategy.',
  'Structured cloning can preserve several built-in collections that JSON cannot represent directly.'
 ],
 errors:[
  'Error is the base constructor for ordinary JavaScript errors and can carry a message and stack information.',
  'TypeError indicates an operation was applied to an incompatible value or type according to runtime semantics.',
  'RangeError indicates a value is outside an allowed range for an operation.',
  'ReferenceError indicates an invalid identifier reference or related binding lookup failure.',
  'SyntaxError indicates source text or dynamically parsed syntax is invalid.',
  'AggregateError groups multiple errors and is used by operations such as Promise.any when all inputs reject.',
  'Error cause can preserve the underlying failure when wrapping an error with additional context.',
  'Custom error classes should preserve useful stack and cause information when translating lower-level failures.',
  'Throwing strings loses structured error information and is generally less useful than throwing Error objects.',
  'A catch block should preserve enough context to distinguish expected failures from programmer defects.',
  'Finally blocks are appropriate for cleanup that must happen regardless of success or failure.',
  'Returning an error object instead of throwing can be appropriate in explicitly designed result-oriented APIs, but the contract should be consistent.',
  'Error messages should be actionable and should avoid exposing secrets or sensitive internal data.',
  'Retry logic should not blindly retry syntax, validation, authorization, or deterministic programming errors.',
  'Network errors can be transient, but a successful HTTP response with an error status is still a response that callers must inspect.'
 ],
 modules2:[
  'ES module bindings are live bindings, so an imported binding can reflect later changes in the exporting module.',
  'Import declarations are hoisted as part of module linking even though module execution occurs later.',
  'Module namespace objects expose exported bindings through a specialized namespace representation.',
  'Dynamic import can be used inside ordinary functions and is useful for conditional or deferred module loading.',
  'Import attributes can provide host-specific metadata for selected module types in supporting runtimes.',
  'JSON modules, when supported by a host, follow host-defined import and assertion or attribute rules.',
  'CommonJS exports can be a function, object, primitive, or other value because module.exports is the exported value.',
  'Assigning exports to a new object differs from mutating properties on the existing exports alias.',
  'Node package exports can define conditional entry points and prevent accidental access to internal package files.',
  'The package type field affects how .js files are interpreted within a package scope in Node.',
  'File extensions can explicitly select CommonJS or ES module interpretation in Node package contexts.',
  'Package self-references can use the package name when exports configuration permits them.',
  'Dependency graphs should avoid unnecessary cycles because initialization order can become difficult to reason about.',
  'A module should avoid importing large optional subsystems when a lightweight path can perform the requested operation.',
  'Lazy module loading can improve startup but may move failures from initialization time to first use.'
 ],
 node2:[
  'The Node event loop includes multiple phases and host-specific scheduling behavior beyond the browser task model.',
  'process.nextTick callbacks have special scheduling behavior and excessive use can starve other event-loop work.',
  'setImmediate is a Node scheduling primitive whose ordering relative to timers depends on the surrounding event-loop state.',
  'Readable streams can be consumed with for await...of for natural asynchronous iteration.',
  'Writable stream backpressure is communicated by write returning false until a drain event occurs.',
  'AbortSignal can coordinate cancellation across HTTP, filesystem, timers, and custom application operations.',
  'Node fetch is based on web platform interfaces but its runtime behavior and implementation details can differ from browsers.',
  'HTTP request headers should be validated and normalized according to the protocol rather than trusted as arbitrary application data.',
  'A server should reject unsupported methods explicitly instead of accidentally treating every request as a successful operation.',
  'Content-Length can be used as an early request-size signal but should not be the only protection when transfer encoding can stream data.',
  'Streaming request bodies reduce memory pressure for large uploads when downstream processing can also stream.',
  'Node buffers are byte-oriented and should not be confused with arrays of Unicode characters.',
  'Buffer.subarray creates a view over existing memory while copying methods allocate independent storage.',
  'File permissions should follow least privilege and should not be made world-writable without a deliberate reason.',
  'Temporary directories should be created through secure APIs rather than predictable names.',
  'Recursive filesystem operations can amplify cost dramatically and should be bounded for untrusted paths.',
  'Worker threads are useful for CPU-bound JavaScript that would otherwise block the main event loop.',
  'SharedArrayBuffer plus Atomics can coordinate workers but introduces synchronization complexity and should be justified.',
  'A worker pool should bound task count and expose failure and shutdown behavior.',
  'Process memory limits and heap pressure should be considered when building large in-memory caches.'
 ],
 browser2:[
  'DOM mutations can trigger style calculation, layout, paint, or compositing work depending on what changed.',
  'Reading layout information after writing layout-affecting properties can create forced synchronous layout in some circumstances.',
  'Batching DOM writes and reads reduces layout thrashing in performance-sensitive interfaces.',
  'DocumentFragment is useful for assembling multiple nodes before inserting them into a live document.',
  'Element.classList manipulates individual class tokens and avoids common string-concatenation mistakes.',
  'dataset exposes data-* attributes through a convenient string-based interface.',
  'HTMLElement.focus can move keyboard focus and should be used with a deliberate accessibility strategy.',
  'A modal dialog should manage focus, keyboard dismissal, and restoration of focus to the invoking control.',
  'ARIA attributes supplement semantic HTML and should not replace native controls when native semantics are available.',
  'Buttons are generally preferable to clickable generic elements for keyboard and assistive-technology interaction.',
  'Pointer capture lets an element continue receiving pointer events after the pointer moves outside its bounds.',
  'Event bubbling enables delegation while stopPropagation can prevent ancestors from observing an event.',
  'preventDefault cancels a cancelable default browser action but does not automatically stop propagation.',
  'Custom elements should clean up listeners and observers when disconnected to avoid retaining stale resources.',
  'Shadow DOM style encapsulation does not prevent JavaScript from communicating with component internals when references are available.',
  'IntersectionObserver callbacks may batch several entries and should process the set rather than assume one callback equals one event.',
  'ResizeObserver callbacks can create feedback loops if size changes trigger more size changes without a stable condition.',
  'Page visibility can be used to reduce polling, animation, or optional computation when a tab is hidden.',
  'The browser main thread should keep synchronous work short to preserve input responsiveness.',
  'requestAnimationFrame is appropriate for frame-based animation while setTimeout is better for general delayed work.'
 ],
 network:[
  'HTTP methods have different conventional semantics; GET is generally safe and idempotent while POST commonly represents a state-changing submission.',
  'PUT is commonly used for idempotent replacement semantics while PATCH represents partial modification.',
  'DELETE is conventionally idempotent at the intended resource level even though repeated calls can produce different status codes.',
  'HTTP status codes communicate broad outcome classes and applications often need structured response bodies for detailed errors.',
  'A client should distinguish transport failure, protocol failure, HTTP failure, and application-level failure.',
  'DNS names can resolve to multiple addresses and clients may need connection retry or selection behavior.',
  'TLS authenticates and encrypts connections when certificate validation succeeds; application authorization is a separate concern.',
  'HTTPS does not make arbitrary application input trusted.',
  'Connection timeouts, response timeouts, and overall operation deadlines represent different failure boundaries.',
  'Retries should use bounded attempts and backoff to avoid multiplying load during outages.',
  'Idempotency keys can make retryable state-changing operations safer when the server implements them.',
  'ETag enables conditional retrieval and can also support optimistic concurrency with If-Match.',
  'Last-Modified and If-Modified-Since provide time-based conditional request mechanisms.',
  'HTTP caching behavior depends on response headers and request context rather than simply whether a URL looks static.',
  'Compression reduces transfer size but adds CPU work and should be considered alongside latency and resource constraints.',
  'Streaming responses let clients process data before the complete body has arrived.',
  'WebSocket protocols require explicit application-level heartbeat, reconnect, and authorization strategies.',
  'Server-Sent Events are text-based event streams and are naturally suited to one-way updates.',
  'WebTransport and newer transport APIs can offer different reliability and multiplexing characteristics where supported.',
  'Cross-origin browser requests are constrained by the same-origin policy and server-controlled CORS rules.'
 ],
 testing2:[
  'A unit test should isolate a small behavioral contract and fail for a meaningful regression.',
  'Integration tests validate interactions between components and can reveal contract mismatches that unit tests miss.',
  'End-to-end tests exercise realistic user flows and should focus on high-value paths because they cost more to maintain.',
  'Mocks can verify interactions but excessive mocking can hide integration failures and couple tests to implementation details.',
  'Fakes provide simplified functional collaborators and can be more robust than call-count assertions.',
  'Snapshots are useful for stable serialized output but can become noise when large outputs change frequently.',
  'Golden tests compare output against a reviewed reference and are useful for compilers, formatters, and generators.',
  'Test isolation requires controlling shared state such as environment variables, global registries, clocks, and temporary files.',
  'Randomized tests should preserve a seed and input when a failure occurs so it can be reproduced.',
  'Property-based testing checks general invariants across many generated inputs instead of a few hand-selected cases.',
  'Mutation testing evaluates whether tests detect realistic small changes in implementation.',
  'Coverage is evidence about exercised code, not proof that the behavior is correct.',
  'A test that never fails when its target behavior breaks is not providing useful protection even if coverage is high.',
  'Flaky tests should be diagnosed for timing, shared state, nondeterminism, network dependence, and environment sensitivity.',
  'Performance tests should control warm-up, input size, runtime noise, and measurement methodology.',
  'Benchmark results should report enough context to distinguish algorithmic changes from machine or runtime variation.'
 ],
 architecture2:[
  'Separate exact deterministic operations from approximate retrieval or generation so arithmetic and validation do not depend on language-model guesses.',
  'A router can select specialized local algorithms based on detected intent and available evidence.',
  'Retrieval-augmented generation can inject relevant corpus statements without encoding every fact into a numerical representation.',
  'Hybrid ranking can combine lexical overlap, semantic-like feature scores, and domain-specific priorities.',
  'Caching should distinguish successful values, negative results, and failures when those states have different lifetimes.',
  'A bounded cache needs an eviction policy such as LRU, LFU, time-based expiration, or a domain-specific rule.',
  'Queues provide admission control and make bursts visible through queue depth and processing latency.',
  'Worker pools should avoid unbounded concurrency because CPU, memory, file descriptors, and network capacity are finite.',
  'Circuit breakers can stop repeatedly calling a failing dependency and allow controlled recovery probes.',
  'Bulkheads isolate resource pools so one workload cannot consume all capacity needed by another.',
  'Timeouts define a maximum useful waiting period and should be propagated to nested operations when possible.',
  'Observability should correlate logs, metrics, and traces using stable operation identifiers.',
  'A health endpoint should report whether the process is alive while readiness should indicate whether it can serve useful work.',
  'Configuration should be validated at startup when invalid values would make the service unsafe or unusable.',
  'Feature flags should have explicit defaults, ownership, cleanup plans, and safe failure behavior.'
 ],
 data2:[
  'A schema describes the expected shape and constraints of data while validation enforces those expectations at runtime.',
  'Optional and nullable fields represent different states and should not be conflated.',
  'Numeric validation should account for NaN, infinity, integer constraints, and safe integer range where relevant.',
  'Number.isSafeInteger identifies integers that can be represented exactly within JavaScript Number integer semantics.',
  'Decimal quantities may require a decimal library or scaled integer representation when binary floating point is insufficient.',
  'Dates should be represented with explicit time-zone assumptions because local time and absolute instants are different concepts.',
  'ISO 8601 strings are useful interchange representations but applications should validate the exact forms they accept.',
  'Pagination cursors should encode stable traversal state rather than rely only on mutable page numbers for changing datasets.',
  'Joins require explicit key semantics and should define behavior for missing matches and duplicate keys.',
  'A pivot transformation can expand distinct values into columns and should guard against uncontrolled cardinality.',
  'Streaming parsers reduce memory pressure for large files and can provide incremental error reporting.',
  'CSV data is not self-describing and requires defined delimiter, quoting, escape, encoding, and header rules.',
  'JSON numbers do not preserve arbitrary decimal precision and should not be assumed to represent every numeric domain exactly.',
  'Schema evolution should define compatibility rules for added, removed, renamed, and changed fields.',
  'Data normalization can reduce duplication but may increase query complexity; the correct tradeoff depends on access patterns.'
 ],
 performance:[
  'Algorithmic complexity often matters more than micro-optimizing individual JavaScript expressions.',
  'Repeated linear scans can become expensive when a lookup structure such as Map or Set would provide faster access.',
  'Allocating many short-lived objects can increase garbage-collection pressure in hot paths.',
  'String concatenation strategy should be chosen based on actual workload rather than assumptions about a single universal performance rule.',
  'Typed arrays can reduce memory overhead and provide predictable numeric storage for large homogeneous datasets.',
  'Web Workers can preserve UI responsiveness by moving CPU-heavy work away from the main thread.',
  'WebGPU can accelerate highly parallel numeric kernels but requires data transfer and pipeline setup overhead.',
  'Memoization can improve repeated computations but can increase memory usage and retain inputs or outputs longer than intended.',
  'Lazy iteration can avoid allocating complete intermediate collections when only part of a sequence is needed.',
  'Generators can express lazy pipelines but introduce iterator protocol overhead compared with simple loops in some hot paths.',
  'Profilers should identify actual bottlenecks before optimization work begins.',
  'Benchmark harnesses should warm up JIT-compiled code and avoid measuring setup work unless setup cost is the target.',
  'Premature optimization can make code harder to maintain without measurable user benefit.',
  'Performance regressions should be protected with representative benchmarks when the workload is stable enough to measure.',
  'Memory retention is often more important than allocation count when diagnosing long-running application growth.'
 ],
 security2:[
  'Input validation should happen at trust boundaries and should not rely solely on UI constraints.',
  'Output encoding should match the context in which untrusted data will be interpreted.',
  'HTML, URL, JavaScript, CSS, SQL, and shell contexts have different injection risks and require different defenses.',
  'Using textContent rather than innerHTML avoids HTML interpretation for plain text insertion.',
  'Trusted Types can constrain dangerous DOM injection sinks in supporting browsers and applications.',
  'Content Security Policy can reduce the impact of script injection when configured to match the application architecture.',
  'Cross-site scripting defenses should include safe templating, output encoding, and careful DOM API selection.',
  'Prototype pollution defenses should reject dangerous object keys where untrusted structures are merged.',
  'Object.hasOwn or null-prototype dictionaries can avoid accidental reliance on inherited properties.',
  'Path traversal defenses should canonicalize and constrain resolved paths to an intended root directory.',
  'File uploads should validate size, type, storage location, and generated names rather than trusting the client filename.',
  'Authentication proves identity while authorization determines what an authenticated identity may do.',
  'Session identifiers should be unpredictable and protected according to their storage and transport model.',
  'Secrets should not be embedded in browser-delivered JavaScript because users can inspect the client.',
  'Logs should avoid credentials, tokens, payment data, and other secrets even when debugging failures.',
  'Cryptographic operations should use established platform primitives and avoid hand-written cryptography.',
  'Randomness for security-sensitive identifiers should come from a cryptographically secure source.',
  'Rate limiting can reduce abuse but should be combined with correct authentication and authorization controls.',
  'Dependency security includes auditing transitive packages and reviewing changes rather than assuming a package name is trustworthy.',
  'Supply-chain integrity can be improved with lockfiles, provenance, review, and controlled build environments.'
 ]
};

export const LARGE_JAVASCRIPT_KNOWLEDGE_V3=Object.entries(packs).flatMap(([domain,items])=>items.map((text,index)=>({id:`js-v3-${domain}-${index+1}`,domain,text}))); 
export const LARGE_JAVASCRIPT_KNOWLEDGE_V3_TEXT=LARGE_JAVASCRIPT_KNOWLEDGE_V3.map(x=>x.text).join('\n');
export const javascriptKnowledgeV3Stats=()=>({version:'3',domains:Object.keys(packs).length,entries:LARGE_JAVASCRIPT_KNOWLEDGE_V3.length,characters:LARGE_JAVASCRIPT_KNOWLEDGE_V3_TEXT.length});
if(typeof window!=='undefined')window.TONYLARGEJAVASCRIPTKNOWLEDGEV3={LARGE_JAVASCRIPT_KNOWLEDGE_V3,LARGE_JAVASCRIPT_KNOWLEDGE_V3_TEXT,javascriptKnowledgeV3Stats};
