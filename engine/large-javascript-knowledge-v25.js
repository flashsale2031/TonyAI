// TONY Large JavaScript Knowledge v25.
// Authored deterministic knowledge expansion; not a pretrained neural checkpoint.
const packs={
language:[['optional chaining','Optional chaining short-circuits nullish property, element, and call access.'],['nullish coalescing','?? falls back only for null or undefined, preserving valid falsy values such as 0 and false.'],['logical assignment','||=, &&=, and ??= assign conditionally according to truthiness, falsiness, or nullishness.'],['private fields','# class fields are enforced private slots rather than ordinary object properties.'],['static blocks','Static class blocks provide one-time class initialization with lexical access to class state.'],['computed properties','Bracketed property names evaluate expressions to create dynamic object or class keys.'],['destructuring defaults','Destructuring defaults apply when a matched value is undefined.'],['rest parameters','A rest parameter collects remaining call arguments into an array and must be last.'],['spread semantics','Iterable spread consumes an iterator; object spread copies enumerable own properties.'],['prototype lookup','Property lookup follows the receiver and its prototype chain until found or null.']],
async:[['promise combinators','all rejects early, allSettled waits for all, race settles first, and any fulfills first.'],['promise finally','finally runs after settlement and normally preserves the original result or reason.'],['async iteration','for await...of consumes async iterables and can await values from synchronous iterables too.'],['abort controller','AbortController exposes AbortSignal for cooperative cancellation in APIs such as fetch.'],['microtask ordering','Promise reactions and queueMicrotask run after the current job before the next task.'],['async generator','Async generators combine await and yield and expose promise-returning AsyncIterator next calls.'],['concurrency limits','Concurrency limiters must release slots on success and failure and define cancellation behavior.'],['backpressure','Producers should coordinate with consumer demand or bounded queues to avoid unbounded buffering.'],['timeout cancellation','A timeout should cancel underlying work when the operation supports cancellation.'],['promise normalization','Promise.resolve assimilates thenables and wraps ordinary values into fulfilled promises.']],
node:[['event loop phases','Node processes timers, pending callbacks, poll, check, and close phases while microtasks run between turns.'],['streams','Node readable, writable, duplex, and transform streams model incremental data and backpressure.'],['pipeline','stream.pipeline centralizes stream completion and error propagation.'],['buffer','Buffer represents raw bytes and should be distinguished from encoded strings.'],['worker threads','worker_threads can move CPU-heavy JavaScript work away from the main event loop.'],['child processes','child_process boundaries require careful argument, environment, exit-status, and stderr handling.'],['process signals','SIGTERM can initiate graceful shutdown by stopping new work and finishing or cancelling active work.'],['environment variables','process.env values are strings and should be parsed and validated at the configuration boundary.'],['module resolution','ES module and CommonJS resolution follow distinct package and module-graph rules.'],['diagnostic channels','diagnostics_channel provides named instrumentation points for lightweight application observability.']],
browser:[['fetch bodies','Fetch response bodies are streams and json, text, and arrayBuffer consume them asynchronously.'],['web streams','ReadableStream, WritableStream, and TransformStream provide incremental browser pipelines with backpressure.'],['intersection observer','IntersectionObserver reports target visibility changes without continuous scroll polling.'],['resize observer','ResizeObserver reports element size changes without repeatedly polling layout.'],['mutation observer','MutationObserver batches DOM mutation notifications and invokes callbacks asynchronously.'],['broadcast channel','BroadcastChannel connects same-origin browsing contexts through named message channels.'],['structured clone','structuredClone creates deep copies for supported structured data types.'],['offscreen canvas','OffscreenCanvas can move supported canvas rendering into workers.'],['page visibility','visibilitychange lets applications reduce background work when documents become hidden.'],['online status','navigator.onLine is only a connectivity hint, not proof that a specific server is reachable.']],
security:[['trusted types','Trusted Types can constrain sensitive DOM sinks to approved typed values and reduce DOM XSS risk.'],['subresource integrity','SRI lets resources declare an expected cryptographic digest.'],['content security policy','CSP restricts resource and execution sources and can reduce injection exploitability.'],['permissions policy','Permissions Policy controls selected browser capabilities available to documents and frames.'],['referrer policy','Referrer-Policy controls how much referring URL information is transmitted.'],['secure cookies','Secure limits cookies to HTTPS while HttpOnly prevents document.cookie access.'],['same origin','The same-origin policy isolates documents and resources across origins unless browser mechanisms allow access.'],['cors','CORS controls browser read access to cross-origin responses and is not server-side authentication.'],['csrf defense','State-changing requests need suitable CSRF defenses such as same-site cookies, tokens, or origin checks.'],['output encoding','Untrusted output requires context-aware encoding for HTML, attributes, URLs, CSS, and script contexts.']],
performance:[['layout thrashing','Alternating DOM reads and writes can force repeated layout; batch reads and writes.'],['request animation frame','requestAnimationFrame aligns visual updates with browser rendering.'],['idle callbacks','requestIdleCallback schedules lower-priority work but should not be treated as a deadline guarantee.'],['long tasks','Splitting long main-thread tasks or moving CPU work to workers improves responsiveness.'],['memory retention','Closures, listeners, caches, and detached DOM references can retain objects and grow memory use.'],['memoization','Memoization trades memory for repeated computation savings when results are deterministic.'],['debouncing','Debouncing waits for activity to settle before running work.'],['throttling','Throttling bounds how often work executes during a burst of events.'],['code splitting','Dynamic import can defer code download and execution until functionality is needed.'],['preconnect','preconnect can reduce connection setup latency for likely-to-be-used origins.']],
architecture:[['dependency inversion','High-level logic should depend on stable abstractions rather than concrete infrastructure.'],['hexagonal architecture','Ports and adapters isolate domain logic from HTTP, databases, queues, and files.'],['idempotency','Idempotent operations can be repeated without changing the intended final state.'],['circuit breaker','Circuit breakers stop repeated calls to failing dependencies and probe recovery later.'],['bulkhead isolation','Bulkheads partition resources so one workload cannot exhaust all capacity.'],['retry policy','Retries should be bounded, use backoff and jitter, and avoid unsafe retries of permanent failures.'],['cache invalidation','Caches need explicit freshness or invalidation rules to preserve correctness.'],['event sourcing','Event sourcing records state-changing events from which state can be reconstructed.'],['saga workflow','Sagas coordinate distributed work with local transactions and compensating actions.'],['observability','Logs, metrics, traces, and correlation IDs connect symptoms to requests and dependencies.']],
testing:[['property testing','Property-based testing generates many inputs and verifies general invariants.'],['fuzz testing','Fuzzing explores malformed and boundary-heavy inputs to expose crashes and parser defects.'],['contract testing','Contract tests verify agreement between independently deployed components.'],['mutation testing','Mutation testing checks whether tests detect intentional implementation changes.'],['deterministic tests','Controlling clocks, randomness, network dependencies, and concurrency improves reproducibility.'],['test isolation','Tests should minimize shared state and clean up resources they own.'],['fake timers','Fake timers make time-based tests deterministic while real scheduling should still be tested where relevant.'],['snapshot caution','Broad snapshots can hide semantic changes; use snapshots where output structure matters.'],['error path coverage','Tests should cover rejection, timeout, cancellation, malformed input, partial failure, and cleanup.'],['load testing','Load tests need defined workloads, concurrency, latency objectives, limits, and error budgets.']],
data:[['json limitations','JSON does not directly represent undefined, functions, symbols, cycles, or many richer built-in types.'],['csv parsing','CSV parsing must handle quoting, escaped quotes, embedded commas, and line breaks.'],['stable sorting','Modern JavaScript Array.sort is stable for elements considered equal by the comparator.'],['map versus object','Map provides arbitrary keys and collection operations; objects suit many fixed record structures.'],['set semantics','Set uses SameValueZero equality and therefore treats NaN as equal to itself.'],['typed arrays','Typed arrays expose fixed-element binary views over ArrayBuffer storage.'],['arraybuffer transfer','Transferable ArrayBuffers can move ownership between workers without copying their bytes.'],['dataview','DataView provides explicit byte-offset access across integer and floating-point representations.'],['numeric precision','Number uses IEEE 754 double precision; unsafe large integers require BigInt or another exact representation.'],['bigint mixing','BigInt and Number are distinct numeric types and generally require explicit conversion before arithmetic.']],
web:[['etag caching','ETag identifies a representation version and enables conditional requests with If-None-Match.'],['range requests','HTTP Range requests retrieve selected byte ranges for supported resumable or partial transfers.'],['problem details','application/problem+json provides a structured representation for HTTP API errors.'],['content encoding','Content-Encoding describes transformations such as gzip or br and is distinct from media type.'],['cache control','Cache-Control defines freshness, revalidation, and caching constraints for HTTP responses.'],['sse','Server-Sent Events provide one-way HTTP streaming with text/event-stream framing.'],['websocket lifecycle','WebSockets should handle open, message, error, and close states and define heartbeat policy when needed.'],['webhook verification','Webhook receivers should verify signatures over the exact payload and defend against replay where supported.'],['http semantics','HTTP methods and status codes communicate operation semantics and failure classes.'],['quic http3','HTTP/3 uses QUIC for multiplexed transport behavior designed to reduce some TCP head-of-line effects.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V25=Object.entries(packs).flatMap(([domain,entries])=>entries.map(([topic,text])=>({domain,topic,text:`${topic}: ${text}`})));
export const LARGE_JAVASCRIPT_KNOWLEDGE_V25_TEXT=LARGE_JAVASCRIPT_KNOWLEDGE_V25.map(x=>`${x.domain} | ${x.topic} | ${x.text}`).join('\n');
export const javascriptKnowledgeV25Stats={version:25,entries:LARGE_JAVASCRIPT_KNOWLEDGE_V25.length,domains:Object.keys(packs).length,pretrained:false,authored:true};
if(typeof window!=='undefined')window.TONYLARGEJAVASCRIPTKNOWLEDGEV25={LARGE_JAVASCRIPT_KNOWLEDGE_V25,LARGE_JAVASCRIPT_KNOWLEDGE_V25_TEXT,javascriptKnowledgeV25Stats};
