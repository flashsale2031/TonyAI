// TONY Large JavaScript Knowledge Expansion v23.
// Authored JavaScript/web engineering knowledge; not a pretrained neural checkpoint.
const packs={
 language:[['array with','Use Array.isArray before assuming an iterable is an array.'],['object prototype','Object.getPrototypeOf reveals the immediate prototype and Object.create can construct explicit prototype chains.'],['property descriptors','Property descriptors control writable, enumerable, and configurable behavior independently of a property value.'],['define property','Object.defineProperty can create precise property attributes when ordinary assignment is too permissive.'],['proxy invariants','Proxy traps must respect target invariants or the runtime can throw a TypeError.'],['reflect api','Reflect methods provide predictable object-operation primitives and are useful inside proxy traps.'],['map keys','Map uses SameValueZero key equality and can use objects as keys without string coercion.'],['set uniqueness','Set stores unique values using SameValueZero equality.'],['weak collections','WeakMap and WeakSet hold object references weakly and are useful for metadata tied to object lifetime.'],['iterator protocol','An iterator exposes next returning an object with value and done fields.']],
 async:[['promise state','A promise settles once; later resolve or reject attempts have no effect on its state.'],['finally cleanup','Promise.prototype.finally is appropriate for cleanup that should run after fulfillment or rejection.'],['async generator','Async generators combine await with yield and produce async iterators.'],['for await','for await...of consumes async iterables and awaits each yielded result.'],['abort signal','AbortSignal lets asynchronous operations observe cancellation without inventing a second promise state.'],['timeout signal','AbortSignal.timeout provides a standard way to represent time-based cancellation in supported runtimes.'],['task ordering','Promise reactions run as microtasks before the event loop advances to later task phases.'],['unhandled rejection','Unhandled promise rejections should be treated as observable failures rather than silently ignored.'],['concurrency limit','A worker pool or semaphore prevents unbounded promise fan-out from exhausting resources.'],['retry policy','Retries should use bounded attempts, backoff, and cancellation rather than recursive immediate retries.']],
 node:[['streams','Node streams model data incrementally and reduce the need to buffer entire payloads in memory.'],['pipeline','stream.pipeline coordinates stream completion and propagates errors across a pipeline.'],['web streams','Web Streams APIs provide readable, writable, and transform abstractions that can interoperate with modern Node code.'],['worker threads','worker_threads isolate CPU-heavy JavaScript from the main event-loop thread.'],['message ports','MessagePort provides structured-clone based communication between execution contexts.'],['child process','child_process is appropriate when an operation needs a separate OS process rather than a JavaScript worker.'],['diagnostics channel','diagnostics_channel lets libraries publish structured diagnostic events without hard-coding a logging consumer.'],['async local storage','AsyncLocalStorage propagates contextual state across asynchronous call chains.'],['module cache','ES module and CommonJS caching behavior should be understood before relying on singleton initialization.'],['graceful shutdown','A Node service should stop accepting work, drain active operations, and then close resources during shutdown.']],
 browser:[['event propagation','DOM events travel through capture, target, and bubble phases unless propagation is stopped.'],['event delegation','Delegation attaches fewer listeners by handling events from a stable ancestor.'],['intersection observer','IntersectionObserver observes visibility changes without repeatedly polling layout.'],['resize observer','ResizeObserver reacts to element size changes and is useful for component layout logic.'],['mutation observer','MutationObserver observes DOM tree changes asynchronously.'],['shadow dom','Shadow DOM encapsulates markup and styles behind a component boundary.'],['custom elements','Custom elements define reusable HTML elements with lifecycle callbacks.'],['history api','history.pushState and replaceState change URL state without a full document navigation.'],['broadcast channel','BroadcastChannel provides same-origin messaging between browsing contexts.'],['offscreen canvas','OffscreenCanvas can move canvas rendering work away from the main UI thread when supported.']],
 security:[['trusted types','Trusted Types can reduce DOM XSS risk by constraining dangerous HTML and script sinks.'],['csp nonce','A CSP nonce authorizes specific inline scripts without broadly allowing arbitrary inline script execution.'],['subresource integrity','SRI lets browsers verify fetched resources against expected cryptographic hashes.'],['same site cookie','SameSite cookie settings help limit cross-site request attachment.'],['secure cookie','Secure cookies are sent only over secure transport.'],['http only cookie','HttpOnly prevents client-side JavaScript from reading a cookie through document.cookie.'],['origin policy','Same-origin rules constrain script access across origins unless an explicit browser mechanism permits sharing.'],['web crypto','Web Crypto APIs provide browser cryptographic primitives but do not automatically design a secure protocol.'],['permissions policy','Permissions Policy can restrict selected browser capabilities for a document or embedded frame.'],['referrer policy','Referrer-Policy controls how much referrer information is sent with navigations and requests.']],
 performance:[['performance observer','PerformanceObserver consumes performance timeline entries without requiring manual polling.'],['long task','Long tasks on the main thread can delay input and rendering and should be measured before optimization.'],['layout thrash','Repeated interleaving of layout reads and writes can force unnecessary synchronous layout work.'],['memoization','Memoization trades memory for repeated computation when inputs are stable and cache invalidation is manageable.'],['typed arrays','Typed arrays provide compact binary numeric storage and predictable element representations.'],['structured clone','structuredClone copies supported data graphs without preserving arbitrary prototype identity.'],['batching','Batching reduces fixed per-operation overhead when many small operations can be processed together.'],['lazy loading','Lazy loading defers work until it is likely to be needed, reducing startup cost.'],['code splitting','Code splitting separates bundles so rarely used features do not block initial loading.'],['cache invalidation','Caches need explicit freshness and invalidation rules because stale data is a correctness problem.']],
 architecture:[['dependency inversion','High-level policy should depend on stable abstractions rather than concrete infrastructure details.'],['idempotency','Idempotent operations can be retried without multiplying their intended externally visible effect.'],['circuit breaker','A circuit breaker stops repeatedly failing downstream calls and allows controlled recovery probes.'],['bulkhead isolation','Bulkheads partition resources so one overloaded subsystem does not consume every available worker.'],['state machine','Explicit states and transitions make lifecycle-heavy systems easier to reason about and test.'],['pure function','A pure function has no observable side effects and returns the same output for the same inputs.'],['command query separation','Separating state-changing commands from read-only queries can simplify reasoning about service behavior.'],['feature flags','Feature flags decouple deployment from activation but require ownership and cleanup to avoid permanent complexity.'],['backpressure','Backpressure communicates downstream capacity limits so producers do not overwhelm consumers.'],['failure domains','Architectures should identify failure domains and prevent one fault from cascading across unrelated components.']],
 testing:[['property based test','Property-based tests generate many inputs to check general invariants instead of only fixed examples.'],['fuzz testing','Fuzzing explores malformed or unusual inputs and is useful for parser and boundary testing.'],['contract test','Contract tests verify that a producer and consumer agree on an interface.'],['deterministic test','Deterministic tests avoid uncontrolled clocks, randomness, and external state when those are not the subject under test.'],['integration test','Integration tests exercise multiple components together and expose boundary assumptions.'],['snapshot test','Snapshot tests compare structured output against a stored representation and require deliberate review when snapshots change.'],['flaky test','A flaky test fails nondeterministically and should be isolated and corrected rather than normalized.'],['load test','Load tests measure behavior under representative concurrency and request volume.'],['regression test','A regression test preserves coverage for a previously discovered defect.'],['mutation testing','Mutation testing checks whether the test suite detects small changes that should break behavior.']],
 data:[['json limits','JSON cannot directly represent values such as undefined, functions, or arbitrary object identity.'],['csv parsing','CSV parsing should handle quoting, escaped delimiters, line endings, and embedded newlines rather than splitting blindly.'],['normalization','Data normalization can make equivalent representations easier to compare and index.'],['schema validation','Schema validation turns assumptions about input shape into explicit executable checks.'],['streaming validation','Streaming validation checks records incrementally without requiring the entire dataset in memory.'],['indexeddb transaction','IndexedDB operations are transactional and asynchronous; callers should design around transaction lifetime.'],['array sort','Modern JavaScript specifies stable Array.prototype.sort behavior, preserving order among equal keys.'],['binary data','ArrayBuffer, DataView, and typed arrays support explicit binary parsing and serialization.'],['frequency table','Frequency maps summarize categorical data by counting normalized keys.'],['moving average','A moving average smooths local variation by averaging a sliding window of observations.']],
 web:[['etag','ETag identifies a representation version and supports conditional requests with If-None-Match.'],['cache control','Cache-Control communicates caching directives such as max-age, no-store, and private.'],['http status','HTTP status codes distinguish successful, redirected, client-error, and server-error outcomes.'],['problem details','Problem Details provides a standard structure for describing HTTP API errors.'],['websocket','WebSocket maintains a bidirectional connection after an HTTP-based upgrade handshake.'],['server sent events','SSE provides a long-lived HTTP response stream for server-to-client event delivery.'],['fetch streams','Fetch response bodies can be consumed as streams when incremental processing is useful.'],['range request','HTTP Range requests allow clients to request selected byte ranges of a resource.'],['content encoding','Content-Encoding describes representation compression such as gzip or br and is distinct from media type.'],['idempotency key','An idempotency key lets an API associate repeated requests with one intended operation when the server implements the contract.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V23=Object.entries(packs).flatMap(([domain,items])=>items.map(([topic,text])=>({domain,topic,text,source:'authored-v23'})));
export const LARGE_JAVASCRIPT_KNOWLEDGE_V23_TEXT=LARGE_JAVASCRIPT_KNOWLEDGE_V23.map(x=>`[${x.domain}] ${x.topic}: ${x.text}`).join('\n');
export const javascriptKnowledgeV23Stats={version:23,entries:LARGE_JAVASCRIPT_KNOWLEDGE_V23.length,domains:Object.keys(packs).length,pretrained:false,authored:true};
if(typeof window!=='undefined')window.TONYLargeJavaScriptKnowledgeV23={LARGE_JAVASCRIPT_KNOWLEDGE_V23,LARGE_JAVASCRIPT_KNOWLEDGE_V23_TEXT,javascriptKnowledgeV23Stats};
