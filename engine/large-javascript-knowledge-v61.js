// TONY Large JavaScript Knowledge v61 — authored deterministic knowledge pack.
// This is source knowledge for the pure-JavaScript LargeLM; it is not pretrained neural data.
const packs={
  language:[['Coercion','Use explicit conversion at API boundaries so implicit coercion does not silently alter program behavior.'],['Equality','Use strict equality by default and Object.is when NaN or signed zero semantics matter.'],['Numbers','Prefer Number.isFinite and explicit integer checks when validating numeric inputs.'],['BigInt','Do not mix BigInt and Number arithmetic without an intentional conversion because operators generally require matching numeric types.'],['Symbols','Symbols provide non-string property keys and are useful for protocol hooks and collision-resistant internal metadata.'],['WeakMap','Use WeakMap for object-associated metadata when entries should not keep keys alive.'],['Descriptors','Property descriptors control enumerability, writability, configurability, and accessor behavior.'],['Proxy','Proxy traps can enforce invariants but should remain small because proxies complicate debugging and optimization.'],['Reflect','Reflect APIs provide predictable primitive operations that compose well inside proxy traps.'],['PrivateFields','JavaScript private fields are enforced by the language and cannot be accessed through ordinary property lookup.']],
  modules:[['LiveBindings','ES module imports are live bindings, so imported values can observe later updates from the exporting module.'],['DynamicImport','Dynamic import returns a promise for a module namespace and enables lazy feature loading.'],['TopLevelAwait','Top-level await can delay dependent module evaluation, so use it deliberately in startup-critical modules.'],['PackageExports','Package exports define supported entry points and can prevent consumers from reaching internal files.'],['ConditionalExports','Conditional exports can select environment-specific entry points while keeping one package interface.'],['ImportMaps','Browser import maps map bare specifiers to URLs and should be scoped to the application deployment.'],['Cycles','Circular module dependencies are valid but can expose partially initialized bindings during evaluation.'],['TreeShaking','Tree shaking works best when modules use static ESM syntax and side effects are explicit.'],['SourceMaps','Source maps preserve source locations through bundling and minification, improving production debugging.'],['ModuleNamespace','A module namespace object exposes the module’s exported bindings through a stable namespace interface.']],
  async:[['Microtasks','Promise callbacks run as microtasks and can starve rendering if code continually schedules more microtasks.'],['AbortSignal','AbortSignal provides composable cancellation state for fetches and other asynchronous operations.'],['PromiseAll','Promise.all is fail-fast and preserves input ordering in its fulfilled result array.'],['AllSettled','Promise.allSettled reports every outcome and is useful when independent tasks should all finish.'],['Any','Promise.any fulfills on the first success and rejects with AggregateError when every input rejects.'],['AsyncGenerators','Async generators combine lazy production with awaitable iteration for streaming asynchronous data.'],['ForAwait','for await...of consumes async iterables and should allow cleanup when iteration exits early.'],['Retry','Retries should distinguish transient failures from permanent validation or authorization failures.'],['Concurrency','Bound concurrency with worker pools or semaphores instead of launching unbounded asynchronous work.'],['Finally','Promise.finally is appropriate for cleanup that should occur regardless of fulfillment or rejection.']],
  browser:[['EventDelegation','Event delegation attaches a handler to a stable ancestor and uses event.target or closest for dynamic children.'],['CurrentTarget','event.currentTarget identifies the element whose listener is executing, while event.target identifies the originating target.'],['CustomEvent','CustomEvent provides application-level DOM events and can carry structured detail data.'],['DocumentFragment','DocumentFragment lets multiple nodes be assembled before one insertion into the live DOM.'],['Template','HTML templates keep inert markup that can be cloned into the document without parsing each fragment manually.'],['PointerCapture','Pointer capture lets an element continue receiving pointer events after the pointer moves outside its bounds.'],['ShadowDOM','Shadow DOM encapsulates markup and styles while defining explicit event and slot boundaries.'],['MutationObserver','MutationObserver batches DOM mutation notifications and should be disconnected when no longer needed.'],['ResizeObserver','ResizeObserver observes element size changes without repeatedly polling layout.'],['IntersectionObserver','IntersectionObserver efficiently detects visibility relationships for lazy work and viewport-driven behavior.']],
  web:[['FetchCloning','Request and Response bodies are streams; clone them before consuming a body that must be read twice.'],['Headers','Headers normalizes HTTP header names and provides a structured interface for request and response metadata.'],['URLSearchParams','URLSearchParams encodes query parameters and handles repeated keys predictably.'],['WebSocket','WebSocket provides bidirectional messaging but requires application-level authentication and reconnection policy.'],['EventSource','EventSource supports server-sent events and automatically manages a persistent event stream.'],['BroadcastChannel','BroadcastChannel communicates between same-origin browsing contexts and should be closed when unused.'],['MessageChannel','MessageChannel creates two linked MessagePorts useful for explicit component-to-component messaging.'],['Blob','Blob represents immutable binary data and can be used with object URLs or upload APIs.'],['ObjectURLs','Object URLs should be revoked after their associated Blob or File is no longer needed.'],['Clipboard','Clipboard APIs are permission-sensitive and should be used only in an appropriate user-initiated context.']],
  streams:[['Backpressure','Backpressure prevents a fast producer from overwhelming a slower consumer.'],['ReadableStream','ReadableStream exposes pull and cancel behavior for incremental data production.'],['WritableStream','WritableStream coordinates writes and provides backpressure through its writer.'],['TransformStream','TransformStream composes streaming input and output while allowing incremental transformation.'],['TextDecoderStream','TextDecoderStream converts byte streams into text while correctly handling split UTF-8 sequences.'],['Tee','ReadableStream.tee can split one stream into two branches, but both branches affect buffering behavior.'],['Framing','Streaming protocols need explicit framing so partial chunks are never mistaken for complete messages.'],['IncrementalJSON','Incremental JSON processing should use a parser designed for incomplete input rather than naive chunk concatenation assumptions.'],['Uploads','Streaming uploads can reduce peak memory when the server and browser support request streaming.'],['Compression','CompressionStream and DecompressionStream can provide streaming compression without buffering an entire payload.']],
  node:[['HTTPTimeouts','Node HTTP clients should configure connection, headers, and request timeouts rather than relying on indefinite waits.'],['KeepAlive','Keep-alive agents reuse connections and can reduce latency, but idle pools need bounded configuration.'],['FileDescriptors','Open file descriptors are finite resources and should be closed deterministically after use.'],['PathRoots','Resolve filesystem paths against an intended root and reject traversal outside that root.'],['Environment','Environment variables are configuration inputs and should be parsed and validated before use.'],['Signals','SIGTERM handling should initiate graceful shutdown and allow a bounded drain period.'],['WorkerThreads','Worker threads can isolate CPU-heavy JavaScript from the main event loop.'],['DNS','DNS results can vary over time; clients should respect TTL and avoid assuming one address is permanent.'],['TLS','TLS configuration should verify certificates and use modern protocol defaults.'],['AsyncLocalStorage','AsyncLocalStorage can associate request-scoped context across asynchronous callbacks.']],
  security:[['Authorization','Authentication establishes identity; authorization must independently verify whether the identity may perform the requested action.'],['CORS','CORS controls browser cross-origin reads and is not a substitute for server-side authorization.'],['CSRF','CSRF defenses protect state-changing browser requests from unintended origins and commonly combine tokens or SameSite cookies.'],['SameSite','SameSite cookie attributes reduce cross-site request exposure but should be paired with server authorization.'],['CSP','Content Security Policy can restrict script and resource sources and reduce exploit impact.'],['TrustedTypes','Trusted Types can enforce safer DOM injection patterns for dangerous sinks.'],['SRI','Subresource Integrity lets browsers verify expected hashes for externally hosted resources.'],['PermissionsPolicy','Permissions Policy restricts access to selected powerful browser capabilities by origin.'],['ReferrerPolicy','Referrer-Policy controls how much referrer information is sent with requests.'],['HSTS','HTTP Strict Transport Security instructs browsers to prefer HTTPS for a host after policy activation.']],
  performance:[['LayoutThrashing','Avoid alternating DOM writes and synchronous layout reads in tight loops because it can trigger repeated layout work.'],['Animation','requestAnimationFrame aligns visual updates with the browser rendering cycle.'],['Debounce','Debouncing collapses bursts into one delayed action and is useful for search or resize handlers.'],['Throttle','Throttling limits handler frequency during sustained input such as scrolling.'],['Virtualization','Virtualization renders only the visible subset of large collections to reduce DOM and layout cost.'],['ContentVisibility','content-visibility can let the browser skip rendering work for offscreen content.'],['Memoization','Memoization helps repeated pure computations when cache growth is bounded.'],['PerformanceObserver','PerformanceObserver consumes browser performance entries without repeatedly polling performance buffers.'],['LongTasks','Long tasks block the main thread and should be split, yielded, or moved to workers when practical.'],['ResourceTiming','Resource Timing exposes network timing details useful for identifying slow assets and connections.']],
  testing:[['PropertyTests','Property-based tests validate invariants across many generated inputs rather than a few examples.'],['MetamorphicTests','Metamorphic testing checks relationships between outputs when inputs are transformed in known ways.'],['DifferentialTests','Differential tests compare two implementations or versions on the same generated cases.'],['RaceTests','Concurrency tests should deliberately vary scheduling to expose ordering assumptions.'],['Fuzzing','Fuzz tests should enforce bounded time and memory budgets while exploring malformed inputs.'],['GoldenTests','Golden tests compare stable serialized outputs and should make intentional updates explicit.'],['ContractTests','Contract tests verify that component interfaces preserve agreed request and response semantics.'],['CleanupTests','Tests should verify resource cleanup such as timers, listeners, workers, streams, and temporary files.'],['RegressionTests','Every fixed production defect should gain a focused regression test when practical.'],['DeterministicRandomness','Seeded randomness makes stochastic tests reproducible while still exploring multiple generated cases.']],
  architecture:[['DependencyInversion','Depend on stable interfaces rather than concrete infrastructure so core logic remains independently testable.'],['CommandsQueries','Separate commands that mutate state from queries that return state to simplify reasoning about side effects.'],['Idempotency','Idempotency keys prevent retries from applying the same externally visible operation more than once.'],['Leases','Leases bound ownership over time and should include renewal and expiry behavior.'],['BoundedQueues','Bounded queues provide explicit overload behavior instead of allowing memory growth without limit.'],['CircuitBreakers','Circuit breakers stop repeatedly failing dependencies from consuming all local resources.'],['Bulkheads','Bulkheads isolate resource pools so one overloaded workload does not exhaust every execution path.'],['DeadLetters','Dead-letter queues preserve messages that cannot be processed after a defined retry policy.'],['Readiness','Readiness indicates whether an instance should receive traffic, while liveness indicates whether it is functioning.'],['Observability','Structured logs, metrics, traces, and correlation identifiers make distributed failures diagnosable.']],
  data:[['MapSet','Map and Set provide explicit collection semantics and avoid many accidental object-key coercions.'],['NullPrototype','Object.create(null) is useful for dictionary-like maps when inherited properties must be excluded.'],['TypedArrays','Typed arrays expose fixed-width numeric views over binary memory.'],['ArrayBuffer','ArrayBuffer owns raw binary memory while typed arrays and DataView provide structured views.'],['DataView','DataView reads and writes binary values with explicit offsets and endianness.'],['StructuredClone','structuredClone copies supported data structures and can transfer selected binary buffers.'],['Sorting','toSorted returns a sorted copy while sort mutates the original array.'],['ImmutableUpdates','Immutable updates make state transitions easier to compare and reason about in UI architectures.'],['Schemas','Runtime schemas validate untrusted data at boundaries before business logic relies on its shape.'],['CanonicalSerialization','Canonical serialization gives equivalent data a stable representation for hashing, caching, and signatures.']]
};
export const LARGE_JAVASCRIPT_KNOWLEDGE_V61_TEXT=Object.entries(packs).flatMap(([domain,items])=>items.map(([topic,text])=>`${domain}: ${topic}. ${text}`)).join('\n');
export const javascriptKnowledgeV61Stats=()=>({version:'61',packs:Object.keys(packs).length,entries:Object.values(packs).reduce((n,x)=>n+x.length,0),domains:Object.keys(packs),authored:true,pretrained:false});
export default LARGE_JAVASCRIPT_KNOWLEDGE_V61_TEXT;
